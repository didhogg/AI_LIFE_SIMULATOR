# P0 补漏执行排程 · 蓝图二次增生审计 · 剩余对撞面评估

<aside>
🧭

**本页性质**：把 [P0 中途补漏清单 · 修订提案（二十一批＋纪元·离场演化双构架裁定＋增生审计八项裁决：机制九项｜系统操作九缺口｜元指令与作弊七缺口｜周目体系四缺口｜mod 包生态七缺口｜多人接口面七缺口｜多域时钟七缺口｜知情过滤七缺口｜状态机并发七缺口｜数值边界七缺口｜动词表六缺口｜开放串治理六缺口｜受众选择器六缺口｜级联触发六缺口｜Resolver签名六缺口｜切片预算六缺口｜规模长跑六缺口｜离场演化纠缠闭包｜重试主权｜记账语义保真六缺口｜酒馆宿主借力四档｜媒介渲染与多源消息流十五缺口｜迁移升版×快照树×指纹重放七缺口｜宿主边界十三缺口）](https://app.notion.com/p/P0-mod--948082e9e15a452083c1894b90204e6d?pvs=21)（十七批＋两次构架裁定）的全部条目按 [P0 执行手册 · 可操作步骤·执行清单·测试流程](https://app.notion.com/p/P0-66188416da9e48f1af13d75b7553c5bf?pvs=21) 的 P0-1～P0-11 → P1 → P2 执行顺序归位，每阶段内按轻重缓急降序；随后逐条对照 [「AI 文游人生模拟器」V4.1 全架构蓝图：顶层设计 · 分模块详解 · 沙盒推演范例](https://app.notion.com/p/AI-V4-1-98a02170416943b780cbc8ae1e0a6f67?pvs=21) 做二次增生 bug 审计（第二部分），最后评估剩余需要沙盒对撞的面（第三部分）。

**分级图例**：🔴 死档级 / 推倒重来级 / T0 ｜ 🟠 高危 / 安全级 / 确定性级 / 死锁级 ｜ 🟡 中危 / 接口级 / 规格级 ｜ 🟢 注释级 / 枚举级 / 口径级。条目标注【批次·编号】，详规一律回查补漏清单原文，本页只做排程与索引。

</aside>

# 一 · 执行排程总表

### 🆕 排活 · AA-合并会（提前到 P0-6 开工前 · 产出三件）

- [x]  重放器**立项派 owner ＋ 编号**（owner = 蔡怡家佳·单人 vibe coding 自任 ✅ · 编号 REPLAY-01）
- [x]  重放器**输入集十类·排程负责人确认冻结**（2026-06-15·= §九全枚举：初始快照／指纹取材集〔含 DSL 文法版＋求值器函数库版〕／意图序列／盐值序列〔读 tick_log 记录值〕／失败工单／调用世代号／外部注入序／选择器展开落账／观测值表＋挂起队列／域累计活跃区间表；重放中 LLM 调用全短路读账）——REPLAY-01 解除阻塞、可进 5(b)
- [x]  **八场景测试清单·排程负责人确认冻结**（2026-06-15）：悔棋后旧任务迟到／fork 瞬间在途／快进多任务乱序／关账边界掉落／重试与原调用同时返回双落账／跨版本段重放×难度段切换(AA8)／宿主世代×引擎世代双层乱序(AA10)／迁移边界×在途调用

### 🆕 排活 · AA3 DoD 基准面豁免清单（P0-10 动工前 · 半天会）

- [x]  逐条过旧卡行为清单，每条打标签 **保留** ／ **豁免（写理由·标第几轮改的）**
- [x]  豁免清单存 docs/spec；P0-10 分两组判：保留组全对／豁免组按新规则重录标准答案

### 🆕 排活 · C 信念瘦身（独立破坏性 PR · 会动老存档）

- [x]  schema 删 `强制度`／`异端容忍`，收编进组织属性轴（P0-1）＋ 迁移映射（P0-2）＋ 全局 grep 旧路径派生 ✅CC A1/A2（f0c7288·复核 7098595）：org.ts:165-168 信念仅留官方体系·思潮派系·防回归断言 schema.test.ts:670-674·engine grep 0 命中·migrate.ts 读旧写新为唯一合法残留
- [x]  **蓝图同步**：模块 8 大框架行 ＋ 模块 6/8 范例旧路径一并改（grep 归零才算交账）✅CC A4：[blueprint.md:1029](http://blueprint.md:1029) 框架行已改·grep 活跃旧路径返回空（剩余仅迁移注记/出厂轴说明）
- [x]  单独 PR、单独可回滚、不与零迁移批混 ✅CC A3：migration.test.ts:590-636 六测试（fixture/抹除/二次幂等/合并保护/no-op）·fixture stat_data_v31_rich.json 录入大明皇朝 强制度=75·异端容忍=30

## 文档与决议（与各批并行 · 实际优先级很高）

- [ ]  🔴【第三批落地·3-1】全仓 grep「拍号 vs 时刻」逐处替换（律 X 正名「全局时刻轴」）已写入蓝图
- [ ]  🟠 全部已拍板口径同步进 [修订决议](https://app.notion.com/p/AI-V4-1-a9d51518f9f747a29d9880bcf1d902df?pvs=21) 与 `docs/spec/*.md`——**coding AI 按旧口径写代码是系统性风险**，每批拍板后立即同步，不攒
- [ ]  🟢 G8 回想截断 / 观测史定性 / 两窗分野 / 串治理键空间清单 / 结构洞定性 / manifest 规格补全 / 律X·律Y·分层律入决议 / 1.7b 增补
- [ ]  🟡【A1·六·三十二 已贴入·最高优先】蓝图 4.11 备忘③旧盐口径——**已复核：蓝图现版已是新口径**（盐=存档头计数器·重掷序号=演出层草稿计数永不进盐）；剩 docs/spec 同步
- [ ]  🟢【C1/D1/D2/E1·六·三十二 已贴入】C1「组边界」四处（6.64/6.42⑦/6.41/4.1）**已复核蓝图均为组边界**；D2 6.42 importer 标注**已修**；剩 D1 三十「弃用清单」补节 + E1 三十总纲四「约束类补丁按 K5 取严」半句（补漏清单侧文档）
- [ ]  🟡【增审3/6·已拍板 ✅】6.69 删楼门控补一行（涉知情扩散楼层强制检查点回滚、补偿通道仅限数值/状态域）；元层旋钮双轨统一回写三处（写类 = 组边界＋UI 回执 / 停机类 = 最近内部拍边界 / 新参数不追溯在途调用）；6.43「最高序号=采纳」句废除回写
- [ ]  🟢【三十·卡格式·已拍板 ✅】可分发卡三句口径写进 docs/spec＋决议：「占位=解析槽、渲染时填名」「导入预设是一种落账来源」「可分发卡 = NPC 包带关系声明超集」
- [ ]  🟢【AA13·五 已贴入】纪元轴世界域归属明文（每域一张·跨域独立推进·6.66 切段锚点按所在域纪元轴解析）→ 6.36/6.62 文档
- [ ]  🟢【三悬案/AA1–AA13 文档·四五 已贴入】三悬案裁决（#1/N2/N4）＋AA1–AA13 口径同步进决议与 docs/spec
- [ ]  🟢【第三批落地·2-3】顶部目录/callout 已同步到 6.73
- [ ]  🟢【第三批落地·3-14】模块14 自愿换角措辞（已对·并入 2-2）
- [ ]  【第三批落地·接口冻结】3-8A（6.66）/3-9A（寻路备忘）/3-10（6.63）确认已在蓝图、无需另写

## P0-1 · schema 包补丁批（含 P0-1x 接口文件）— 状态：P0-1 已完成 → 打补丁 PR

> 窗口性质：白名单派生（P0-6）尚未开工，schema 改动免费传导。**注意：本批并非全部零迁移**（见审计 #11）。
> 
- [ ]  🔴【反向核查·G-1·誊写未完】4.2 世界域加 `累计活跃区间表[]` 字段 + 域钟标派生展示缓存（域钟=派生自它·6.54 D5）——代码已落 world.ts:68（e8cd4e5）、蓝图已改，残留 = 4.x 变量总表誊写 + C3 双时钟多人注释（焊死前誊完，勿跨焊死点） ❓-代码已落·4.x总表誊写未完
- [ ]  🔴【反向核查·G-1·未完】4.2 世界域「累计活跃区间表」字段+域钟派生缓存 schema 已落 world.ts:68（e8cd4e5）·蓝图已改；⚠ 残留：C3 双时钟多人注释 4.x 总表誊写待补（故 uncheck） ❓-代码已落·C3注释/总表誊写未完
- [ ]  🔴 **【H篇】** 把内部信念写成可逻辑推理的格式，交给符号求解器（ASP/Clingo）在行动前校验一致性，而不是再问一次 LLM。→ 治 P–R–B 信念派生"由谁校验"的空白。 ❓-论文新增·校验层未实装
- [ ]  🔴 **【L篇 + One Life篇】** 动作/转移的标准结构 = 前置条件(precondition) + 效果(effect)；One Life 更进一步把整个转移函数拆成"小概率律"的混合，每条律只改状态的一个窄切片。→ 治动词表无统一结构、效果范围不受控。 ❓-论文新增·动词结构未实装
- [ ]  🟡【第三批落地·3-6】知情分治 W1 schema 注释（知情类 append-only／印象类可衰减）已写入蓝图 ❓-已写入蓝图·schema 注释未落
- [ ]  🟡【三十二·A2·彩蛋池装配器直写】记忆种子三件拆两通道：关系声明/实体/账面走五道闸；`$隐藏记忆库彩蛋池` 落 `$` 层走引擎装配器直写（第③闸前缀权限零特例·`$运气` 注册表写入同族）——禁把「过五道闸」字面执行到 `$` 层写入·装配器规格一行 ❓-装配器规格·实装未落
- [ ]  🟡 **Γ 六类约束要进 schema**：变量类型 t、对立关系 opp、**唯一性 ∃!（一个角色同时只能在一处 at(char,∃!place)）**、复现重要度 r、资格 qualif、目标母题 motif。治 bug：NPC 同时在两地、矛盾事实并存、分支任务目标互不相干。 ❓-论文新增·约束 schema 未落
- [ ]  🟢 **quest motif 保证一棵任务树的分支共享连贯目标**。治 bug：同一任务的不同分支跑去做毫不相关的事。 ❓-论文新增·未实装
- [ ]  🟢 **人格 5 轴设为"不可漂移锚"**（正篇身份漂移：Isabella 兴趣被他人同化）。治 bug：长线后 NPC 人设被对话侵蚀、千人一面。 ❓-论文新增·人设漂移防护未实装
- [x]  ⏳【第三批落地·3-8B】信念瘦身设计已写入蓝图（4.4/4.11），实装六步见执行单（独立 PR·会动老存档） ❓-独立PR待跑·会动老存档
- [x]  🟡【第三批落地·3-5】NPC 占位补「模板快照?」（4.3·与组织/地点对齐）已写入蓝图 ❓-已写入蓝图·schema 实装未落
- [x]  🔴【第三批落地·3-4】席位表升格（4.3·镜头焦点角色 单指针→席位表真结构·C1 推倒重来级）已写入蓝图 ❓-已写入蓝图·schema 实装未落
- [x]  🔴 动词信封 `提案血统?`＋⚠失败工单结构定稿＋覆写日志 `提案单引用?`（6.68 Z3/Z5 地基） ❓-Z3/Z5 schema 地基·焊死前补未落
- [x]  🔴 **人格 facet 轴 + 社会角色定义表 + 权重表 w_i,k + δ 效应量表**（P–R–B）。NPC 信念派生的全部**输入**要进 schema（派生结果不存）。防 bug：没有 schema 锚，信念派生无处取数 → NPC 信念只能写死或拍脑袋，回到"人格→个体信念"断链。 ✅完结 3294a23（性格五轴.facet? optional＋玩法预设 社会角色 3 表·以 P0-6 主战场 L-1/L-6 为准·信念派生管线 defer P0-8）
- [x]  🔴 **认知档案须存"观测时的状态快照"，不是只存"看得见"**（正篇环境树：*Agents are not omniscient: their tree gets out of date as they leave an area, updated when they re-enter*）。治 bug：NPC 离开房间后仍"知道"该房间当前真值 → 知情过滤只挡了"现在看不见"，没挡"我离开时它是旧状态"。认知档案的每条印象要带**观测拍号 + 当时快照**。 🟢 观测拍号 ✅完结 ab3bb14（印象条目Schema.观测拍号?:int）｜当时快照 ✅完结 3294a23（印象条目.当时快照?所在地点+情绪键·字段白名单子集·以 P0-6 主战场 L-2 为准）
- [x]  🟡 **【L篇】** 实体指代消歧（稳定实体 ID + 别名归一）✅完结 ab3bb14（复用 归并条目Schema/键空间归并表/角色ID·禁第二实现·零代码变更·以 P0-6 主战场 L-11 为准）
- [x]  🟡 **【G篇】** 生成前把世界圣经 + 资产白名单 + 可行走坐标枚举进 schema，约束 LLM 的取值空间。✅完结：可行走?＋资产白名单 ab3bb14｜世界圣经 3294a23 复用 _lore知识库（以 P0-6 主战场 L-3 为准·零代码变更）
- [x]  🟡 **记忆条目四要素**（自然语言 + 创建拍 + 最近访问拍 + 重要度=复用「权重」字段·不新增「重要度分」）✅完结 ab3bb14（权重 memory.ts:32 加注「重要度点数·召回排序主权重」·四要素已齐·伪缺口消解·以 P0-6 主战场 L-4 为准·L-21 前置满足）
- [x]  🟡 **地点 schema 物理规范字段**（容量/营业时间/活动类型）✅完结 ab3bb14（地点条目Schema.容量?/营业时间?/活动类型?·单双人语义并入容量·以 P0-6 主战场 L-5 为准）
- [x]  🟡 **粒度决策：30 facet vs 你的 5 粗轴**（P–R–B）。建议折中——5 轴对外、每轴挂 facet 子结构可选展开。防 bug：直接上 30 facet 会和你既有 5 轴属性系统双轨打架（你最痛的"十对双轨"老毛病）。 ✅完结 3294a23〔性格五轴.facet? optional·以 P0-6 主战场 L-1/L-6 为准〕
- [x]  🟡 **角色激活阈值字段**（>0.6 / <0.4，P–R–B）做成 config 可空字段。防 bug：阈值写死在代码里 → mod/校准期无法调，且不进指纹。 ✅完结 3294a23（角色激活配置 激活上限/沉默下限·叙事旋钮·以 P0-6 主战场 L-7 为准）
- [x]  🟡 **越界分类法枚举**（PANGeA）：Off-Topic（时代错置/地域/泛化）+ Cheating（提示泄漏/未来视/物理违规/NPC 黑入/越权技能）作为 config 枚举。防 bug：校验闸若硬编码违规类型 → 无法扩展、无法 mod、无法按世界域开关。 ✅完结 3294a23（二审维度条目.越界类型 enum〔Off-Topic,Cheating〕·以 P0-6 主战场 L-8 为准）
- [x]  🟡 **任务/剧情步骤建模成 STRIPS planning（precond/effect）+ 语义完整性约束 Γ**（quests 篇：F 原子、operator=(name,precond,effect)、初态 Sj、目标 Gi）。直接对接你**动词表 + 五道闸**：precond=入闸条件、effect=结算。治 bug：生成出违反世界约束的步骤。 ✅ precond/effect 完结 3294a23（动词Option基础Schema.precond?＋effect_decls?·以 P0-6 主战场 L-9 为准·Γ 六类约束 defer P0-4）
- [x]  🟢 **角色/信念定义可外置为 mod 结构**（P–R–B 可迁移性）schema 预埋。防 bug：后期 mod 想加"巡检员/谏官"等角色时被迫改核心 schema。 ✅完结 3294a23（复用既有 实体模板库Schema.NPC模板 slot·零代码变更·以 P0-6 主战场 L-10 为准·registry flip 待 G-a/G-b）
- [x]  ✅【schema 已落 002e48d·见 P0-6 B-b·测补 f6a3135·doc 7165b90/bb30a1f】🔴 **U3a 存档头「引擎版本谱＋迁移戳[]」**（{源版本, 目标版本, 迁移映射哈希, 墙钟纯展示}）＋时间线分块写入版本标记
- [x]  ✅【已落 memory.ts:181·见 P0-6 B-c】🟠【反向核查·G-4·schema 待誊】4.8 加 `触发扫描器状态{观测值表, 挂起命中队列}` 字段（观测史落账·随拍前快照回滚·随线分叉·只迁形状不重算·J3/J6）——蓝图已改、schema 待誊，焊死前誊完
- [x]  🟡【DP·偏好字段】$玩家偏好 加 动态提示词?{启用?:boolean(默认true·explicit/light 自动开),云端源?,刷新频率?,已缓存版本?,已缓存内容哈希?}（玻璃箱可见开关·偏好层不进盐·入排除名单）✅ GW 落地 commit 7ae2f3b（dollar.ts:102·隐性排除·EXCLUDED 文档 defer B6-Step7）
- [x]  🟡【DP·引子来源】$模型画像.破限引子 / 叙事条目覆盖串加 来源?:"引擎内置"|"云端缓存" 标记 + 云端策略包 版本?/内容哈希? 字段（叙事层·入 FINGERPRINT_EXCLUDED_FIELDS·零迁移）✅ GW 落地 commit 7ae2f3b（dollar.ts:199·$ 前缀 invisible·fingerprintManifest.ts 零 diff）
- [x]  🟡【slice 实证·账本 per-entity 口径钉死·2026-06-16·P0-1 shape 全完结 2357afb→a06152d→c9e53d5·P0-7 守恒接线另列 P0-7 段】slice M5 平账确认：`货币系统.账户` 须明确为 **per-entity 账本**（按实体键索引现金/应收/应付），守恒断言 `Σ全实体(现金+应收−应付)=常数` 跨**所有**实体成立（slice 已如此实装：林九/王掌柜/红姨各持余额 + `assertTrialBalance`）。蓝图模块 11 `货币系统.账户` 口径偏主角中心、读似单一账户——**焊死前钉死 per-entity 语义**，否则 NPC/组织/银行当铺（万物皆组织）的现金容器与守恒闸边界留洞、债权（约定库缔约方=债主）落不到对称对手方。✅ GW 语义注释已落 commit 7ae2f3b（economy.ts:31 前置注释 +:81 行注释·形状不变·账户键=实体键约定）·⚠ 形状变更（账户Schema→z.record(实体键,账户Schema)）走独立账本 per-entity 化迁移批（破坏性·排 B6 Step6 白名单派生 fire 前·**✅ shape 完结 commit 2357afb·2299 绿·lint 220·三取材组逐位恒等·红线零 diff**）——**拍板（2026-06-17·Step0 侦察后）**：backfill=**选项 B 空 map 不预填**（零假设·一举消除「镜头焦点≠账户所有者／焦点为空／悬空 NPC」三雷·侦察确认 `_席位表[席位id].焦点角色键` 仅会话本地视角、引擎结算不读、不可作 owner 键）；幂等门=**Shape 嗅探**（检测 账户 首值是否已含 持有/储蓄 等账户字段·非 migration_version 版本号门）；**本批仅 shape**：economy.ts:81 `账户:z.record(z.string(),账户Schema).default({})` + migrate.ts:1110 链末插 `backfill货币账户PerEntity`（旧单例形→{} · record/空→no-op）+ whitelistDryRun.ts:236 probe→`货币系统.账户.{id}.持有.{id}` + schema.test.ts 6-8 条（1277-1310 + 2220 + 1643 可写键串）；货币系统在 BUNDLE/PRESET/SNAPSHOT 三取材组零命中→黄金向量逐位恒等不受影响。⚠ **守恒断言已拆两条（2026-06-18 拍板）**：① **纯函数（工具）→ P0-6**（见 P0-6 段「守恒断言 core」行）；② **接进 `runTick` 结算管线 → P0-7**（见 P0-7 段「守恒断言接线」行）。**🟢 ① core 纯函数 = ✅完结（2026-06-18·commit 00bb3ed·提前于 P0-6 落地·从 slice assertTrialBalance server.ts:117 抽共享口径·禁第二实现·与三人债务模型解耦·本 shape 批不落接线）**。⚠ soak 补 debt/试算平衡不变量已记于 P0-8 赊账实证行（repro `--seed 12345 --runs 1`）。
- [x]  🟠【新排查·4.7 经济层「应收/债权人」缺口·2026-06-18·✅完结 commit a06152d】4.7 `货币系统.账户` 命名了 `持有`(=现金) 与 `负债`(=应付·→约定库引用·内含 `债主` 字段)，唯独**守恒断言 `Σ全实体(现金+应收−应付)=常数`（本批行注已写）的「应收」项在账户 schema 内无命名容器、无派生来源声明**——债权人(债主)的对称债权只隐式藏在债务方 `负债→约定库引用` 的 `债主` 字段，A 借 B 时仅 B.负债 落账、A 侧无对称应收落点。债务方建模完整、债权方(应收)缺位 → **当前守恒断言批·选项A 的 `getNetAsset=现金+应收−应付` 取不到「应收」**(护城河②无法断言)。**✅ 拍板（2026-06-18·选 a·今天跑完）**：账户Schema 加**显式 `应收` 字段**与 `负债` 对称、**双分录存储**（缔约即同时落债务方应付腿＋债权方应收腿）、**原子事务保证两腿同步**（同一事务/worklist 内提交·禁半落账）；getNetAsset 从存储的 应收/应付 现算，与 slice 实证（6.77 模块11 存储式 assertTrialBalance·应收恒==欠账）对齐、消除原则 (b) 张力。⚠ **一处待 CC 侦察后定**：负债现为 `Record<string,string>`（约定库键引用·非数值）→ 应收对称取 `应收{[债]:约定库引用}`（引用式·金额单源仍在约定库）抑或数值腿，CC 侦察账户Schema 后拍此一处。落点 = **P0-1 schema 窗口**（账户Schema 加字段 + migrate.ts backfill + whitelistDryRun probe + schema.test schemaKeys/指纹复核·与上方 per-entity 行同族）·**独立 shape 批（非 assertConservation core 批·见下守恒拆分）**。**✅ 完结（2026-06-18·commit a06152d·选 i 引用式）**：账户Schema 加 `应收:z.record(z.string(),z.string()).default({})`（镜像负债·金额单源在约定库）＋migrate backfill 逐实体补 `应收:{}`（幂等 Shape 嗅探·migration_version bump 对齐 migrateS1S1b·不在三取材组）＋whitelistDryRun 自动派生 2 entry（应收 record＋open-string leaf·不入 VERB_TARGET_PROBES）＋schema.test 9 新测试。验收全绿：tsc 零新增·test 2313(+9)·lint 220·schemaKeys 52·指纹 84·黄金向量逐位恒等·REPLAY-01 22/C2 17·红线 diff 空·双机迁移恒等＋二次幂等。⚠ AA4 新 record 面 `货币系统.账户.{id}.应收`(Record<string,string>) 须纳入 JS 保留键黑名单（governedKeySpace.ts:34·随 B6-Step8 fire）；getNetAsset 应收腿回约定库解析金额 = P0-7 守恒接线（见 P0-7 段）。
- [x]  🟡【K2/K3/K4·6.52·schema 半·2026-06-18 结清】占位形态 `模板快照?` + 血统元数据只读化（**schema 部分**）：NPC(4.3)+org(9367930·4.4) 占位形态+模板快照已落；血统只读化✅（来源包→_来源包·模板引用→_模板引用·模板快照→_模板快照·actor/org/secret三处·31e6659+113e6ba）。⚠ **enforcement 半（键名冻结策略=闸逻辑）已移 P0-6 正文**（见 P0-6 段「K2/K3/K4 键名冻结 enforcement」行）·schema 此处结清、不再吊 P0-1 闸一。
- [x]  🟢【最小事件密度钩子】系统.事件来源权重 ＋ 触发概率可被外部密度控制器调制、非只读常数（机制后做钩子现在留·与 §三久旱保底并轨）✅ GW 落地 commit 7ae2f3b（system.ts:61 外部密度系数?·机制实装 defer P0-7）
- [x]  🟡【临时容器=易失态铉死·对撞⑥】临时对话隔离容器明文为**快照外易失态**：不进拍前快照 ring buffer·不进 U1 迁移面·不进重放输入十类·崩溃即弃（同中性暂存 sessionStorage）——防实装者塑进真相树变跨拍可变态 ✅ CC 本轮（1486绿/26 files）：system.ts 临时会话Schema（快照外易失态）+ index.ts $临时会话 入 BLUEPRINT_KEYS+RootSchema（键 48→49）
- [x]  🔴【D2/D7·6.54·✅e8cd4e5 world.ts】全局拍轴 T0 定性注释 + 两类时间字段两分（拍轴=唯一时间真相主键、域钟=派生展示量；全局拍号 vs 域历法时刻彻底两分）——⚠ 实装前先过审计 #1 的术语澄清
- [x]  🔴 P0-1 schema（可空零迁移 · 入指纹排除名单 · 不影响判定·✅ 1c725aa+113e6ba：注册表／模型画像采样参数（四键结构）＋预算控制台三覆盖层全落·入排除名单＋6条断言）：注册表加 `采样参数?{温度?,top_p?,频率惩罚?,存在惩罚?}`、`最大回复tokens?`、`思维链?{启用?,努力档?}`、`切片预算?{软上限tokens?,硬上限tokens?,截断优先级[]}`；`$模型画像.采样参数` 扩展；`$预算控制台` 加 `采样覆盖层?`、`切片预算覆盖层?`、`渲染模式覆盖?`
- [x]  🔴【C1/C3·6.53】`镜头焦点角色` 重定性为会话本地视角键（席位表退化单元素）+ 双时钟多人语义注释——接口冻结级，写错即推倒重来 ✅ **席位表已落 actor.ts（77cb28c·4.3）**：三枚举控制者·单机退化 `\{本机:\{...\}\}`·迁移映射已更新；C3 双时钟多人注释随 4.2 世界层补
- [x]  🔴【R1/R6·6.63·✅457aab2】P0-1x：CombatResolver 三段签名冻结（init/step/settle + 未实装抛错）+ 签名泛化三处（参与方[]/意图开放串/回合事件[]）——必须在 P0-7 写调用点之前定稿
- [x]  🔴【6.66】P0-1x：离场补结三段签名占位行（初始化/推进/收束）——同上时效 ✅ 457aab2（findRoute/赌局Resolver/提案单 schema 同批落）
- [x]  🟠【N2·6.51】存档头「系统事件镜像」只读白名单 vs 写进 1.7b Non-Goals——**二选一必拍**，卡存档头 schema 定稿（见审计 #5）✅ 已裁=开放窄缝，存档头+系统事件镜像 schema 已落 dollar.ts（002e48d·4.9）
- [x]  🟡【缺口1】死亡事件加可空 `拦截?` 结构（只放配方引用）✅ 已落 actor.ts（457aab2）
- [x]  🟡【缺口3/6.48】org.ts `属性轴?` + 轴条目结构 `{数值, 显示名?, 停用?, 域?, 衰减速率?}`（出厂七轴键名冻结）✅ 已落 org.ts（9367930·4.4）
- [x]  🟡【缺口3补充·✅f0c7288·①②③全确认】信念容器瘦身（删 强制度/异端容忍 收编出厂轴）——①schema 无双轨（org.ts:165-168 仅 {官方体系,思潮派系}）②读取侧 grep 归零（engine 0 命中）③蓝图模块 8/6 已用属性轴路径 · 闸一信念瘦身收口
- [x]  🟡【缺口7】玩法预设 `换角许可?` ✅ 已落 preset.ts（565dbd7·4.10）
- [x]  🟡【缺口8/6.47】`$meta.周目谱系` 节点加 `父快照拍号?` `分支原因?`（单树定稿）✅ 已落 dollar.ts（457aab2）
- [x]  🟡【V1/V2/V4·6.58】指令信封 `txn_id?` + 约定库子类型族字段（子类型?/周期?/终止条件?/失败策略?/挂靠时钟域?/触发条件?/目标失效回退?）+ 条款标的表达式化——⚠ 表达式/谓词文法未冻结，见审计 #9 ⏳ 约定库子类型族(discriminated union)+挂靠时钟域?+目标失效回退? 已落 secret.ts（e44e26a·4.5）；txn_id?(指令信封·模块3)✅31e6659 ＋ 条款标的表达式化✅930ef25（DSL v1.0·标的?=union[string,{v,expr}]·5断言·旧档零迁移）
- [x]  🟡【D1·6.54】资金/负债条目 `域籍?` ✅ 已落 economy.ts（44e941e·4.7·资产开放串类别+负持有透支档一并落地）
- [x]  🟡【6.62】manifest `生效锚点?` + 基底契约 semver 依赖声明 ✅ 已落 memory.ts mod注册表（4.8/457aab2 确认·含内容哈希?）
- [x]  🟢【缺口4】继承包 `世界遗产白名单?[]` ✅ 已落 secret.ts（e44e26a·4.5）
- [x]  🟢【缺口5/6.66】org.ts `离场演化契约?` + `关联声明?[]` ✅ 已落 org.ts（9367930·4.4）
- [x]  🟢【缺口6】播报条目 `遮蔽样式?` ✅ 已落 memory.ts（457aab2·明牌|暗骰|全暗）
- [x]  🟢【缺口8】tick_log `盐值?` ✅ 字段已落（4.1·接线待 P0-9 全局回滚计数器快照）
- [x]  🟢【Q1·6.60】知情名单条目 `来源选择器?`（审计血统）✅ 已落 secret.ts（e44e26a·4.5）
- [x]  🟢【W1/W4/W6·6.65·✅e8cd4e5】schema 注释三件：涟漪稀疏化阈值 / 关系边跌破门槛转 L2 / 环形缓冲 N 住预设
- [x]  🟡【新排查·纠缠闭包弱边阈值默认值·6.66】6.66「弱边按阈值截断」缺出厂默认 → 住预设给保守默认（建议 0.2·累积强度<阈值即截断、界定闭包半径防扩散全图·解法同涟漪稀疏化阈值）·随档快照入判定面指纹·P0-6 前定 ✅ 拍板 0.2（2026-06-14）·✅ CC 落 79bf513（preset `default(0.2)`·入判定面整包 FINGERPRINT_BUNDLE_MEMBERS·hashJudgmentBundle 签名·property「改值→指纹变」·未进排除名单）——指令①逐条全中
- [x]  🟡【三十·卡格式·✅ 77cb28c·4.3】NPC 包/角色 schema 加可空段：`关系声明?[]`（方向敏感槽·复用 Z2 五类方向槽）/`既往记忆种子?[]`/`开场白?[]`（素材包数组·升格自原 `固定开场白?{}`·制式见清单三十之二·**禁双轨**）/占位解析槽（`user`/`char`→实体键）；来源枚举加「导入预设」值（迁移推定同族）——全部可空零迁移
- [x]  🟢 文档：律庚（迁移＝换底片不换照片·全树形状迁移）／律辛（已结算归历史·未结算归未来·结算线非版本线）入修订决议 ＋ docs/spec ✅ 入 docs/spec/[resolution.md](http://resolution.md) 新增第七节·1448绿
- [x]  🟢【AA5·四 已贴入】S3 归一的 Unicode 版本钉死（自带固定实现或显式版本号·H5 同款·= 六禁⑥）✅ NFC 钉版 15.1.0 已落（d486e93·P0-5）
- [x]  🟢【AA10·五·✅e8cd4e5 memory.ts】AA1 调用信封旁注：结算层世代（回滚计数器＋拍锚）与演出层世代（Y4/X5 草稿计数）两层正交、互不回填
- [x]  🟢【AA11·五·✅e8cd4e5 dollar.ts】存档头旁注：N2 系统事件镜像（终身累计·只读）vs G9b 树内换角计数器（每线重计）两量并存非双写
- [x]  🟠【N2 悬案·四 已贴入】存档头「系统事件镜像」只读白名单（全局回滚/周目/换角/裸SL 次数·引擎写内容侧只读·复用 G9b 扩展位）——三悬案已裁 ✅ schema 已落 dollar.ts（002e48d·4.9）
- [x]  🟢【收尾】蓝图↔schema 一致性脚本 diff 归零 ✅ 44 键 diff 归零（0c05631）
- [x]  ✅【已落 memory.ts:181·见 P0-6 B-c】🟠【反向核查·G-4·schema 待誊·未完】4.8 加「触发扫描器状态{观测值表,挂起命中队列}」字段——已改蓝图、待誊 schema
- ✅🟡【反向核查·G-5】4.3 席位表注：老档 镜头焦点角色→席位表 需迁移映射(非纯零迁移)
- ✅🟡【反向核查·S-2】4.9 注：migration_version(单档schema版) vs 迁移戳 分工非双写
- ✅🟡【反向核查·S-3】4.6 赛事实例加 `模板快照?`(脱包兜底·同K4)——已改蓝图
- [x]  🟡【签·6.74·本轮回填】mod 注册表加 `作者公钥?` / `签名?` / `签名算法?` 三字段（防倒卖·可空零迁移·键名冻结·入指纹排除名单·内容哈希复用 B1c）✅ 已落 memory.ts（11fc33a·4.8·验签逻辑待 P0-6 导入闸）
- [x]  🟢【发现D·本轮回填】`$会话状态.本拍重掷序号` → `演出层草稿计数`（本期新增字段·零迁移·演出层永不进盐；盐源 = 存档头全局回滚计数器·P0-9 落地，P0-5 阶段 rngFor 盐由调用方传入、不读尚不存在的存档头）✅ 确认落位 dollar.ts（002e48d·4.9·未双写）
- 📋【发现C / X 批·本轮回填·归位指引】✅ X1–X5/X15 叙事流 schema + 播报条目 tagged union(5渠道) 已落 secret.ts（c689d8d·4.5）；媒介渲染与多源消息流 X1–X15 已终审入蓝图 6.70，全量任务行见 P0-5 现阶段全量任务清单；本页落点散布：X1–X5/X15 → P0-1c schema、X9/X11/X12 → P0-7、X6/X7/X8/X13 → P0-8、X10/X14 → P0-9、前端 → P1（不在此重复展开）
- [x]  🟡【6.75·新功能批·全落 0c05631】⏳ 4.8 部分（注册表+3=叙事质量二审/玩家代理回复/小剧场 + 渲染模式? + actor_source）已落（11fc33a·4.8）；4.9 部分（舞台状态?+舞台可比较属性?）已落（002e48d·4.9）；4.10 部分（二审维度库+小剧场剧本库）已落（565dbd7·4.10）；叙事写法双桶/二审维度库/小剧场/舞台状态/写权观战 的可空·改动 schema 一次钉进（4.1 _叙事设置人称二元组+叙事权限+功能开关表 / 4.8 注册表+3+actor_source / 4.9 舞台状态?+可比较属性·指纹排除 / 4.10 二审维度库[开放·反油腻已并入]+小剧场剧本库 / 小剧场收藏[]隔离）·零迁移·详见蓝图 6.75
- [x]  🟡【6.76·玩家代理/叙事控制/观战·全落 0c05631】⏳ 4.8 部分（actor_source 第四值「NPC自主/系统驱动」）已落（11fc33a·4.8）；B 桶可空/改动 schema：4.1 决策权限三档 + 人称加视角锁定+第二人称 + 功能开关表加观战推进模式 / 4.8 actor_source 加「NPC自主/系统驱动」/ 观战内容入主角认知?=派生·细化 6.75·零迁移·详见蓝图 6.76

### 🎚️🎬🤖 玩家主权 / NSFW 四档 / 破限引擎化 · P0-1 schema 行（✅906e89b→d266305 四处缺口全收口 · CC 修复 2026-06-14 · 1288 tests 绿 · 源块见文末三模块）

<aside>
🔎

**CC 核验裁定（2026-06-14）**：906e89b 实为「字段在位、硬约束未落」，原「6 行全落」改判 ⏳。逐项——疲劳系数 / assistant预填：字段+排除 ✅（疲劳系数键名按全库中文惯例、步长属 UI 层，本项可单独 [x]）；破限引子：字段在但**未入指纹排除名单**（无排除名单条目、无 property test）；内容分级 enum：**值为中文「关/SFW/NSFW/community」、无 default、错放在功能开关表**；字段2「允许玩家覆盖/玩家SystemPrompt覆盖」与字段4「专家模式唯一入口」：schema 层**无约束、仅注释**。**三条拍板修复（落 CC）**：① 破限引子入 `FINGERPRINT_EXCLUDED_FIELDS` + `EXCLUDED_MUTATIONS` + 跑「排除员变→指纹不变」property test；② 字段2/6 scope 改用 stubs.test.ts 编译期断言（证明核心调用条目类型结构上不含 覆盖/预填 字段）、**不拆分离 schema**；③ 内容分级 enum **统一英文 `"off"|"light"|"explicit"|"community"` + 默认 `"off"`**、移回 `$玩家偏好`、补 refine 强制「community 才解锁覆盖」。三项全绿后本节 6 行方可 [x]。

</aside>

- [x]  🟡【🎚️玩家主权】`$玩家偏好.nsfw_fatigue_multiplier?`（默认1.0·0~2.0·步长0.1·入指纹排除·消费点接 P0-7 NSFW饱和度→正剧拉回）
- [x]  🟠【🎚️玩家主权】调用类型注册表「叙事」条目加 `允许玩家覆盖SystemPrompt?`(默认false)＋`玩家SystemPrompt覆盖?`——作用域硬约束=schema白名单(记账/检定/谜底校准/结算条目结构上不含·防覆盖泄进结算)·入指纹排除名单
- [x]  🟡【🎬NSFW四档】`$玩家偏好.内容分级?` 四档枚举 `"off"|"light"|"explicit"|"community"`（默认off·入排除名单·唯一前台内容档开关）
- [x]  🟠【🎬NSFW四档】`内容分级` enum 驱动专家模式门（off/light/explicit→覆盖=false；community→解锁覆盖）·enum 唯一入口·不另设独立专家勾选(防双控件)
- [x]  🟡【🤖破限引擎化】`$模型画像.破限引子?{思维链引子?,注入角色?(system|assistant),预填串?}`（per-provider·入排除名单·玻璃箱可见·零迁移）
- [x]  🟠【🤖破限引擎化】调用类型注册表「叙事」条目加 `assistant预填?`——作用域硬约束=schema白名单(核心调用结构上不含)·入排除名单·零迁移
- [x]  🟡【N-6·多人 per-seat 路由归属·注释先钉·✅注释已钉 prompt/index.ts:75-84·1448绿·实装见P2】多人下模型路由归属：路由=全局成本/key 行为 → 降房间级共识/房主权·非 per-seat 各自切·P0-1 schema 注释先钉（实装 P2·接 C5 per-seat 投影/MP6 swipe 房主权）
- [x]  🟡【N-7·目标模型键单写·✅077f3c7 已单处】`NSFW降级目标模型键` 单一权威=`$预算控制台.NSFW降级目标模型键`（非连接预设 vs $模型画像 双写）·画像侧若需即派生现算——本轮确认单处无双写漂移·防回归 re-grep 归 ⑧（P0-6 前）
- [x]  🟢【N-8·连续失败暂停计数器=演出层·注释·✅注释已钉 schema/dollar.ts·1448绿·行为见P1】关态「连续失败 N 次自动暂停」计数器属演出层（不进盐·不随拍快照·N 住偏好）·与盐源/tick_log 路由快照两分·P0-1 注释钉死

## 🍺 P0-1 · 酒馆功能字段预埋（黄金窗口·按轻重缓急·详见酒馆功能实现清单）

- [x]  🟠【酒馆·采样透传·✅b623196】注册表/模型画像加 `附加采样参数?{}` 自由透传字典（Min P/Top K/Typical P/TFS/DRY/XTC/Mirostat… 不解析原样透传后端·一字段覆盖全部本地采样器）＋ `停止序列?[]`（adapter 传停止串·防越界续写/冒充下一角色）＋ Top K 并入核心采样子集——全部入指纹排除名单·零迁移
- [x]  🟠【酒馆·内容分级·✅b623196 SFW/NSFW+容忍度/硬审查/解禁】功能开关表加「内容分级开关（成人内容开/关·分级）」=B 桶状态（权限路由·确定性非自由文本）＋ 模型画像加 `内容容忍度/硬审查标注`（按容忍度路由·开成人时自动避开硬审查模型）＋ `解禁/风格补正提示词`（per-provider·adapter 打进 PHI 末尾位·引擎控制不交玩家手编）——字段预埋·零迁移
- [x]  🟡【酒馆·情绪键·✅b623196 入播报基础】叙事输出 schema 加 `情绪键/表情键?`（说话人当前情绪·叙事调用一次出·免独立分类模型）——统一驱动源：立绘/BGM/环境音/Live2D·VRM 表情由同一情绪键消费（一次出多处消费）·标签集开放可自定义·零迁移
- [x]  🟡【酒馆·生图字段·✅b623196 生图配置+视觉锚定特征】生图字段预埋：`生成模式?`（角色肖像/自拍/场景/背景/最后消息可视化）＋ 源可配置（本地 ComfyUI·A1111 / 云 DALL-E·NovelAI·复用端点可配置）＋ `人物视觉锚定特征表?`（角色名→固定 SD 标签·GAL 皮肤层）＋ `配图密度/节奏参数?`——实装 P2·触发走函数调用·零迁移
- [x]  🟡【酒馆·语音字段·✅b623196 语音配置+声线】TTS/STT 字段预埋：源可配置（云/本地）＋ `角色专属声线?`（RVC 语音皮肤·情绪键驱动动态声线）＋ STT `消息模式?`（追加/替换/自动发送）＋ `触发词门控?`——实装 P2·零迁移
- [x]  🟡【酒馆·RAG字段·✅b623196 RAG配置·三作用域/embedding/分块/注入】Data Bank/RAG 字段预埋：三作用域知识库（全局/角色/聊天附件 → 世界级/角色级/存档级）＋ `embedding 提供者?`（可配置·中文需换中文 embedding）＋ 分块参数（块大小/重叠/阈值·中文按字符校准）＋ 注入模板/位置——实装 P2·嵌入模型换了必重算向量·零迁移
- [x]  🟡【酒馆·多模态字段·✅已在 11fc33a】`$模型画像.多模态能力?`（已在【调】批）明文绑定「内联媒体输入/图像理解」——能力声明降级链：无 vision 退化为 caption 文本·实装 P2

## 🌍 世界信息接口 · P0-1 lore 知识库字段预埋（已定案·替代酒馆世界书 WI·黄金窗口）

- [x]  🔴【世界信息·lore schema·✅d803ff9 已实装（六件+食物/方言样本+TOOL占位）·✅复核通过：权限层=`_lore知识库` read-only·谓词进指纹注释已标(P0-6接线)】声明式 lore 知识库（世界知识层·只读·模块15·规则包即数据）字段族——**通用知识包 schema（域无关·绝不为服装单独建表）六件**：① `嵌套分类树`（交领>唐/明 · 菜系>地域 · 方言>语音特征）② `别名/同义词表`（形制别名/食材别名/方言词↔通用词·进 S 批归并表）③ `触发谓词`（时代/地域/场景/在场实体/状态命中·非关键词字面·过 DSL 文法）④ `知识载荷`（描述性文本·供叙事读取）⑤ `状态转移逻辑?`（衣物「解扣不脱·解带方脱」/食物「应季时令」/方言「某地默认口音」·可选）⑥ `硬约束/禁令?`（禁清式盘扣/禁反季食材/禁角色串口音）——字段预埋·零迁移·汉服形制表=首个规格样本（schema 须能同形表达食物/风土/方言）

## P0-2 · migration — 状态：已完成 → 随信念瘦身 PR 补一刀

- [ ]  🔴 P0-2：U6 宿主侧外置状态（**死档级·互审Bug2**：MVU 双角色两分——账本／已结算标记属真相层随树迁移不可重建，仅展示镜像重建；双宿主 fixture 加「迁移后镜像重建恒等」断言）📌 **阶段标注（2026-06-18·防交接窗口漏看）：spec@P0-2（迁移面规格此处定稿）｜impl/test@P0-11（恒等断言依赖双宿主 adapter）·非 P0-6 前可完成项·闸一不卡此条** ❓-spec@P0-2·impl/test 依赖P0-11双宿主adapter
- [ ]  🔴 P0-2：U5 冻结载荷族迁移覆盖清单（K7种子载荷／R3战斗注入／Z5失败工单／6.66补结注入／E2知情快照／渲染缓存索引／占位模板快照G-2——映射失败走墓碑·禁静默丢弃）📌 **阶段标注（2026-06-18·防交接窗口漏看）：spec@P0-2（覆盖清单此处 finalize·防静默丢载荷）｜impl 完成依赖载荷族 shape 定稿（Z5/6.66/R3 部分@P0-7）·非 P0-6 前可完成项·闸一不卡此** ❓-spec@P0-2·impl 依赖载荷族shape(部分@P0-7)
- [ ]  🟡 **记忆召回的 recency 走统一衰减累积器**（正篇 recency = 指数衰减，因子 0.995）。治 bug：记忆衰减各处私算 → 与情绪/印象衰减口径不一致（违反你"衰减器三处共用"铁律）。把记忆召回的时间项并入 P0-3 衰减器。 ❓-论文新增·并入P0-3衰减器待实装
- [ ]  🟡【BASELINE·存档 clamp】SFW/light 包加载越界档存档（内容分级=explicit/community）→ migrate 期 clamp 降级（explicit→light·community→off·超出当前 baseline 一律落最高可用档）fail-safe·复用 community-gate 自愈 safeParse（warn+置默认·绝不 throw 防死档）——随 baseline 实切上线 ❓-依赖baseline实切·未上线
- [ ]  🟡U5 冻结载荷族覆盖清单：种子/战斗注入/失败工单/补结/发布快照/渲染缓存索引 枚举 ❓-P0-2 迁移面·枚举未落
- [ ]  🟡MVU 镜像＝重建 vs 账本/已结算标记随树迁移 两分 ❓-P0-2 迁移面·规格未落
- [ ]  🟢 上述新增 facet/角色/δ/枚举字段**全部可空、旧档落默认**（PANGeA+P–R–B）。防 bug：老存档迁移后 NPC 信念字段缺失抛错；按你"无法推定填 schema 默认 + 标迁移推定"处理。 ❓-依赖P0-1新schema字段·迁移默认未落
- [x]  🟡【姓名知识迁移默认·对撞③】老档无姓名知识字段 → 迁移 default=**「已知名」**（backward-compat·旧世界无姓名雾）·默认「不知」会让全员变「那个人」体验崩——同信念瘦身默认值教训 ✅ CC 本轮：build认知档案() 写 姓名知识='已知姓名'（actor.ts）+ 认知档案/记忆条目加 mentioned_known_names/refs·NpcSchema 加 介绍策略/案底（1486绿）
- [x]  🟡【缺口3补充·✅f0c7288 迁移函数+幂等+fixture+6测试】信念容器收编的迁移映射 + 全局 grep 旧路径 + 二次迁移幂等 + fixture 同步
- ✅🟡【第三批落地·3-8B-②·f0c7288】迁移函数：老档 信念.强制度/异端容忍 搬到 属性轴对应轴+幂等（独立 PR·随 P0-1 步①同 PR）
- ✅🟡【反向核查·G-5】迁移映射：老档 `镜头焦点角色:指针` → `席位表{本机:{焦点角色键:旧值}}`(席位表升格非纯零迁移)——**已随 4.3 更新（77cb28c）**
- ⏳🟠【反向核查·G-3】U1 迁移面清单显式补「系统.已结算标记」(律辛已结算/未结算分界依据本身·漏迁=分界错乱)

## P0-3 · 时间核 — 状态：已完成 → **审计新增追溯项**

- [x]  🟠【审计 #4·H5 追溯·✅d486e93】检查衰减累积器与历法/复利换算是否用了平台 `Math.pow/exp/log`——若有，替换为 P0-5 固定实现并重录衰减/历法 fixture（补漏清单未给 P0-3 开此项，本页补上）
- [ ]  🟡 **时代/历法权威边界**给时代错置校验做数据源（PANGeA·Temporal）。防 bug：时代错置校验若自己算"哪年有没有手机/科举"会出现第二处时间换算 → 违反"core 内不存在第二处时间换算"铁律。校验闸必须**查时间核**，不自算。 ❓-论文新增·时代错置数据源未落
- 🟢 衰减累积器：蓝图口径 = 速率 × `_本拍跨度`（线性·无超越函数）→ 安全，但任务规格须明文「衰减禁平台 Math、复用 P0-5 同一份 expFixed」防回归
- 🟢 历法换算 computeTickSpan / getAge：日历边界差值·整数运算·无超越函数 → 安全（顺手 grep `new Date`/`Date.UTC` 确认无 Date 依赖触发禁①）
- ❗ **probOverSpan = 具体追溯命中**：蓝图明文 `1−(1−月几率)^跨度月` 用 `Math.pow`，替换为 H5 稳定式 `-expm1(n·log1p(-p))` 的固定实现，**替换后重录触发概率折算 fixture**；依赖 P0-5 `stableProb` 接口先定义
- 🟢 Math.random() 已受 P0-0 ESLint 自定义规则拦截、与六禁不重叠，确认无遗漏

## P0-4 · 状态机补丁批

- [x]  🔴 **物品/角色状态要建成不可逆状态机 {active / lost / destroyed}，"复活"必须有叙事解释否则回滚**（SCORE 核心：item 在 lost/destroyed 后无解释地 active → 判连续性错误，**保持前一状态不更新**）。治 bug：毁掉的剑下一集又出现、死掉的 NPC 无故复活、用掉的道具还在背包。你的状态机要显式禁止这类非法转移，并在校验闸处回滚而非放行。 ✅**完结 2026-06-19·commit b72b1d1**（数据层实体状态机⊥P0-4 模态机·零改 stateMachine.ts·hasRevivalFlag＝转域续命授权瞬时令牌·生产者＋revival fixture → P0-7·详见 ⊕-L L-15 完结行）
- [x]  🔴P0-4 · 初始状态→开场白模态边界单测(捏人产出 → 开场白模态首拍断言,确定性 fixture) ✅完结（stateMachine.test.ts:108·CHARACTER_CREATE→OPENING 五断言全绿·以 P0-6 主战场 Phase I 为准·CC 侦察 2026-06-19）
- [x]  🔴 **G1 (抢话/视角/观战/二审 元层开关切换走组边界)元层开关切换走组边界** (F1 机制虽在,但 6.76续 这条 G1 没单贴)。 ✅完结 2026-06-19〔CC G1·test-only 零 engine diff〕：tests/stateMachine.test.ts 新增 describe 'P0-4 G1·元层开关走组边界（I-a·专项）' 4 用例（FIFO 累积每步无效果基础态不变／指令组边界整批 flush 顺序恒等队列清空／未 flush 队列阻断读档安全接缝堵 silent fallthrough·flush 后通过／多组循环纯函数恒等第二轮 flush 仅含第二轮条目无跨组混入）·操作标签用本地占位 tag〔G1_ctrl_alpha/beta/gamma〕不命名正式语义串〔拍板：断言机制不命名〕·test 2544·engine 零 diff·指纹/REPLAY/C2 零变动·红线空·[bugs.md:167](http://bugs.md:167) 同步打勾·以 P0-6 主战场 Phase I-a 为准
- [ ]  🟡 **叙事语义校验闸 / 二审 / 自反思重写走指令组边界**（PANGeA #1#3 + WhatELSE #2），不许拍中途生效（G1 同款）。防 bug：校验在一拍中途改状态/改视角 → silent fallthrough、模态栈错乱、六不变量被破。 ❓-论文新增·校验组边界未实装
- [ ]  🟡 **【G篇】** 任务=显式状态机（START→SEARCHING→RETURNING→COMPLETE）+ 可达性检查（无死端、无不可达终局）。 ❓-论文新增·任务状态机未实装
- [ ]  🟡 **【L篇】** 效果只分 4 类（移动/设属性/建对象/删对象），且房间不可挂属性以防被永久堵死。→ 预设"哪些实体可被哪些效果修改"的白名单，防 LLM 把世界改到不可逆。 ❓-论文新增·效果白名单未实装
- [ ]  🟡 **【One Life篇】** "律只在相关时才激活"形成每次转移的专属计算图。→ 治动作判定时全量规则空跑、性能与可解释性差。 ❓-论文新增·律激活图未实装
- [ ]  🟢 **校验失败的纠偏重写**作为模态内步骤、不新增模态栈深度。防 bug：每次纠偏 push 一层模态 → 栈深超 4 被拒、卡死。 ❓-论文新增·纠偏步未实装
- [x]  🔴【G1·6.49·✅ee5cedb】INHERIT_DECISION 空候选兜底边（死档级）+ 全覆盖单测
- [x]  🟠【F2·6.56·✅ee5cedb】tick 单飞锁 + 拍生命周期显式状态机 + CI 连点轰炸（重入真洞）
- [x]  🟠【F1·6.56·✅ee5cedb】元层写 FIFO 排队至**指令组边界**（M5 收紧版）+ 停机类例外——⚠ 蓝图多处仍写「拍边界」，见审计 #6
- [x]  🟠【F3·6.56·✅ee5cedb】看门狗强制 pop = 显式超时边 + 统一清理序 + 进非法迁移表
- [x]  🟡【缺口1·✅70387de】`死亡 → 拦截检查 → (转域续命|谢幕)` 显式边
- [x]  🟡【G5·6.49·✅70387de】读档/立碑时机门规进非法迁移表
- [x]  🟡【C4·6.53·✅70387de 接口骨架·实装P2】席位掉线 × 看门狗 AI 托管降级（接口规格）
- [x]  🟡【D2·6.54·✅70387de 接口骨架·实装P2】拦截穿越比率链式复合 + 死亡时刻双登记（全局权威+域钟展示）
- [x]  🟡【F5/F6/F7·6.56·✅70387de】栈满待弹队列硬边 / 同类型模态禁重入 / 内存态三档盘点表（第三档必须为空）
- [x]  🟡【缺口7·✅70387de】INHERIT_DECISION「自愿换角」进入边（不触发拦截扫描）
- [x]  🟢【R2/R4·6.63·✅70387de 注释】COMBAT=RP_FOCUS 离散孪生钟口径注释 + settle 任意回合可调 + 模态回合循环接线——⚠ 多人口径见审计 #10

## P0-5 · RNG 与检定（当前位置 · 手上直接做）

- 📐【贯穿全程·架构纪律/开发规范·NSFW 隔离设计·2026-06-15 拍板】NSFW 相关提示词/破限语句/解禁词/assistant 预填/敏感字段引用一律集中到独立配置模块（`nsfw_content_full.ts`/`nsfw_prompts.ts`）·代码只经 import 使用·**禁散落硬编码**——P0 只做此隔离（不动确定性引擎/指纹/结算冻结进度）；条件编译（Webpack/Vite 插件·三套入口·`BUILD_VARIANT` 切导入路径·Tree Shaking 剔除）+ CI 反向断言（sfw 包零 NSFW 串）推迟 P1/P2。隔离即埋好剔除接缝·改造成本可控·不留技术债（详见文末 🌍 NSFW_BASELINE 节）
- ✅【截至 commit d486e93 · 已完成】六禁 CI + 反向断言 / 固定数学库 fixed.ts（fixedLn·fixedExpm1·fixedPow·fixedSqrt·stableProb）/ probOverSpan→stableProb 追溯 / canonicalize（禁⑤）/ NFC 钉版 15.1.0（禁⑥）/ H2 assertFinite / AA6 fingerprintManifest + 指纹双向 property + 双跑逐位恒等 / 指纹两分（发现A）+ 暴击映射进指纹 / 盐源对齐（发现D）/ 配方表拓扑+宿主类型（缺口2/3）/ 一致性脚本 blueprintConsistency —— 709 tests 全绿（R5 战斗相关部分待 P0-7 确认）
- [ ]  🔴 **U3 指纹版本分段**（U 批最深）：引擎版本＋schema 版本进指纹取材集（P0-3 泛化车第七现）；历史拍指纹 = 观测史只搬运永不重算；跨版本段降级完整性校验（哈希链＋迁移戳血统）＋分享档显式警示；M6 难度归档裁决与本条共用同一台分段机器 ❓-P0-5 指纹·U3版本分段未实装
- [ ]  🔴 **信念派生取材集进指纹枚举表**（P–R–B #1#2 + AA6 先例）。防 bug：派生结果影响 NPC 行为却不进指纹 → 确定性重放（REPLAY-01）分叉、golden vector 漂移。配"取材变→指纹变"断言。 ❓-依赖P–R–B派生·指纹枚举未落
- [ ]  🔴 **LLM 打的"重要度分"必须创建即冻结、进指纹、永不重算**（正篇 importance 由 LLM 在创建时打 1–10）。治 bug：重放/swipe 时重新打分 → 分数漂移 → 召回集变 → 确定性分叉（同 Z5 失败工单冻结、历史拍指纹只搬运的纪律）。 ❓-论文新增·重要度冻结未实装
- [ ]  🔴 **【F篇】** 坐标哈希 → seed → 固定 LLM 采样：重访同一地点产出**完全相同内容、且无需持久化存储**。→ 这就是"能派生的不存储"的落地。 ❓-论文新增·坐标哈希派生未实装
- [ ]  🔴 **【F篇】** 模型升级会打碎确定性哈希：同 seed 在新模型下产出不同内容 → 存档/重访崩。→ 锁定 LLM 版本，或对已生成内容做持久化快照（写进 P0-9 约束）。 ❓-论文新增·约束待写P0-9
- [ ]  🟠 P0-5 · G6 小剧场盐隔离 ❓-P0-5 RNG·盐隔离未实装
- [ ]  🟡 **【One Life篇】** 用"概率律混合"**确定性地建模随机结果**（如丧尸下一步的概率分布）。→ 给你检定/随机性的结构化表达范式。 ❓-论文新增·概率律范式未实装
- [ ]  🟡 **校验闸/因果校验器若调 LLM，独立盐隔离**（PANGeA + WhatELSE），不进主回滚计数器/tick_log（6.66 第三种盐纪律 / G6 小剧场盐先例）。防 bug：校验调用混进主盐 → swipe 重 roll 时校验消耗扰动主命运链。 ❓-论文新增·校验盐隔离未实装
- [ ]  🟡 出厂离场契约进指纹取材＋补结 RNG 第三种取材纪律（锚离场区间起止拍号、不混回滚计数器盐）（6.66/W 批细节） ❓-P0-5 RNG·离场取材未实装
- [ ]  🟡 **向量/嵌入相关性召回的非确定性要隔离在叙事层**（正篇 relevance = embedding 余弦；PANGeA 用 ChromaDB）。治 bug：嵌入召回结果若影响状态又不进指纹取材集 → replay 不可复现。召回"取了哪几条"要么冻结进指纹，要么严格只在 LLM 侧不回写真相层。 ❓-论文新增·召回隔离未实装
- [ ]  🟢 **【J篇 + One Life篇】** 约束/律越多、序列越长，校验越贵。→ 按重要性/违反概率排序，提前剪枝
- [x]  🟠【M6·6.50＋AA8 修正 ✅·d0d62c1 裁定+注释落地·分段机器待U3】难度归档裁决——取「**组边界可切**＋指纹分段」（AA8：废原「拍边界可切」，难度切换 = 指令组边界生效、分段点 = 组锚点、与裁决六组边界口径统一、不下沉内部拍边界），与 U3 指纹版本分段共用同一台分段机器（难度段 = 版本段同款处理，助手档免标记口径保留）；随旋钮可变性表落地实装
- [x]  🟠【H2·6.57·✅d486e93】`assertFinite` 双卡口 + NaN fail-fast + 源头盘点（本批最毒）
- [x]  🟠【H1·6.57·✅f95312a ledger.ts】账面量上下界 clamp + 越软顶=clamp+⚠播报
- [x]  🟠【H5·6.57·✅d486e93】确定性三禁第三条：自带 pow/expm1 固定实现 + CI 静态检查——⚠ 建议扩为五禁，见审计 #3
- [x]  🔴【五禁④·✅d486e93】CI 规则 `no-locale-compare`：全仓禁 localeCompare/Intl.Collator（字典序=Unicode 码点序）——挂点 = 第②闸归一化函数＋所有展开/排序调用点；当前仅审计文字拍板、无独立排程行，**P0-5 必上线**
- [x]  🔴【五禁⑤·✅d486e93】CI 规则 `canonical-serialize-only`：指纹取材计算函数体禁裸 JSON.stringify、强制 canonicalize()（键排序）——覆盖 U3a 迁移映射哈希/迁移戳/跨版本哈希链；不上线 = 补丁集/归并表/谓词哈希全假指纹（同表不同序=假分叉），**P0-5 必上线**
- [x]  🟠【AA5 升格·✅d486e93】确定性五禁扩为**六禁**，⑥ = NFC Unicode 版本钉死：归一化走自带固定 Unicode 数据库或显式钉版本号；CI 规则 `no-platform-normalize`（grep `.normalize(` 确认调用点全走固定实现）——S3 串治理全押 NFC、归一不可复现=归并表假分叉；蓝图「确定性三禁」字样同步更名「六禁」（4.11 H5 段 + docs/spec）
- [x]  🟡【P0-3→P0-5 接口依赖·✅d486e93】P0-5 先定义 `stableProb(p,n)`/`fixedExp`/`fixedPow`/`expFixed` 固定实现接口，P0-3 概率折算与衰减追溯替换调用之——防 P0-3 自写「差不多的」实现引入新双轨
- [x]  🟢【反向验证·✅d486e93】pnpm test 含「故意写 Math.pow/localeCompare/裸 JSON.stringify 确认 CI 报红」反向断言；pnpm lint 全过纳入 P0-5 收尾验收
- [x]  🟡【断言①·✅d486e93】同档同 seed 双跑逐位恒等——**六禁全绿后**可小时级跑通（P0-5 gate）；比对 harness 必须走 canonicalize()（禁⑤第一个消费者·否则键序不同的语义等价态假报失败）；依赖六禁 CI 全绿在先
- [x]  🟠【缺口1/8·✅b5160a0】天命通道命名规范（生死/周目级概率强制天命）+ 盐源升级为存档头全局回滚计数器
- [x]  🟡【R5·6.63·✅b5160a0·a/b落·c stub+defer P0-7】确定性三件：意图/注入序落账、rngFor 加回合序号、快照锚 pin 整场
- [x]  🟡【缺口2/3·6.48·✅d486e93】配方表 `拓扑`（即掷|骰池占位）+ `宿主类型` + 出厂派生配方 + 停用轴中性缺省分支
- [x]  🟡【指纹族打包·┅50aab6f+c73ac1b】K5 补丁集 / Q5 谓词定义 / J5 级联深度 N + 轮号入种子 / S4b 归并表 / 拦截概率参数住配方表——一次接线全收 ⏳ K5 补丁集哈希已接（c73ac1b）；Q5/J5/S4b/DSL 文法版 留 TODO（家在 P0-6/P0-7）；K5 作弊标记跟随生效源 + 发现B 派生量配方 ✅已落 f95312a
- [x]  🟡【指纹补丁·┅50aab6f】表达式/谓词 DSL 文法版本进指纹取材集与 U3 版本分段（审计 #9 升级：Z5 失败工单按「归一后逐字节同串」比对表达式槽后，DSL 文法已是判定输入——文法冻结＋版本号须在 P0-6/P0-7 写求值器之前定稿）
- [x]  🟠【新排查·DSL 求值器函数库版本进指纹·接 §十A 分层方案】文法版本之外、**求值器实现/数学函数库版本**也须进指纹取材集并随 U3 版本分段：pow/sqrt 核心固定实现逐位恒等可免版本；exp/ln/sin/cos 等向后兼容增列档=优化即 bump 版本、旧档锁旧版本语义重放——否则优化求值器后重放旧档假分叉；补进 AA6 全名单＋M·2 枚举表＋§九第2类 ✅ CC 落 79bf513（preset `default(1)` 占位·property「改版本→指纹变」）；✅ 两点已确认（CC C组）：①C1=求值器函数库版本与 DSL 文法版本同在 FINGERPRINT_PRESET_FIELDS（hashPresetFingerprint 签名·并列口径正确·指令写 BUNDLE_MEMBERS 系笔误·DSL 文法版本本就在 PRESET_FIELDS）；②C2=代码侧 fingerprintManifest.ts/rng.ts/preset.ts/[fingerprint.property](http://fingerprint.property).test.ts 已闭合·AA6 全名单与 M·2 枚举表归文档批单独补录求值器函数库版本条目（剩 doc 登记·§九第2类本页已含）
- [x]  🟡【K5·6.52·✅f95312a】作弊标记跟随最终生效源（是否作弊字段·多补丁叠覆取最终生效者语义接线 P0-6）
- [x]  🟡【B1 族·✅c73ac1b 全封口（a/b/c/d/e 均落·三处纠正落实）】指纹面封口五项：①AA6 三员（判定骰型/钳制表/数值面域）②B1b 戆击映射入册【戆→暑】（随判定骰型·显骰入排除）③🟠B1c 生效中内容包集哈希（K5 泛化为全部已启用包内容哈希·事件包/战术包档外内容=判定输入·热加载点同步更新·本轮最大真洞）④B1d 分区制（判定面整包 canonical 哈希单一成员·叙事面显式排除·P0-3⑦边界画完）⑤B1e 排除名单整册＋「排除员变→指纹不变」断言——随指纹族打包一次接线（蓝图 B1b/B1c 本轮已回写 6.57骰面铁律①/6.52 K5）
- [x]  🟡【H3/H4·6.57·✅f95312a fx.ts/prob.ts】汇率单锚两跳取整恒不利 + 概率域夹逼 + `-expm1(n·log1p(-p))` 稳定式
- [x]  🟢【6.45/6.63·✅c73ac1b】指纹断言测试 +「字段变→指纹变」断言 + 排除名单（gate A/B 共 18+ 断言）
- [x]  🟢【6.62·┅50aab6f 留位／H6·✅1795c06】纪元包集版本指纹留位（一行注释）；【H6】除零中性缺省进配方规范
- [x]  🟡【AA6·四·✅c73ac1b】「影响指纹的取材集成员全名单」显式枚举表（约十项＋B1 补判定骰型/钳制表/数值面域三员·照受治理键空间清单先例·新增须入册＋「成员变→指纹变」断言）——与指纹族打包一次接线
- [x]  🟠【AA8·五·✅d0d62c1】难度切换生效边界统一为**组边界**（组锚点生效·分段点=组锚点·与 U3 共用分段机器不下沉内部拍边界）——已就地修正 M6 行
- ✅🟡【第三批落地·2-2】6.50 M6 难度＝组边界（AA8）已写入蓝图
- ✅🟡【第三批落地·3-16】模块16 重 roll 段补 AA8 难度边界句 已写入蓝图
- ✅🟡【第三批落地·3-3】DSL v1 文法版本号进指纹取材集、随 U3 版本分段 已写入蓝图
- [x]  🔴【发现A·指纹粒度裁定·✅c73ac1b】预设判定面整包 = 仅含从不快照的预设字段；难度系数组/判定骰型/暴击映射/钳制表为「开局锁定·随档快照」、从档内快照单独取哈希、不入预设整包——两边互斥不重叠（防中途预设漂移致双重计入或留缺口）；同步回写蓝图 + 冻结清单 M·2/M·4
- [x]  🟡【发现B·派生量配方归位·✅f95312a】独立派生量配方Schema 已进 FINGERPRINT_BUNDLE_MEMBERS；确认个人派生量配方（HP=f(体质) / 精力恢复=f(体质,心理)）归检定配方表派生子表（随整包入指纹）；若为独立容器 → M·4 判定面补一行 + 蓝图回写
- [x]  🟠【6.76 续·硬化批·本轮加入·✅P3 1795c06·P5/P6 e4b6499】P3 叙事控制簇整体进指纹排除（叙事层⊥判定层·B1d 同族·＋「排除员变→指纹不变」断言·玩家拨动只落偏好/演出层永不进盐·G6 推广全簇）；P5 二审重写不改 actor_source·不单独落账（⊄单写者链）；P6 二审 retry 预算绑世代（swipe 给新预算·同世代自动退回≤1）；P1 自动代写不可逆决策安全地板在 P0-5 备指纹/通道侧配合（主体落 P0-7）——详见蓝图 6.76 续
- [x]  🟢【slice 实证·叙事温度进指纹排除·2026-06-16】slice M5 Web UI 确认：叙事调用温度=0.9（`DEEPSEEK_TEMPERATURE` 可覆盖）只为同动作出不同文字、**不进盐·入指纹排除名单**——重放短路读 tick_log 不碰 LLM，温度漂移不破事实/指纹恒等（= 6.68 逐字不可复现但语义保真·P3 叙事层⊥判定层同族）；根因＝温度未显式设置沿用 provider 默认致重复动作逐字复现，显式设高即解

## 🌍 世界信息接口 · P0-5 概率原语（已定案）

- [x]  🟡【世界信息·概率触发·✅e4b6499】概率触发事件（WI 触发概率%·如 1% 唤醒上古神）→ 走引擎判定/事件系统**概率原语**（rngFor·复用触发契约四类的概率类·锚拍号混盐），非 prompt 注入；快进下走 probOverSpan 折算（1-(1-p)^跨度·H4 稳定式）防每拍裸判失真——与 6.45 拦截概率参数同口径（住配方表/进指纹）——⚠ 对撞报告 R8

### **🔧 P0-6 派生器 dry-run 审计回填 · 引擎内部键加 _ 前缀（df0674e Gate① · 黄金窗口·建议直接补）**

- [x]  🔴【dry-run·分层歧义·✅d624fb3 已完成】状态机/系统/存档头/席位表 四个引擎内部顶层键**现无前缀 → 按前缀规则会落 writable 层**，现靠手工 ENGINE_INTERNAL_KEYS 集合豁免。**裁定建议：趁黄金窗口加 _ 前缀（_状态机/_系统/_存档头/_席位表）+ 一次 migration，删手工豁免集**——手工集 = 第二真相源（违「白名单自动派生」自身精神·新增引擎键必漏豁免漂移）；_ 层语义 = AI 只读可见·引擎可写，四键皆非秘密（$谜底另走 $）故 _ 正确非 $
- [x]  🔴【dry-run·深挖·四键之外·✅d624fb3 已完成】嵌套于 writable 父域的引擎内部键同病：全局.覆写日志 / 全局.编年史 / 全局.作弊标记（append-only 引擎日志·AI 必不可写）——现仅靠「无动词命中」单层保护（dry-run c），兜底动词高频晋升可破防；建议同加 _ 前缀（_ 排除 + 无动词命中 = 双层防御），与四顶层键同 PR；编年史 改 *编年史 不影响玩家可见（*=AI 只读但可见）
- [x]  🟢【dry-run·扛住·登记】(a) 前缀分层 / (b) 开放串 vs 枚举（Zod _def.typeName·ZodString→开放串 / ZodEnum→枚举）/ (c) 动词目标路径覆盖 三项 dry-run 通过（df0674e）→ P0-6 核心假设（白名单自动从 schema 派生）成立；$谜底 嵌于 writable 父域正确归 invisible

## 🚦 P0-6 准入闸（玩家拍板 · 2026-06-14 · 两步都过才解锁 P0-6）

<aside>
🚦

**铁律：当前阶段不得进入 P0-6。** 必须依次过两道闸——① P0-1~P0-5 全部剩余 bug 改完跑绿；② 再过「脚本检测 + 可校验手段」**至少 6 轮**把引擎雏形固定住。两步都过才解锁 P0-6；在此之前任何「可关·进 P0-6」结论一律不成立，红着不许往下走。

</aside>

### 闸一 · P0-1~P0-5 全清（前置）

- [x]  P0-1~P0-5 所有剩余 bug 跑绿（含信念瘦身独立 PR 三项确认全绿 · 1235 tests 绿）：①schema 真删强制度/异端容忍字段 ✅（org.ts:165-168 仅 {官方体系,思潮派系}，防回归断言绿）· ②读取侧派生公式 grep 归零 ✅（engine 0 命中；migrate.ts 读旧写新为唯一合法处；狂热度/激进度等派生 P0-7 才实装、当前无旧路径）· ③蓝图模块 8/6 旧路径回写 ✅（模块 6 范例=属性轴.民心、模块 8 五大子系统/范例=属性轴；6.48 出厂七轴键名为有意保留）→ 闸一全绿

### 闸二 · ≥6 轮固定引擎雏形（闸一全绿后才开 · 逐轮留证/commit · 任一轮红则回退重改）

> 🚦闸一已全绿（2026-06-14）→ 闸二开启。轮次现状：1-3 的实现已在 P0-5/派生器预演落地（六禁 d486e93 / 指纹双向 c73ac1b / dry-run df0674e·d624fb3），逐轮跑成 gate 留证即可打勾；4 签名+抛错已落（457aab2/77cb28c）·断言 stub 待补；5 重放器依赖 AA-合并会先立项；6 C2 混沌猴需先搭 harness；7 双写 re-grep + AA3 半天会为真新活。
> 
- [x]  1 · 六禁 CI ④⑤⑥ + 反向断言（✅c780c63·pnpm lint exit 0·lint-rules 19/19·禁④localeCompare/禁⑤JSON.stringify/禁⑥.normalize 单元+集成双层各自报红）
- [x]  2 · 指纹双向 property（✅c780c63·[fingerprint.property](http://fingerprint.property) 74/74·Gate A-bundle16/A-snap5/A-preset3/B 全排除名单/C/D 全绿）
- [x]  3 · schema 派生白名单 dry-run（✅c780c63·whitelistDryRun 39/39·Check A 前缀分层/B 开放串vs枚举/C 全动词路径 全绿）
- [x]  4 · 6 个冻结接口断言 stub 三件套（✅c780c63·stubs 40/40+12 expectTypeOf 编译期口径锁·findRoute/CombatResolver/赌局Resolver/离场补结/提案单/席位表 返回类型全冻结·签名+抛错在 457aab2/77cb28c·总 1247/1247 绿）——⏸ 停在第 4 轮，第 5 轮重放器等八场景清单定稿
- [x]  5 · DSL fuzzer 跨机确定性 + 重放器最小实现（输入集十类·盐值读 tick_log 记录值·跨机逐位恒等）——**5(a) DSL fuzzer ✅a167d1a**（fuzzer.ts xorshift128+ seeded 无 Math.random·eval.ts fixedPow/fixedSqrt 无平台 Math·13 测试同 seed 逐位恒等+叶节点回归·1260 绿·lint clean；✅ 5(a) 收货四项全补（CC 7098595·dslFuzzer 20→34 tests·四项逐一对应 B1-B4·1313 绿）：🟠跨机 golden hex 向量常量化入库+二机 diff（或显式归 P0-10 H5 双机用例）＋确认 xorshift128+ 走 BigInt/受控32位、不借 Number 53 位尾数；🔴`/` 取整方向「向不利于发起者」确定性回归（div-0 已测、方向未测·审计#9 直改账面）；🔴防双轨：eval.ts fixedPow/fixedSqrt 须 import P0-5 fixed.ts(d486e93) 同一份·禁第二实现；🟡fuzzer 覆盖 v1 全集 {min,max,clamp,pow,sqrt}·补 min/max 明证）；**5(b) 重放器 REPLAY-01** 八场景+十类输入已 2026-06-15 排程负责人确认冻结（见上 AA-合并会）·**5(b) REPLAY-01 ✅本轮 1415 绿（+59 tests）**：replay/（types.ts 十类输入集 ExternalInjection/FailureTicket/AccountingRecord/ReplayTickInput·engine.ts replayTick 纯函数·AA1 partitionTickets 弃过期世代工单·N-2 replayRoute 读冻结路由·逐字节路由一致断言）·replay.test.ts 10 tests 覆盖前三场景（悔棋+旧任务迟到＝断言③·live 偏好改动不动路由+过期世代丢弃／fork AA1 弃3留1／快进乱序纯函数 C→A→B≡A→B→C）——5(a)+5(b) 两半全绿·第 5 轮打勾；剩余五场景（关账掉落／重试双落账／版本×难度 AA8／双层世代 AA10／迁移×在途）归 AA-合并会续轮——✅ **本轮全交付（1486绿/26 files）**：REPLAY-01 场景四–八 + 5 AA 对撞场景（replay.test.ts）·chaos/scheduler 扩 10 事件类·golden hex 5c1d0233/63b3e729/db10d5c7 → AA-合并会八场景清单全绿
- [x]  6 · C2 混沌猴随机交错万拍（swipe 风暴×元层写排队×失败注入×fork×掉线托管随机穿插·六不变量恒真+存档可加载+关账态一致）✅本轮 1432 绿（+17 tests）：chaos/scheduler.ts 纯 scheduleEvents() 走 prand.xorshift128plus·5 事件类权重[30,25,20,10,15]·同 seed 跨机同序；invariants.ts I1 单写者/I2 账本守恒/I3 路由冻结=N-1/I4 世代单调=AA1/I5 已结算单调/I6 指纹纯函数；harness.test.ts 17 tests 万拍每拍六检查+检查点 0→5000 重载校验和恒等+双跑逐字节+golden hex seeds1/2/3=0952b06c/92e812cf/e757e2c3+事件分布±5%——闸二只剩第 7 轮（双写 re-grep + AA3 半天会）
- [x]  7 · 新字段双写 re-grep（含 🎚️🎬🤖 6 字段·语义版「能派生的不存储」）✅**⑧ re-grep 本轮已跑（1448 绿·26 files·2026-06-15）：30+ 字段全部单源或明确定格快照·无双写项**（内容分级是唯一有旧→新位置迁移者·写新即 delete 旧·无双轨残留）+ AA3 DoD 基准面豁免清单半天会 ✅ **AA3 玩家 2026-06-16 正式签字放行**（char 保护＝社区第一重视·§5.2 群聊卡 就按源码定·样本留 P1 补）·实质判定七节全锁定·剩 CC admin 收尾（汇总豁免清单→回写 docs/spec→勾完成）

> 七项（≥6 轮）全绿 ＋ 闸一全清 = 才解锁 P0-6。
> 

<aside>
📝

**闸二第 7 轮收尾 · AA3 → `docs/spec/dod-baseline.md` 回写设计（2026-06-17 拍板锁定）**

**5 拍板**：① 落点＝独立新文件 `docs/spec/dod-baseline.md`；② 粒度＝压缩三桶规范表＋附录（详稿留 AA3 页）；③ commit＝独立 docs commit·PR 暂缓（攒批手动建）；④ 同步 修订决议（notion-23）一并回写口径；⑤ 外链按字段类型分桶＝精炼版。

**三桶规范表（按 §1–§7 归类·每条一行）**：✅ 保留 spec-in（导入闸必须支持）⊥ 🟡 豁免 out-of-scope（优雅降级不报错）⊥ ⚰️ 墓碑 reject（拒收＋落墓碑·禁静默丢弃）⊥ ⚖️ 中立不识别（内容自带使用限制声明）。

**附录**：① 指纹排除名单 B1d（baseURL/endpoint/apiKeyRef＋反代 6 字段＋外观主题字段＋渲染缓存）；② 透明切模型硬约束（禁黑箱·切换落 _tick_log.路由快照可见）；③ Defer 清单（反代自定义端点档→P0-7 / 群聊卡样本→P1 / RAG·记忆图旁路→P0-8·P2）。

**🆕 外链三态（精炼版·收紧 §6.2/§7）**：图片/字体/媒体/被动文本资产字段 → ✅ 保留·**导入期本地化快照＋content-hash 去重**·实时热链机制仍 ⚰️·本地化失败→🟡 占位降级；`endpoint`/`baseURL` 字段 → 🟡 豁免（提示·不自动套用·机密区主动配）；`script`/`code`/明文 `key` 字段 → ⚰️ 墓碑（红线）。理由＝被动≠安全（远程图＝追踪像素/IP 泄露）＋易腐破确定性＋离线。实现＝导入期本地化快照器·归 P0-6 导入闸。

**执行序**：Step 0 CC 读-only 侦察 docs/spec 现状（回报不猜）→ Step 1 CC 著 `dod-baseline.md`＋独立 commit → Step 2 我回写 notion-23 口径＋AA3 页底三勾＋闸二 row7 勾完成 → **闸二全清·解锁 P0-6**。红线：不碰指纹取材计算/结算/RING_K/定点数学。

**✅ 落地（2026-06-17·commit c185dd6）**：[dod-baseline.md](http://dod-baseline.md) 已 commit·AA3 页底三勾＋闸二七轮全勾·[「AI 文游人生模拟器」V4.1 修订决议](https://app.notion.com/p/AI-V4-1-a9d51518f9f747a29d9880bcf1d902df?pvs=21) §6.41 已回写口径 → 闸二全清·P0-6 解锁。

</aside>

### 🆕 排活 · AA3 DoD 基准面豁免清单（P0-10 动工前 · 半天会）

- [x]  逐条过旧卡行为清单，每条打标签 **保留** ／ **豁免（写理由·标第几轮改的）**
- [x]  豁免清单存 docs/spec；P0-10 分两组判：保留组全对／豁免组按新规则重录标准答案

### 🆕 排活 · AA-合并会（提前到 P0-6 开工前 · 产出三件）

- [x]  重放器**立项派 owner ＋ 编号**（owner = 蔡怡家佳·单人 vibe coding 自任 ✅ · 编号 REPLAY-01）
- [x]  重放器**输入集十类·排程负责人确认冻结**（2026-06-15·= §九全枚举：初始快照／指纹取材集〔含 DSL 文法版＋求值器函数库版〕／意图序列／盐值序列〔读 tick_log 记录值〕／失败工单／调用世代号／外部注入序／选择器展开落账／观测值表＋挂起队列／域累计活跃区间表；重放中 LLM 调用全短路读账）——REPLAY-01 解除阻塞、可进 5(b)
- [x]  **八场景测试清单·排程负责人确认冻结**（2026-06-15）：悔棋后旧任务迟到／fork 瞬间在途／快进多任务乱序／关账边界掉落／重试与原调用同时返回双落账／跨版本段重放×难度段切换(AA8)／宿主世代×引擎世代双层乱序(AA10)／迁移边界×在途调用

## 🌍 世界信息接口 · P0-6 lore 导入闸 + 受控接口能力集（已定案）

- [ ]  🔴【世界信息·lore 谓词冻结·已定案】lore 条目触发谓词过 DSL 文法冻结（增审4 同一台·路径只读真账+谓词限深1+注入安检）+ 导入闸校验（谓词可解析+键过 S 批归一+硬约束合法）+ 受治理键空间归一（分类树键/别名/标签） ❓-P0-6 lore导入闸·未实装
- [ ]  🔴【世界信息·受控接口能力集·已定案】mod 作者声明式受控接口能力集须对标 narrative-agent `[TOOL]`：`code`（确定性计算·**走 DSL 或签名 Resolver 插件·非 mod 内嵌任意 JS**·R6-a）/ `llm`（受控子调用·产出过五道闸+预算/世代号纳管·R6-b）/ 内置 `roll_dice`（DC+优势劣势+爆炸骰·复用统一检定·rngFor 变长消耗确定性·R6-c）/ JSON schema 参数 / `trigger`（planning=结算前·post_pipeline=结算后·改状态走级联轮 J1·R6-d）/ `output_tag`（跨轮回看·自定义变量存储层+前缀权限·R10）+ 状态面板渲染 + 判定——能力须覆盖社区高端玩法多数，否则 mod 作者退回想要任意 JS（JS-Slash-Runner 老路）；安全换表达力代价必须够小 （🟢 R6 json_schema 双轨对齐 ✅完结 3040f53·ToolCapabilityType 5→6·executor 仍 stub）❓-P0-6·受控接口能力集未实装
- [ ]  🔴【世界信息·设定/状态分界·已定案·同 Y13】lore 知识库**只装「世界恒真知识」（服装形制/食物/风土/方言）·实体现状与秘密禁入**（否则 = Y13 世界书设定/状态病的 lore 版·第二真相源旁路知情过滤）·导入闸对含实体名+状态断言的 lore 条目作者警示（并入警示族）——⚠ R7-a ❓-P0-6 lore导入闸·作者警示未实装

## P0-6 · 五道闸 + 导入闸（开工前最后窗口 · 主战场）

> **🟠 2026-06-18 拍板：卡格式核验全族（卡-A／卡-B／卡-C／卡-D／组-F）→ 改判 P1，不在 P0-6 预埋纯函数 stub。** 理由三条：①无 P0-6 载体（读卡 JSON／跑 regex／character_book 拆解／merge 写档全依赖 hosts/tavern·P1 空桩·碰即触红线）②纯函数 stub 非 freeze-sensitive·任何时候加都零成本·不占 schema 黄金窗口·无「P0-6 后补＝迁移」压力③premature-stub 与 P1 真导入器 shape 易错配致双重返工（动词表批同款明文教训）。卡-D 同 →P1（lore＝纯 config·非存档/迁移/replay·三件套指纹归属待 P0-6「lore 谓词冻结」+ fingerprintManifest lore.触发谓词入 BUNDLE_MEMBERS 落地才能定 AA6 位）。**Phase C 真主体 = C-b**（键名冻结 enforcement / S5 / V3 动词目标槽展开 / G7 死亡拦截硬顶）。下方各卡格式项随 P1 全导入器（6.42 封闭测卡 Beta）一并做·详见 P0-6 主战场 Phase C-a。
> 
- [ ]  Z·导入闸：缺数值/方向槽→作者警示（警示族打包行未含 Z） ❓-P0-6 导入闸·警示族未含Z
- [ ]  🔴【S6 + 受治理键空间清单明文·与 S1/S1b 同窗】① 六键空间枚举清单（anchor 进 S1 注册表·新增键空间须显式入册）⚠ **reconcile 待清·populate 路径 II 前·不卡路径 I**：权威基线 = 代码 12 项命名空间枚举（币种/单位/稀有度/地点类别/标的类型/特质子类/状态子类/sideEffect句柄/cascade句柄/母题/纪元/mod包·governedKeySpace.ts）；文档 §十三 旧 6 项（关系类型/母题/技能类别/部队姿态/缔约形式/印象·意象标签）= 陈旧子集·仅「母题」重合·4 缺项（关系类型/技能类别/部队姿态/印象·意象标签）待 CC 核 = 已换名覆盖／有意 defer／真漏需加 enum·详见冻结接口清单 I 区注 ✅ CC 核完拍板 2026-06-19：4 缺项全有意 defer·无真漏·无需新增 enum·13 项枚举完整·G-b populate／S2 仲裁·母题写入口 defer 待 mod 生态；② S6 实装铁律「未注册串 = 注册非拒收 + 自然降级（无特权·不进选择器命中/涟漪走缺省/母题并入单一保守桶）」——禁把注册闸写成白名单；法律地位一句话另抄进决议/docs ❓-P0-6·与S1/S1b同窗·reconcile待清
- [ ]  🔴【S-1·三十六·随 DSL 文法冻结一并拍】存量「未结算」表达式/谓词跨 DSL 版本：v2 文法须向后兼容 v1（只增不改不删语义·新读旧）或大版本迁移时重写——二选一必须和 DSL 文法冻结同刻拍定；配 fixture gate「v1 表达式当前文法 parse 过且求值逐位一致」（不定 = 升版后老遗嘱/老租约求值崩）。落点 = P0-6/P0-7 写求值器前 ❓-P0-6·随DSL文法冻结一并拍
- [ ]  🔴 **认知档案/记忆写入也必须走闸 + 血统**（正篇未来工作明示 *memory hacking：精心设计的对话能让 agent 相信一个从未发生的过去事件*）。治 bug：玩家用对话给 NPC 注入假记忆，被当真值落档 → 绕过单写者/血统。记忆写入要标来源（一手观测/二手转述/玩家陈述），不可无血统升格为"事实"。 🟢 来源类型 enum schema ✅3294a23（印象条目.来源类型?·以 P0-6 主战场 L-22 为准）｜记忆写入走闸活线 ❓-（defer 提案闸/P0-8）
- [ ]  🔴 **两种模式要给每个子系统明确指派，别混用**：**模式1 Graph-First（"deny all"）**：确定性引擎是唯一决策者，LLM 只做 NLU/表达；只回答图谱能安全生成的，杜绝幻觉但受图谱覆盖面限制 → 对应你**五道闸/结算核心**。**模式2 Post-Generation Validation**：LLM 先草拟，确定性引擎按规则**校验→改正/拦截/升级人工**再交付 → 对应你**叙事校验闸/二审**。（治 bug：同一决策点让 LLM 既当作者又当裁判。关键真相/数值走模式1，叙事文走模式2。」 ❓-论文新增·模式分派未实装
- [ ]  🔴 **Prompt injection（"忘记之前的规则"）是 LLM 固有缺陷、训练无法根治，只能靠确定性层隔离**。治 bug：玩家在对话里注入"忽略设定/你现在是管理员"越权改写规则或真相。你的 NPC prompt 组装 + 知情切片必须做到：**玩家输入只能进"被观测内容"，永不进"规则/真相/指令"层**。（与 A 篇 memory hacking、PANGeA NPC Hacking 同源，三篇共同指向同一道闸。） 🟢 架构隔离复核 ✅3294a23（不加 schema·hosts/ 红线零变更·以 P0-6 主战场 L-23 为准）｜hosts/ 运行时输入路由 ❓-（defer P0-11 双宿主）
- [ ]  🔴 **归一化层（normalization）是不可省的治理层，不是工程便利。** 消融实验铁证：去掉归一化 → 冒烟测试 100% 过，但校验通过率**直接归 0**。结论：**光靠 prompt + schema 约束保证不了引擎可执行的语义一致**。你的五道闸里必须有一道"确定性自动修复 + 准入"环节（补缺字段、非法 prefab 引用、越界坐标、对不上的分支 ID 全部确定性修掉或丢弃），不能只做格式校验。 ❓-论文新增·归一化准入未实装
- [ ]  🔴 **【G篇】** 确定性归一化层不可省：消融实验里去掉它 → 冒烟 100% 过、但**校验通过率归 0**。→ 光靠 prompt+schema 保证不了引擎可执行的语义一致，闸必须含"确定性自动修复 + 准入"。 ❓-论文新增·确定性修复闸未实装
- [ ]  🔴 **【F篇】** "not hallucinated（无幻觉）"是把结构有效偷换成事实正确。→ 别宣称 schema 闸"防幻觉"，它只防畸形输出；要真防需跨会话一致性度量 + grounding。 ❓-论文新增·grounding度量未实装
- [ ]  🔴 **【H篇】** 校验失败的处理闭环：二分搜索定位**最小矛盾子集** → LLM 翻成自然语言反馈 → agent 重写 → **最多重试 3 次**（有界）。→ 治闸"被拦后怎么给反馈、怎么有界重试"。 ❓-论文新增·有界重试闭环未实装
- [ ]  🔴 **【L篇】** 动态生成的新动作**不保证可通关性**（玩家可能把自己卡死）。→ 开放动词必须加一道"可达性/可完成性"回归校验。 ❓-论文新增·可达性回归未实装
- [ ]  🔴 **【J篇】** 铁证级取舍：纯 prompt 约束满足率仅 ~38%、给不了保证；硬约束解码满足率 100% 但延迟 1.8s→22–48s（15–25×）且质量塌；只有 prompt+decoder 端到端才两全。→ "保证 100% 合规"必然在 decoder 层强制，而强制必付延迟代价 → 放非阻塞路径。 ❓-论文新增·decoder强制未实装
- [ ]  🟠【effect 包进指纹·对撞④】声明式 effect 包内容哈希 **进 B1c 生效中包集哈希**（effect 包改判定 flag = 判定输入）·热加载点同步更新——不进则不同 effect 包同指纹=假恒等·重放假分叉（⏳ 焊死前批③ 深度 A 已落聚合函数签名 stub〔body 抛·零接线·绝不进 rng.ts〕＋编译期断言＋注释对齐〔rng.ts:197/:215＋fingerprintManifest.ts:78→[freeze.md:252](http://freeze.md:252)〕；**本 P0-6 行残留**＝写聚合函数 body＋接 hashPresetFingerprint＋effect 挂载 RootSchema＋content_hash 自动填充＋热加载点·并进 AA6 全名单＋「成员变→指纹变」断言 fire）✅**【拍板 2026-06-18·Wave 4 走薄壳：先只进 AA6 成员名单登记·聚合函数 body／接 hashPresetFingerprint／挂载／热加载 等活线后补·避开指纹取材计算红线】** ❓-P0-6·薄壳已登记·聚合函数body未写
- [ ]  🟡 **【J篇】** 当前约束方法只能处理**词法约束**，语义约束仍是开放难题。→ 与 F 篇同源，语义层得靠 H 篇符号求解。 ❓-论文新增·语义约束待H篇
- [ ]  🟡 **【L篇】** 前置条件可直接翻译成代码检查（位置/库存/属性三类）。→ 闸的代码化模板。 ❓-论文新增·闸代码模板未实装
- [ ]  🟡 **越界"Cheating"中可结构化的部分并入钳制闸/动词白名单**（PANGeA·物理违规/越权技能/NPC 黑入）。防 bug：物理违规和越权技能里"能落成写入"的那部分（瞬移、凭空获得资产、改他人状态）本就该被五道闸钳制/白名单拦——这部分**不该丢给语义校验**，否则确定性防线漏给了 LLM。明确分工：**结构可判的→P06 闸；只能语义判的→P08 校验闸。** ❓-论文新增(PANGeA)·结构化部分待入铳制闸
- [ ]  🟡【DSL 求值轨迹 trace 钩子】6.58 求值器冻结前穿 trace context 过每个 eval 节点（事后 on-demand 重放派生 AST 级轨迹·不存储）——作者侧试算沙盒 ＋ 玩家侧事后解释；静默回退/钳制必须高亮；耦合 §二拍前快照 ring buffer（争议 patch pin 快照）。机制后做、钩子现在留 ❓-P0-6·钩子未穿
- [ ]  🟡【effect 包过闸+受补丁约束·对撞④】effect 包 deltas 过五道闸铳制（单次Δ上限）+ 受规则补丁 clamp/lock 约束（**补丁取严先于内容**：money_delta 不得穿透账面下界锁）——与 K1/K2 导入期仲裁同口径 ❓-P0-6·过闸铳制未实装
- [ ]  🟡【保真度档落血统·对撞⑦】导入保真度档（strict/plus/native）若影响落库字段集（native 丢字段/重排）→ 档决策落实例血统元数据 + 落库即脱包·档本身不再影响运行（同卡同档同结果） ❓-P0-6·档落血统未实装
- [ ]  🟡【S1/S1b/S2·6.59】受治理键空间注册+归并表 + 跨包映射冲突导入期仲裁 + 母题写入口注册闸 ⏳**S1/S1b schema ✅B5 Step2（commit 6d71770·挂 RootSchema 零迁移）·S1/S1b 写卡口 fire ✅路径 I（commit 2872c24·regression lock·registry 留空 fail-open）·S2 仲裁+母题 fire B6（defer 路径 II 后）**——⚠ 建议把 `地点.类别`（区域级）入册，见审计 #13 ❓-S2仲裁/母题fire待路径II
- [ ]  🟡【V3·6.58】动词目标槽联合类型（单实体|选择器串，展开按真键字典序） ❓-P0-6/P0-7·展开器未实装
- [ ]  🟡【G7·6.49】死亡拦截器引擎级硬顶 ❓-死亡拦截硬顶·routed P0-7
- [ ]  🟡【S5·6.59】规则引用完整性扫描扩维（被规则引用即冻结） ❓-P0-6·引用完整性扫描扩维未实装
- [x]  🔴 **【F篇】** 跨字段逻辑依赖校验（schema 闸≠语义/真相闸·"冰原生态+极端高温"照样过 schema）✅完结 ab3bb14（发育阶段/相邻条目 superRefine·仿 intervention_pack_v1Schema·以 P0-6 主战场 L-25 为准）
- [x]  🟠【提案闸批·五道闸 proposal pipeline·2026-06-18 正式纳 P0-6·CC Step0+审计】**导入闸侧活线已尽（E-a）·提案闸=五道闸本名·M2/C6/K5/E-e/effect deltas 钳制 的 fire 点**·五闸纯函数齐全·⊕-1 computeDelta 路径写入 ✅**完结（1502a22）**·⊕-2 runProposalGate 装配器 ✅**完结（171b9b9·test 2455·clamp 折叠 clampLedger〔hardHi=ceiling·±Infinity 证 SAFE〕·structuredClone 快照原子回滚·覆写日志 setAtPath 直写绕 Gate③·五验收门全绿·红线 diff 空）**·⊕-3 下游 fire 挂入 ✅**完结（0df7c7a·test 2473〔+18〕·M2 Gate③ pre-check reject-only + M3 set 值比较 + C6 Rule②③④ + K5 多包·E-e 按拍板②剔出归导入闸 Phase E-e·墓碑写路径=拍板①候选B reject-only）**·⊕-4 端到端验收 ✅**完结（24339b5·test 2479〔+6 e2e-1..6〕·tsc28/lint220/指纹84/黄金向量逐位恒等/REPLAY22/C2 17/红线 diff 空·✅ e2e-4 follow-up 已闭（2026-06-19·commit a0d0389·方案 B+：断言收紧 .toBe('②-whitelist') ＋ forward-only∩whitelist===∅ 防回归守卫 ＋ dead-defense 文档化·test 2480·黄金向量逐位恒等·红线空）**·**🎉 提案闸 Phase 收官（⊕-1~⊕-4 全完结）**·orchestrator 本体 P0-6-standalone（无 runTick/LLM）。**scope 修正**：守恒断言闸排除→P0-7（需 getNetAsset 接线·守恒=结算拍级非单笔级）／输入=K5DeltaEntry[]（提案单→动词→delta + V3 展开=单独前端层·V3 verb schema 已 done）／C6=第②闸／单信封原子=P0-6·txn组级·到达序·拍首快照预求值(V1/V6/Z5)+覆写日志 Z3 血统=P0-7／applyDelta 必过 AA4 保留键黑名单+白名单先于写(防原型污染)+定点-safe。详见 [P0-6 五道闸主战场 + 导入闸 — 批次切分与排程](https://app.notion.com/p/P0-6-daf72af9f4934e6fa5aaad650a9474fe?pvs=21) 提案闸 Phase。 ✅提案闸 Phase 收官（⊕-1~⊕-4·24339b5）·✅ e2e-4 follow-up 已闭（a0d0389·方案 B+）
- [x]  🟠【K2/K3/K4 键名冻结 enforcement·6.52·从 P0-1 移入·2026-06-18】「任何被存档引用的 mod 键」键名冻结的**闸 enforcement**（导入闸校验·冻结键改名即拒收/墓碑）——schema 半已在 P0-1 结清（占位形态/模板快照/血统只读化）·此为闸逻辑半·随五道闸键校验一并 fire。✅**C-b Step 0 侦察（2026-06-18）·B6 活线=0**：schema 半已完结·enforcement 归 **Phase E**（随五道闸 fire·墓碑原因 enum 扩「冻结键改名」复用 memory.ts:354–368）。同 C-b 四子项：S5→Phase G（待 registry populate）·V3 展开器→P0-6/P0-7·G7 硬顶→P0-7（冷卼0 警示→P0-8）。⚠ [CLAUDE.md](http://CLAUDE.md) 旧列本项为 B6 活线·已废。 ✅**完结 2026-06-19·commit a67dee3**（Phase E 导入闸侧 fire·memory.ts mod墓碑原因枚举 +「冻结键改名」＋migrate.ts E-e block〔frozenKeys 扫归并条目别名指向冻结规范键→写 _mod墓碑库·复用 writeM2Tombstone〕·test 2532〔+4〕·指纹84/红线空·以 P0-6 主战场 E-e 为准）
- [x]  🟠【守恒断言 core·选项A·✅完结 2026-06-18·commit 00bb3ed】`assertConservation(accounts, expectedNetAsset, getNetAsset)` **纯函数（工具）归 P0-6·本轮提前落地**（落 `packages/core/engine/conservation.ts`+`ConservationError`·economy.ts 补 export 账户Schema/账户Type·tests/conservation.test.ts 5 单测·码点序·throw + 实体级 diff·getNetAsset 注入·与债务模型解耦·禁第二实现·单 commit 3 文件 +124/−1·tsc 零新增·test 2304·lint 220·红线零 diff·指纹/向量基线不回归采信）。⚠ **接进 `runTick` 的接线代码 = 拆 P0-7**（见 P0-7 段「守恒断言接线」行·蓝图落地映射②）。
- [x]  🟠【K1·6.52】两段式加载：SCC 缩点 → 拓扑序 → 全集合并后才派生白名单（死锁级）✅**B1 完结**（commit 5ecc309/1e05ce0/66d9fb4·活线消费接线 fire B6）✅**E-a 活线 fire 完结 2026-06-18·commit 4d55b5f**（migrate.ts K6 块后 deriveModAwareWhitelist+runDryRun·fail-closed throw 拒载·含 K6 轨道分流）
- [x]  🟠【K4·6.52】禁用闸 + 墓碑化存根（跨包悬空引用真洞 · 高危优先）✅**B2 完结**（commit d17fd9a/d7c7518/ef9e570·墓碑可审计·活线 fire B6）
- [x]  🟠【K6·6.52】单一权威 pack_id（废三级 fallback）+ 命名空间正则白名单 + 自环拒收（安全级）✅**B2 完结**（+ K6⑤ key===pack_id 收紧 commit 92dbb92·命名空间唯一·别名可达）
- [x]  🟠【K6 扩·QoL 轨道·6.78·2026-06-17】✅**B2 schema 完结**（轨道 enum gameplay|cosmetic|view|macro 落·轻轨带可写键拒收·闸分流活线 fire B6·✅E-a 已 fire 4d55b5f）mod 命名空间化加「能力种类／轨道」维度（`gameplay|cosmetic|view|macro`）：能力声明驱动闸分流——重轨（碰可写键/effect）过五道闸；轻轨（零可写键+零effect+零判定输入引用）走声明式表现+只读 selector+意图宏。与 S5 可写键贡献字段同位落 schema·**CC 指令同步加此维度**（详见蓝图 6.78 双轨制）
- [x]  🟠【K2/K5·6.52】semver 区间求交（交空显式拒收）+ 补丁叠加约束取严/内容后载覆盖 ✅**B3 semver（acd5f07）+ B5 Step1 K5 取严 merge（4a33e03）完结**（intersect 消费/导入闸 fire B6）
- [x]  🟠【M2/M3·6.50】覆写授权源认证（mod 自封天命降级打标记）+ 规则补丁负面清单（结构不变量硬排除、forward-only）✅**B5 Step3 完结**（commit 03d502c·M2 谓词+墓碑'覆写授权越权'·M3 forward-only/硬排除）·**活线 fire ✅⊕-3 提案闸（0df7c7a·M2 Gate③ pre-check reject-only + M3 set 值比较）**
- [x]  🟠【S3·6.59】码位规范化进双闸（NFC+去零宽+全半角折叠+trim；繁简只警示）✅**B5 Step4 读卡口完结**（commit 5caaac9·migrate.ts 规范化·写卡口纯断言已交·繁简警示+写卡口活线 fire B6）
- [x]  🟡【N4·6.51 已拍 ✅ = 预设可开＋钳制】默认禁跨枝·预设可开口子（只许知识/称号/血脉·钱/装备默认排除·过第④闸钳制）——否决绝对引擎律全禁备选；P0-6 导入闸按此实装
- [x]  🟠【C6·6.53】第②闸席位作用域（per-seat 提案资格）✅**B5 Step4 谓词完结**（commit 5caaac9·单机≤1席位=全权限）·**gate② fire ✅⊕-2/⊕-3 提案闸（0df7c7a）**——⚠ 清单原文误标「P0-3 五道闸」，正确归位在此，见审计 #2

### 🔩 焊死前批次执行序（2026-06-16 拍板·1–6 ＋ 植入 meta）

> 收尾原则：能可空预埋的现在一次焊（零迁移）；要动闸逻辑／迁移／存储形状的留焊死时刻。各批共用红线：不碰确定性引擎／指纹取材计算／结算管线／canonicalize／RING_K／定点数学库。
> 
- [x]  **① 动词表批（✅ 完成·2026-06-16·commit f1ed596）** ✅决策A(18:47 修订) 目标槽选择器＝单态 `z.string()`·**不复用 DSL v1**(数值文法·无实体语义)·展开按真键字典序留运行时契约(P0-6/P0-7)·设计稿见 [动词表 V批设计 · V3 目标槽 × V5 对称性 × 越界 × 植入 meta](https://app.notion.com/p/V-V3-V5-meta-732c2315e4834e02bccb1cca237be022?pvs=21)（8 根／10 标识·转移/缔结⊕解除/赋予⊕剥夺/调整/披露/移动/施加/植入）；V3 目标槽（单实体｜选择器串·单态）＋ V5 对称性分桶（对称可逆 ⊥ 单向不可逆 flag）＋ 越界动词族（施加）＋ 🆕植入 meta 动词。全 .optional()／未挂 RootSchema／零迁移；纯声明式·禁内嵌 JS（R6-a）；闸逻辑（第④闸/N4/安全地板）留 P0-6/P0-7
    - **进度(2026-06-16 19:39)**：新文件 `packages/core/schema/verb.ts`(独立语义域·index.ts barrel·复用 memory.ts `副作用级别枚举Schema`)。✅Step1 `verbId` 枚举(10)＋各 option(.optional)·✅Step2 目标槽单态串·✅Step3-B 动词 option 改用共享 `动词Option基础Schema`(.strict)＋`side_effects?`(handlerRef 暂 string＋TODO 6.59)·✅Step3-A `cascade_on_change?` 挂 `属性轴表Schema`(轴级声明·preset.ts)＋`组织属性轴条目Schema`(实例级覆盖·org.ts)·均无 default·NPC 扁平数值属性跳过·✅Step4 verb.ts 新增 `重掷策略枚举Schema`(禁用|警示)＋`不可逆Schema`(.strict·重掷策略默认禁用)·挂 `特质条目Schema`＋`状态标签条目Schema`·体征/死亡/种族跳过·无环已验(verb.ts 仅 import zod·actor.ts 单向 import 不可逆Schema·grep 确认无反向)。全程零迁移(未挂 RootSchema/BLUEPRINT_KEYS/migrate·1772 绿不掉·tsc 旧债 17 行不变·未触碰 fingerprintManifest/B1a·指纹零变)。✅Step5 通道 A 五类形(标的类型/子类键 z.string().optional()＋TODO 6.59·side_effect 选键/选择器/option 三类确认到位未重做)＋通道 B 占位(新建 interfaces/verbSignedCodePlugin.ts·仿 irreversibleGuard/toolCapability stub 三件套·启用默认要求显式 true＋executeVerbSignedPlugin 抛未实装·grep 证零 import 零接线·verb.ts 仍只 import zod·actor.ts→verb.ts 仍单向)＋键空间踢出(verb.ts 文件头注释·币种/单位/纪元/地图节点类型/稀有度归 6.59)。全程零迁移(1772 绿不掉·tsc 17 行不变·RootSchema/BLUEPRINT_KEYS/migrate/fingerprintManifest/B1a 零改动·指纹零变)。Step5 已核放行·Step6/6.5 编译期断言（stubs.test.ts「Task 4E guard」＋verbScope.test.ts slice 活路径）＋Step7 运行时解析（schema.test.ts 末尾 4.8.x verb 表驱动）全绿。✅ **批①收尾（2026-06-16 21:14）**：1791→1840（+49）·pnpm lint 净·tsc 旧债 core17/slice29 不变·闸二行为面零漂移（REPLAY-01 八场景 22/22＋C2 混沌猴 17/17＋指纹 84/84·黄金向量逐位恒等）·commit f1ed596（schema 预埋 10 files +568/−1·全零迁移）＋b5d7e14（[CLAUDE.md](http://CLAUDE.md) docs）·工作树干净。
    - **⚠ 两处缺口→踢迁移批(零迁移挡掉·非本批)**：① **NPC 扁平数值属性 entry 化**(体质/智慧/感知/魅力/心理·HP/精力/颜值·性格五轴·actor.ts 扁平 number)——挂轴级 `cascade_on_change` 需改 `{值,…}` ＝破坏性形状变更；② **死亡(存活状态/死亡时间/死因)＋种族(顶层 string) 不可逆 flag 化**——非子类 entry·挂 flag 须对象化＝迁移(死亡不可逆性本由死亡管线＋P0-7 安全地板结构性保证·不靠 flag)。二者归**属性 entry 化迁移批**(可并 K6 序⑤ 或单列)·焊死前不做。
- [x]  **② 受治理键空间 6.59 批（✅ 完成·2026-06-17·commits fcd8658→8110da7→39c1fb4→cebc024）** step 拆解＝设计页 §十八（序②执行排程·Step0–8 全 ✅）；注册表＋归并表＋母题注册 schema 位；地点.类别（区域级）入册（审计#13）；码位规范化纯函数（NFC 钉版15.1.0＋去零宽＋全半角 NFKC＋trim·繁简降级留 P0-6）＋JS 保留键黑名单（AA4）＋deltas.path/handlerRef 收紧 string→受治理路径/句柄Schema（add-constraint 零迁移·fail-open）＋Step8 编译期断言（registry 键空间⊥记账/检定/结算·受治理路径/句柄 infer===string 零迁移类型证）；全程零迁移·1840→1913（+73）·三组回归逐位恒等·零指纹变·fire/wiring 留 P0-6（见上「焊死后 P0-6 接线清单」9 条）
- [x]  **③ effect 包 content_hash 进 B1c 指纹（✅ 完成·2026-06-17·commit 1ac4adb·深度 A 薄壳·1913→1920·三组逐位恒等）** Step0 勘察（R1–R5）改判：member 早已 registered（`生效中内容包集哈希` 在 BUNDLE_MEMBERS／PRESET_FIELDS／M·2·c73ac1b 封口）·真残留＝聚合函数（代码零存在）＋挂载＋content_hash 填充＋热加载＝**全属 P0-6 红线**（写聚合函数／接 hashPresetFingerprint＝触碰指纹取材计算）→焊死前只做薄壳。深度 A＝① 聚合函数签名 stub（body 抛·绝不 import·绝不进 rng.ts）② 编译期断言 test-only（content_hash z.infer===string ＋ 聚合输出===string ＋ ⊥ 记账／检定／结算）③ 注释对齐 comment/doc-only（rng.ts:197／:215＋fingerprintManifest.ts:34〔实测·非:78〕＋[freeze.md:252](http://freeze.md:252)·6 项口径补 effect包）。R5 拍板＝空集 fail-open·黄金向量 5c1d0233／63b3e729／db10d5c7 不重生成。step 拆解＝设计页 [动词表 V批设计 · V3 目标槽 × V5 对称性 × 越界 × 植入 meta](https://app.notion.com/p/V-V3-V5-meta-732c2315e4834e02bccb1cca237be022?pvs=21) §十九。**defer P0-6**：聚合函数 body＋接 hashPresetFingerprint／effect 挂载 RootSchema（挂载批·破坏性迁移）／content_hash 自动填充／热加载点＋同步重算／AA6 全名单＋「成员变→指纹变」断言 fire
- [x]  **④ 安全边界钉死（✅ 完成·2026-06-17·commit 88d2b65·方案 C 混合·4 子域一批 5 步·step 拆解见设计页 §二十·三组 123/123 逐位恒等·零指纹变·1920→1937·真闸逻辑 defer P0-6/P0-7·⚠ PR 待手动创建 gh 未安装）** 反代 baseURL 导出剥离 ／ IM4 第三方 JS 同源隔离（沙箱 iframe）／ 玩家主权安全地板（重大不可逆强制需确认·凌驾抢话档）【🎮玩家拍板 2026-06-16】范围＝死亡／婚姻／血脉绑定／永久失核心资产 ＋ 关键NPC离场／绑架（⚠离场/绑架 MVP OUT·现为叙事谎称型 case②；此为离场演化 P2 落地后的前置安全契约·焊死前先钉明文契约+编译期断言、闸逻辑随 P0-7）；交互强度＝抢话档下自动暂停等确认（非裸覆盖·复用 N-8 连续失败/久旱快进自动暂停 演出层同款）／ effect 包过五道闸钳制（单次Δ上限·补丁取严先于内容·money_delta 不穿透账面下界锁）。焊死前能做＝schema 位＋编译期断言＋明文契约·闸逻辑随 P0-6/P0-7
- [x]  **⑤ K6 pack_id 收紧批（✅ 完成·2026-06-17·commit 92dbb92·唯一非零迁移·独立 PR〔待手动创建·gh 未安装〕·1937→1944·三组 123/123 逐位恒等·零指纹变·step 拆解见设计页 §二十一 Step 0–4 全 ✅）** 单一权威 pack_id（废三级 fallback）＋命名空间正则白名单＋自环拒收＋含 mod 注册表既有字段＝要迁移＋幂等＋fixture＋双机迁移恒等；effect 包 pack_id 跟随 K6 收紧·绝不预收·暂同 mod 现状 z.string().default('')
- [x]  **⑥ stub＋编译期断言** ✅本轮已落（interfaces/irreversibleGuard.ts·interfaces/toolCapability.ts·engine/deterministicGuard.ts＋4A–4D 断言·1486 绿）；剩＝每批落地重跑闸二（REPLAY-01 八场景＋C2）留证 ＋ **AA3 半天会 ✅玩家 2026-06-16 已签字放行**（剩 CC 收尾回写 docs/spec→勾完成）
- ⑦ 贯穿：docs/spec 同步（每条拍板即时同步）

> **AA3 半天会素材**：DoD 基准面三选一判定的外部参照已就绪——[官方源码核验结果](https://app.notion.com/p/380b7725263c80799b4edb78f063613e?pvs=21)（SillyTavern 卡格式／正则·宏／组装器／分词器／机密区／多用户目录）＋ [**📖 社区教程参考与同类项目**](https://app.notion.com/p/380b7725263c8033b412ecddbef43f15?pvs=21)（TauriTavern 5 批／Mimir／covel 源码核验·导演模式认知隔离·记忆植入治理灰区）。AA3 实现功能清单＝其简化版，按参考使用。
> 

**⊕-L 依赖外移登记（L-12..L-30 中 P0-6 做不完整项 · 2026-06-19 · 见 P0-6 主战场 Phase L 三桶 triage）**

> P0-6 本批已焊项见主战场 Phase L「① 本批可做」（L-22/L-23/L-24命名/L-25/L-28结构/L-30 hook）；下列为有依赖/归他阶段、P0-6 阶段无法做完整者，句尾 ❓ 标依赖。
> 
- [x]  L-15 · 物品/角色三态状态机 {active/lost/destroyed} + 非法转移回滚（不可逆 flag 部分🔀 B5）✅**完结 2026-06-19·commit b72b1d1·test 2544→2565（+21）**：A 类改判成立（数据层实体状态机⊥模态机·零改 stateMachine.ts）；三件全落＝schema enum＋纯谓词 itemCharStateInvariant.ts＋Gate③-L15 挂点 runProposalGate.ts。**hasRevivalFlag 拍板**＝指令信封瞬时令牌 转域续命授权（proposal.ts·不落 RootSchema/指纹/盐）·否决复活点＞0；生产者＋revival REPLAY fixture → P0-7。⚠ 字段名码点已核全中文形（CC grep b72b1d1·零日文变体）（态/续/权·勿日文 態/続/権）。详见 P0-6 主战场 Phase L L-15 完结行。
- [x]  L-12 · 横切 migrate backfill（随各 Phase-L schema 字段落地追加·不独立实装）✅完结 3294a23（Step-1 backfillPhaseL1b＋Step-2 字段全 optional/.default·旧档自动落默认·以 P0-6 主战场 L-12 为准）
- [ ]  L-14 · 历法权威表 + 时代错置校验源 ❓-（依赖 P0-3 时间核·历法权威表落点拍板）
- [ ]  L-13 · 记忆 recency 并入统一衰减累积器（0.995 指数因子·禁第二实现）❓-（依赖 L-1/L-6 facet 字段 + P0-3 衰减引擎接入点）
- [ ]  L-16 · 叙事校验闸/二审/自反思走指令组边界接线（边界机制🔀 P0-4）❓-（依赖 P0-8 叙事校验闸实装）
- [ ]  L-17 · effect 4 类声明式枚举 + 实体×效果白名单 ❓-（依赖 L-9/L-11 命名拍板·P0-4）
- [ ]  L-18 · 纠偏重写=模态内步骤不增栈深 ❓-（依赖 P0-8 叙事校验闸·触发方拍板）
- [ ]  L-19 · 任务显式状态机 + 可达性检查 ❓-（依赖 L-9 STRIPS precond·P0-4）
- [ ]  L-20 · 校验闸/因果校验器 LLM 独立盐隔离 ❓-（依赖 P0-5 RNG·盐隔离工程含义拍板）
- [ ]  L-21 · 重要度分创建即冻结+进指纹+永不重算 ❓-（依赖 L-4 重要度数值化拍板·阻塞序 L-4→L-21）
- [ ]  L-24 · 结构性确定性自动修复（命名范围本批 P0-6 做·结构性外移）🟢 命名修复 ✅3294a23（复用 normalizeRegistryKeyNames）｜结构性修复 ❓-（依赖 P0-8·自动修复范围拍板）
- [ ]  L-26 · 二分搜索最小矛盾子集→反馈→有界重试≤3 闭环（P0-6 仅可选预埋重试计数器 schema）❓-（依赖 P0-8 叙事校验闸）
- [ ]  L-27 · 前置条件代码化模板（位置/库存/属性）❓-（依赖 L-9 precond schema·阻塞序 L-9→L-27）
- [ ]  L-28 · Cheating 越界分类枚举（结构拦截🔀⊕批·枚举待补）🟢 枚举 ✅3294a23（随 L-8 越界类型 enum 同批落）｜语义判部分 ❓-（外移 P0-8 校验闸）
- [ ]  L-29 · 两模式按子系统指派注释边界（P0-6 仅注释·主体 P0-8）❓-（依赖 P0-8 叙事校验闸/二审实装）
- [ ]  L-30 · 可完成性检查算法（新动词 REPLAY fixture hook 本批 P0-6 做·算法外移）🟢 REPLAY hook ✅3294a23（verbIntentFixture＋2 测试用例）｜可完成性算法 ❓-（依赖 P0-10 回归体系）

### 🗂️ 受治理键空间 6.59 批 ② · 焊死后 P0-6 接线清单（schema 位已埋·待 fire/wiring·避免遗漏 · 2026-06-16）

> 批 ②（设计页 [动词表 V批设计 · V3 目标槽 × V5 对称性 × 越界 × 植入 meta](https://app.notion.com/p/V-V3-V5-meta-732c2315e4834e02bccb1cca237be022?pvs=21) §十八）本轮只埋 schema 位＋纯函数（add-constraint·零迁移·1883 绿）；以下 fire/wiring 一律留 P0-6，逐条勾销避免遗漏。
> 
- [ ]  🟠【S1/S1b·6.59】受治理键空间写入口注册闸 fire（注册表/归并表 schema 已埋 `governedKeySpace.ts`·待接导入期登记键＋校验 against registry） ✅**【Step1 完结·路径 I 写卡口 regression lock·commit 2872c24·+3 tests·tsc29 零新增·test2338·指纹84/黄金向量逐位恒等·红线 diff 空·2026-06-18】**（本行保留 [ ] 因 broader：路径 II populate defer 待 mod 生态）**【Step0 侦察拍板·2026-06-18·走路径 I】**：写卡口 `assertGovernedKeysNormalized` 接 migrate.ts K6①（computeLoadOrder/tombstone）后·post-读卡口-normalize·**throw fail-closed**（幂等不误拒老档）·+CI test 喂脏 registry 证 catch·零迁移。⚠ **关键发现**：`mod条目.可写键`=string[]（无命名空间）·键条目Schema 命名空间必填→**无法从可写键 populate**。路径 II（mod 条目加带命名空间的键声明字段 + 导入期 populate）=**留玩家拍 mod 契约**·MVP 无 mod 声明键·registry 空·不本步；路径 III（成员级 superRefine）defer P0-6；S2 仲裁/母题（依赖 registry 有数据）defer。本步实质=卡口就位+regression lock·真 governance 待 mod 生态。
- [ ]  🟠【S2·6.59】跨包映射冲突导入期仲裁 fire（仲裁策略枚举 4 项〔先到先得/来源包优先/显式优先级/报错待人工〕已埋·待接 fire） ❓-P0-6 跨包仲裁fire待路径II
- [ ]  🟠【AA4·6.59】JS 保留键黑名单 record 解析层接入（黑名单常量 `__proto__`/`constructor`/`prototype` ＋ `是JS保留键` helper 已交·待接 record 解析层拒收/加前缀·存储层 null-proto 或 Map·并入 S3 双卡口·🆕**账本 per-entity 化后 `货币系统.账户.{实体键}` 为新增 record 解析面**·实体键须一并纳入此黑名单防护〔2026-06-17 侦察确认本 shape 批未加·随本步 B6-Step8 fire〕；🆕 **B5.6(c9e53d5) 后 record 解析面 = `货币系统.账户.{id}._应收`/`._负债`(已重命名)**·AA4 覆盖随 S3 写卡口同批 fire(交接页 pre-weld 4 确认)·并 SINK_ENTITY_KEY('**sink**') 列保留键禁 mod 占用为账户实体键）。🆕 **账户面 ✅完结 commit 3bfa009（2026-06-18）**：账户 8 record key + entity key 上 账户键Schema（是JS保留键 superRefine·throw fail-closed）·test 2335·指纹 84/红线 diff 空/老档零回退·SINK 不误拒。其余 record 解析面（deltas.path/handlerRef registry 收紧·null-proto 存储层·币种/行业 key）仍 B6·故 [ ] 不勾满（诚实：本行 broader·仅账户面 done）✅**【Step0 侦察完结·2026-06-18·走路径 I 一步收全】**：存储层全为普通对象（无 null-proto·路径 II 范围大 defer）·黑名单⊥registry（独立可分开做）·红线仅 governedKeySpace/economy/memory+test。**6 个未防护面**：intervention_pack agent_delta/money_delta 第一层 entity key〔高·AI 直出面〕、调用类型注册表 key、币种定义 key、汇率 key、行业景气 key、_mod墓碑库 key〔低·随 mod注册表〕。**+ mod注册表 key 部分防护洞**：pack_id 正则 `/^[a-z][a-z0-9_]*$/` 挡 `__proto__`（首字符 *）但 `constructor`/`prototype` 全小写漏过→须补 是JS保留键。→ 路径 I superRefine 一步收全（仿账户键Schema·throw fail-closed·零迁移·零指纹/向量影响）。⚠ 2 loose end 待拍：① agent_delta 第二层 key（字段路径）「defer-M3 兑底」假设须验 apply-path 真挡保留键、否则同 superRefine 顺手保（plain-object 存储下第二层 `proto` 仍可污染）；② actor.ts 引擎内部 record 面（特质/状态/技能/物品/衣物/爱好/信念·提案 path）「经五道闸是否真挡保留键」CC 标待确认·别让低优先静默变安全·须确认或登记独立 follow-up。 ✅【Step1 完结·路径 I 一步收全·commit 2cd746e·+38 tests·test2373·tsc29/指纹84/黄金向量逐位恒等/红线 diff 空·零迁移·2026-06-18】：6 未防护面（agent_delta/money_delta key·调用类型注册表 key·币种定义/汇率/行业景气 key）+ mod注册表洞（补 是JS保留键·挡 `constructor`/`prototype`）全收。关键＝防护落 key schema 层（z.string().superRefine 在 Zod 构造前拦截；whole-record refine 会漏 `proto*_`——result[键]=value 设原型非 own property·Object.keys 看不到）·agent_delta 第一层+第二层均拦。★② 五道闸结论＝securityBoundary 校验effect包过五道闸现为 stub（未接线·留 P0-6）→ actor.ts 内部 record 面**未受保护·已登记 notion-67 独立 follow-up**（外部写入口 agent_delta 已拦·非本批作用域）。残留＝deltas.path/handlerRef registry 严格化（defer 路径 II）+ null-proto 存储层（defer·范围大）+ actor.ts 面（follow-up）·故仍 [ ]。
- [ ]  🟠【handlerRef 进指纹·6.59·2026-06-16 新登】handlerRef 集（side_effects／cascade_on_change）P0-6 接线时成判定面输入 → `fingerprintManifest.ts` 收口（[action.md:100](http://action.md:100)「改 side_effects 集→指纹变」）；Step 3-A「指纹零变」注释仅指本批字段缺省 undefined 不动现有存档指纹·**非永久排除**·P0-6 fire 时须进 AA6 全名单＋「成员变→指纹变」断言✅**【拍板 2026-06-18·Wave 4 走薄壳：只把 handlerRef 加进 AA6 成员名单（doc/枚举登记）·不接活线（不写 hashPresetFingerprint 消费）·详细接线后补·避开 fingerprintManifest 红线】** ❓-薄壳登记·活线后补
- [ ]  🟠【DP·验签放行】云端策略包当远程 mod 包过同一道闸：6.74 验签（作者公钥验内容哈希）+ 预设模块分类器（判「叙事皮肤」放行注入·命中真值绑定=拒收）+ 警示族；**纯声明式·禁内嵌任意 JS**（R6-a 同款） ❓-P0-6 验签放行·未实装
- [ ]  🟠【Y13·二十一批】世界书「设定/状态」分界硬化：实体现状与秘密禁入世界书（第二真相源宿主版＋旁路知情过滤），导入闸对含实体名＋状态断言条目作者警示（并入警示族） ❓-P0-6 导入闸·Y13分界未硬化
- [ ]  🟠【签·6.74·本轮回填】导入闸验签：用 `作者公钥?` 验 `签名?` 是否盖在内容哈希上（= B1c 生效中包集哈希里该包单包内容哈希）；验不过 → 拒收或「未验证 / 疑似篡改」警示（并入警示族）；与 K6 pack_id 同道安检·原生卡包通道（⑮）零豁免·诚实边界：防去水印 / 改作者名 / 篡改后分发，不防原样整包转发（平台授权管） ❓-P0-6 导入闸验签·未实装
- [ ]  🟠【6.76 续·硬化批·本轮加入】IM1 二审审稿提示词注入面（数据化喂入+输出严格 schema+无写权沙箱+重写再过机械校验+导入注入扫描）；IM2 写权限/抢话档入「玩家主权不可被 mod 锁定」清单·P1 安全地板凌驾预设不可关；IM3 文风/维度/剧本键全入受治理键空间+pack_id 命名空间化（S 批同族）；IM5 mod 二审维度默认全局+只标记不重写（重写=引擎内置维度特权）；L3 人称二元组合法性导入闸校验（全知×第一人称非法·并入警示族·🟢 L3 ✅完结 3040f53 checkL3PersonGate·余 IM1／IM3／IM5 未实装）——详见蓝图 6.76 续 ❓-P0-6 硬化批·IM注入面未实装
- [ ]  🟠【卡-A·源码核验】L0 判型严校 `spec`/`spec_version`（`chara_card_v2`/`v3` 强校验·防「有 spec 字段就当 v2」伪造/半残卡静默走错分支）——接现有 L0 后判型·只补严校（ST `getCharaCardV2` 无 `spec_version` 强校验=反例） ❓-卡格式改判P1·随全导入器
- [ ]  🟠【卡-C＋正-B·源码核验】卡/预设内嵌 `regex_scripts` §6 三选一判定（保留/豁免/墓碑）＋ 按 avatar/preset 粒度执行白名单·**默认隔离须显式授权**（ST `allowedOnly` 官方先例·SCOPED/PRESET 正则默认不跑·`promptOnly` 正则=注入面·`replaceString` 可藏指令）——接现有 §6 净化·与三十·卡格式同窗 ❓-卡格式改判P1·随全导入器
- [ ]  🟠【卡-A·源码核验】L0 判型严校 spec/spec_version（chara_card_v2/v3 强校验·防「有 spec 字段就当 v2」伪造/半残卡静默走错分支）——接现有 L0 后判型·只补严校（ST getCharaCardV2 无 spec_version 强校验=反例） ❓-卡格式改判P1·随全导入器
- [ ]  🟠【卡-C＋正-B·源码核验】卡/预设内嵌 regex_scripts §6 三选一判定（保留/豁免/墓碑）＋ 按 avatar/preset 粒度执行白名单·**默认隔离须显式授权**（ST allowedOnly 官方先例·SCOPED/PRESET 正则默认不跑·promptOnly 正则=注入面·replaceString 可藏指令）——接现有 §6 净化·与三十·卡格式同窗 ❓-卡格式改判P1·随全导入器
- [ ]  🟠【声明式 effect 包·低门槛档·TinyWorld】⏳**schema ✅**（intervention_pack_v1Schema·B4 哈希层 commit 9e1c830）·过五道闸+clamp fire B6 导入闸之上留「简单声明式 effect 包」档（`agent_delta`/`money_delta`/`flags_add`·intervention pack 版本化格式 `xxx.intervention_pack.v1`＋错误收集不静默＋`_float_delta`/`_safe_int` clamp 安全落地）——降 mod 作者门槛·产出仍过五道闸·与「规则补丁（参数面）」并列为 mod 第六种轻量形态 ❓-P0-6·过五道闸+clamp fire B6
- [ ]  🟡【S2·6.59·母题】母题写入口注册闸 fire（母题注册 schema 已埋·`secret.ts` 母题仍开放 string·fire 时**不收紧字段**·是否真正统一进键空间留 mod 系统设计批） ❓-P0-6·母题注册闸fire未接
- [ ]  🟡【deltas.path/handlerRef·6.59】refine 由 fail-open 收紧为严格（Step6/7 落 refine·registry 尚空 fail-open 放行·P0-6 registry 实际填充键后才严格拒未注册键） ❓-P0-6·refine待registry填充
- [ ]  🟡【S3·6.59·繁简】繁简只警示（需简繁对照表·只 warn 非规范化·21:23 拍板降级·`governedKeySpace.ts` 已留 TODO(P0-6)） ❓-P0-6·繁简对照表未落
- [ ]  🟡【⑮·六·三十之二 已贴入】原生卡包导入通道分流：L0 后判型（manifest 原生声明＋semver＋Zod 全过=跳 L1 直入·零幻觉；任一条目失败仅该条目降级回 L1·非阻塞·混合包合法）＋缺「原卡整卡冻结快照」作者警示（并入警示族）——同一管线 L0 后分流、禁做第二套导入管线 ❓-P0-6 导入通道分流·未实装
- [ ]  🟡【增审4·已拍板 ✅】表达式/谓词 DSL 最小文法冻结：白名单运算符＋路径引用＋取整接 H7 三件套；文法版本进指纹、随 U3 版本分段——P0-6/P0-7 写求值器前定稿 ❓-P0-6/P0-7·DSL文法冻结未落
- [ ]  🟡【三十·卡格式·已拍板 ✅】卡关系声明过五道闸＋Z2 对账闸、缺方向槽 → 作者警示（并入警示族）；导入冲突检测口径：同方向关系边/同恋人位重复 → 提示玩家（覆盖/并存/取消）、禁静默覆盖 ❓-P0-6·关系声明过闸未实装
- [ ]  🟡【N4 悬案·四 已贴入】「遗产不跨枝」= 预设可开＋钳制（默认禁·只许知识/称号/血脉·可累积数值资源默认排除·过第④闸钳制）——三悬案已裁 ✅ ❓-P0-6 导入闸·遗产钳制未实装
- [ ]  🟡【第三批落地·3-3】表达式/谓词 DSL v1 文法冻结（EBNF+版本号·写求值器前定稿）已写入蓝图 ❓-已写入蓝图·求值器实装未落
- [ ]  🟡【第三批落地·3-9B】区域键（地图.地点.类别 区域级取值）入受治理键空间·第②闸+导入闸归一 已写入蓝图 ❓-已写入蓝图·归一实装未落
- [ ]  🟡【第三批落地·3-8B-③】全局 grep 旧路径（重点改读取侧派生）+ 一致性脚本 diff 归零（独立 PR） ❓-独立PR·grep实装未跑
- [ ]  ✅【反向核查·S-1·已拍板】存量「未结算」表达式/谓词跨 DSL 版本 = **向后兼容**（v2 只增不改不删语义·新读旧·绝不迁移重写·迁移重写仅作大版本逃生口）；配 fixture gate「v1 表达式当前文法 parse 过且求值逐位一致」；已写进 M·1 DSL 规格头与蓝图 DSL v1 条目⑧——DSL 文法可正式冻结 ❓-已拍板·fixture gate实装未落
- [ ]  🟡【导入保真度三档·TavernHeadless】⚠**未覆盖→已补入 P0-6 主战场 B6 DoD（落血统·脱包·同卡同档同结果）** 预设/卡判定加 `compat_strict`（严格复刻 ST）/`compat_plus`（兼容＋声明式增强）/`native`（原生重排）三档·把「宏/条件不支持」纳入档位语义而非一律拒——落 AA3 §1 预设判定档位·与原生卡包导入通道（⑮）同台 ❓-P0-6·三档判定未实装
- [ ]  🟡【卡-B·源码核验】未知 extensions/未知宏 **降级保留不执行**（ST helperMissing catch-all 原样保留花括号·绝不当指令执行）——＝ TavernHeadless 软拒中间档「降级保留（不进判定）」同源·导入闸分类在 豁免/墓碑 之间加此档 ❓-卡格式改判P1·随全导入器
- [ ]  🟡【卡-D·源码核验】卡内嵌 character_book 拆进 _lore知识库 ＋ 计时三件套（sticky/cooldown/delay）锚游戏绝对时间 ＋ use_regex 关键词永远是正则（中文无词边界分块铁律）——接卡格式导入·与独立 lorebook 字段同构 ❓-卡格式改判P1·随全导入器
- [ ]  🟡【卡-B·源码核验】未知 `extensions`/未知宏 **降级保留不执行**（ST `helperMissing` catch-all 原样保留花括号·绝不当指令执行）——＝ TavernHeadless 软拒中间档「降级保留（不进判定）」同源·导入闸分类在 豁免/墓碑 之间加此档 ❓-卡格式改判P1·随全导入器
- [ ]  🟡【卡-D·源码核验】卡内嵌 `character_book` 拆进 `_lore知识库` ＋ 计时三件套（`sticky`/`cooldown`/`delay`）锚游戏绝对时间 ＋ `use_regex` 关键词永远是正则（中文无词边界分块铁律）——接卡格式导入·与独立 lorebook 字段同构 ❓-卡格式改判P1·随全导入器
- [ ]  🟢【V5·6.58】15 语义动词缔结/解除对称性审计 + 派生量落实值红线复核 ❓-P0-6·对称性审计未实装
- [ ]  🟢【警示族打包】E1b 零成本宿主媒介 / J2b 振荡对 / H1 年化>100% / 6.48 停用轴配套 / G7 冷却0拦截器——同一警示管线一次实装 ❓-P0-6·警示管线未实装
- [ ]  🟢【AA4·四 已贴入】JS 保留键黑名单（`__proto__`/`constructor`/`prototype` 拒收或加前缀·存储层 null-prototype 或 Map）并入 S3 双卡口 ❓-P0-6·黑名单并入S3未接
- [ ]  🟢【组-F·源码核验】导入结构校验 ＋ 按稳定键（identifier）去重合并·冲突报警**不静默覆盖**（ST mergeKeepNewer 新覆旧·我方更严）——接现有导入合并 ❓-卡格式改判P1·随全导入器
- [ ]  🟢【组-F·源码核验】导入结构校验 ＋ 按稳定键（`identifier`）去重合并·冲突报警**不静默覆盖**（ST `mergeKeepNewer` 新覆旧·我方更严）——接现有导入合并 ❓-卡格式改判P1·随全导入器
- [x]  🟠【S3·6.59】⏳**B5 Step4 读卡口已接 migrate.ts·写卡口纯断言已交（commit 5caaac9）·活线接 hosts/ fire B6** 码位规范化双闸 wiring（`规范化键码位` 纯函数 NFKC＋去零宽＋trim 已交·待接导入口/写入口归一·S4 归一时点 P0-7·禁第二实现）✅**【Step0 侦察完结·关闭·2026-06-18】**：CC 全库 28-pattern grep 证 **双闸已全接·零未接写入口**——读卡口 normalizeRegistryKeyNames(migrate.ts:1157·5caaac9)/写卡口 assertGovernedKeysNormalized(migrate.ts:1238·随 S1/S1b path I 2872c24 接线)·注册表键写入唯一管道=migrate rawMigrated 构建阶段、读卡口归一后到写卡口断言前无再写。**hosts/ 零注册表写路径**（serializeArchive=SliceSnapshot 无注册表键·verbScope.test.ts:162-166 编译期隔离）→「活线接 hosts/」实为 **no-op + 触红线**·**维持 defer（hosts ALERT·defense-in-depth）**。S4 归一时点单一调用点不成立→@P0-7（不在本行）；繁简只警示=独立 🟡 行 @P0-6；本行 wiring 完结故 [x]。⚠ **forward-coupling**：path II populate 落地的「导入期键登记写入口」**必须过同一 `规范化键码位` 归一·禁绕 S3**（已登记 notion-67）。 🆕 **S3 写卡口〔RootState 层·非 hosts/〕✅完结 commit fbef62d（2026-06-19）**：原「维持 defer hosts ALERT」口径更新——写卡口落 migrate.ts 核心层〔checkS3WriteGate 纯观测·扫 NPC·mod注册表·_mod墓碑库·调用类型注册表 record 键命中 是JS保留键→MigLog error ALERT·fail-open 不 throw〕·defense-in-depth 最外圈·hosts/ 侧仍 no-op〔红线·与本写卡口正交〕·test 2532→2536〔+4〕·指纹 84/红线空·以 P0-6 主战场为准。
    - [x]  🟠【解除通道·拦截器引用·6.59 行生·2026-06-16 新登】`不可逆Schema.解除通道`（verb.ts:100·`z.string().optional()`·无 TODO）收紧为受治理引用——**现 11 命名空间枚举无「拦截器句柄」槽**·须先补第 12 槽（或归死亡拦截器同一管线·设计页 §十一「拦截器引用须来自受治理注册表」）才可 refine；Step 7 据此**不碰**·留此独立缺口（namespace-enum 决策·非纯 add-constraint）✅**【拍板 2026-06-18·补第 12 命名空间槽「拦截器句柄」→ 解除通道收紧为受治理引用·随槽 add-constraint 一并落·Wave 1 次活线】** ✅**【Step0 侦察完结·2026-06-18】**：⚠ off-by-one 订正——枚举现已满 **12 项**（非 11·注释「封闭·12项·锁定」），「拦截器句柄」加入为**第 13 项**；`解除通道`（verb.ts:102·`z.string().optional()`·无 refine）消费者仅 actor.ts:75/97（特质/状态·不可逆）+ test·引擎层 irreversibleGuard 全 stub 不读值；已存测试值「断肢重生术」（纯 CJK 过 `\p{L}+` 正则）不假拒 → **纯 add-constraint·零迁移·可立即动**：enum +「拦截器句柄」+ 解除通道改 `受治理句柄Schema`（已实现·非空+JS保留键黑名单+扁平正则·side_effects/cascade 同款复用·成员级校验 fail-open 留 P0-6）+ 更新 governedKeySpace.ts:79 注释（「不碰」反转）+ 计数 12→13。⚠ **死亡事件.拦截(actor.ts:520)/死亡拦截器条目.条件引用(preset.ts:418)= 配方表键(formula ref)·与拦截器句柄不同命名空间·本批不一并收**（是否另立配方键受治理面留后续考量）。 ✅**【Step1 完结·2026-06-18·commit 82e1fbe·+5 tests·test2378·tsc29/lint220/红线 diff 空·z.infer 仍 string 零迁移】**：enum 12→13 加「拦截器句柄」+ verb.ts:102 解除通道 → `受治理句柄Schema.optional()`（复用·一行）+ governedKeySpace.ts:79 注释反转+计数 13。测试：断肢重生术（纯 CJK）不假拒 + JS保留键×3 被拦 + 空串 + 含点号（违扁平正则）被拦。成员级校验仍 fail-open（随 path II / deltas.path·handlerRef 严格化同窗 fire·P0-6）。本行收紧完结故 [x]。

## 🍺 P0-6 · 酒馆功能导入闸

- [ ]  🟠【酒馆·内容分级闸】内容分级 mod 校验：硬审查模型（Gemini safety/OpenAI moderation）打不穿 prompt → 开成人分级时，若玩家配置了NSFW模型先问玩家是否回退，可选总是回退，或仅这次回退，点不再提醒后不再出现提示 ❓-P0-6 酒馆导入闸·未实装
- [ ]  🟡【酒馆·RAG兼容闸】embedding 模型兼容性校验：换嵌入模型必重算向量·导入闸警示「向量库与当前 embedding 不匹配」 ❓-P0-6 酒馆导入闸·未实装
- [ ]  【规则补丁冲突套娃】合并策略每键声明（覆盖/钳制/加权/叠加/锁/后载覆盖）替代全局「取严」；优先级降序 官方·作者·玩家（玩家自调最高）；绝对化必须显式 锁；冲突出预览报告（接 DSL trace/试算闸）——合并语义进键 schema（焊死前） ❓-P0-6·合并语义进键schema未落
- [ ]  🟡【命名实体音译漂移·写入侧】实体键唯一真值（名字串绝不进键位）＋ 摄取/记账边界别名解析（解析不出走澄清/新建）＋ 别名表集中登记（扩昵称1/2/3）＋ 方言皮肤渲染确定性（皮肤×实体键→固定串） ❓-P0-6·别名解析未实装
- [ ]  🟡【mod 发布前静态校验＋试算闸】作者侧：引用键存在/类型/除零/选择器空集/回退完整性 ＋ dry-run 出轨迹预览；挂导入闸前，与运行时玩家侧 trace 两个独立挂载点 ❓-P0-6·试算闸未实装

**→ P0-6 导入闸（baseline 拒收越界 mod·随 baseline 实切上线）**

- [ ]  🟡【BASELINE·导入闸】SFW/light 包导入携带 explicit/community 覆盖串或破限引子的社区预设/动态提示词包 → 拒收+提示「当前发行版不支持该内容档」（复用预设模块分类器+作者警示族） ❓-依赖baseline·导入闸未实装
- [ ]  🟢【BASELINE·确定性确认】内容分级已在 FINGERPRINT_EXCLUDED_FIELDS·baseline 门不影响指纹/重放（跨 baseline 同事实档存档事实/指纹恒等·呈现尽力·复用 SV2）——无需新指纹工作 ❓-依赖baseline·确认待落

### 🎚️🎬🤖 玩家主权 / NSFW 四档 / 破限引擎化 · P0-6 导入闸/专家模式门 行（源块见文末三模块）

- [ ]  🔴【🎬NSFW四档·皮肤层/IM4】永禁第三方 JS 主页面同源裸跑(会泄 API key/篡改存档/冒充玩家发请求)·对社区明牌此安全边界(非黑箱锁) ❓-P0-6 安全边界·明牌未落
- [ ]  🟠【🎚️玩家主权】专家模式风险门：手动勾选「启用专家模式」＋强制阅读风险提示(API封号/内容不可控/破限不保证)才解锁覆盖·默认关·预设带覆盖串但未开专家=忽略回落引擎内置 ❓-P0-6 专家模式门·未实装
- [ ]  🟠【🎬NSFW四档·皮肤层/IM4】自定义 HTML/CSS/JS 两条安全开法：①声明式 HTML/CSS(IM4净化·无script·纯展示) ②沙箱 iframe(跨域隔离·JS 拿不到 token/DOM/存档·同 Notion HTML 附件机制) ❓-P0-6 皮肤层·沙箱未实装
- [ ]  🟠【🎬NSFW四档】社区预设模块分类器：导入逐模块分「叙事皮肤」(节奏/视角/文风/防抢话/破限/NSFW→放行注入叙事)vs「真值绑定」(时间状态栏/关系值/衣物/进度→只接管文字样式·数值回引擎账本渲染) ❓-P0-6·预设模块分类器未实装
- [ ]  🟡【🎚️玩家主权】覆盖作用域闸(导入侧)：携带 SystemPrompt 覆盖时校验仅命中「叙事」条目·命中记账/检定/谜底校准/结算=拒收＋作者警示(并入警示族)·引擎不验证能否真破限(玩家自负) ❓-P0-6·覆盖作用域闸未实装
- [ ]  🟡【🎬NSFW四档】真值绑定提示：命中真值绑定模块提示「数值由引擎账本接管·仅样式生效」——防社区预设把状态栏塞单次叙事调用致玩家误判失效 ❓-P0-6·真值绑定提示未实装
- [ ]  🟡【🤖破限引擎化】接预设模块分类器：命中「思维链锁/破限引子」类模块(system/assistant 文本块)归叙事皮肤放行·按模型族归位对应引子槽·真值绑定模块仍回账本渲染 ❓-P0-6·引子归位未实装
- [ ]  🟡【🤖破限引擎化】预填随专家模式门：`内容分级=community` 时玩家覆盖串可携带自定义 assistant 预填 **＋ 思维链引子(CoT)** 一并覆盖(专家模式解锁后·覆盖 assistant 消息与模型思维链引子两者)；off/light/explicit 一律用引擎内置预填与思维链引子 ❓-P0-6·预填门未实装
- [ ]  🟢【G8·6.43·回想截断注释】6.43 补一行：parent 链拼接在每个祖先块按子节点 `父快照拍号?` 截断，禁拼入 fork 点之后的祖先行（防裸SL/fork 后回想出现幽灵未来行） ❓-P0-6·6.43注释未补
- [ ]  🟢【三十二·D1/E1·文档】D1 三十「弃用清单」补节（v1 七项裁决收编去向 + `固定开场白?{}`→`开场白?[]` 升格）；E1 三十总纲四补半句「变体/场景/modifier 夹带约束类补丁按 K5 取严、不得后层放松」（D2 6.42 importer 标注已修） ❓-P0-6·弃用清单补节未落
- [ ]  🟢【律明文写入决议·并入贯穿全程文档同步】律甲/乙/丙/丁/戊/己/庚/辛 各口径明文统一并入「全部已拍板口径同步进修订决议与 docs/spec」umbrella 行，不单独成行（重点确保律庚/辛「换底片不换照片·已结算归历史」、律戊/己「数源单一·结构对账」在决议在册） ❓-文档同步·律口径未入册

## **如何最大降低进 P0-6 的焊死风险**

「焊死」=一旦 P0-6 五道闸/派生白名单/段结构写死，schema 和指纹契约就不再零迁移。按杠杆从大到小：

- [ ]  **穷尽 schema 黄金窗口（最大杠杆）**——把所有新功能要的 schema 位一次性可空预埋：姓名知识、临时容器、effect 包格式、反代端点档(protocol/baseURL/apiKeyRef/modelId)、副作用级别(none/sandbox/irreversible)、越界动词族、保真度三档、AOHP option schema。**漏一个，P0-6 后补就要动闸+迁移。** ⏳ 本轮收口大头（1486绿）：姓名知识/临时容器/反代端点档(protocol/baseURL/apiKeyRef/modelId)/副作用级别/保真度三档/AOHP option schema 全落；✅ effect 包格式已落（packages/core/schema/memory.ts 扩既有 intervention_pack_v1Schema 加 5 可空字段：pack_id／deltas〔path 占位／op／value／max_delta?〕／trigger／side_effect_level〔复用共享枚举〕／content_hash〔占位·留 P0-6 进 B1c〕·旧三字段 agent_delta/money_delta/flags_add 不动·.strict() 保留·未挂 RootSchema/迁移管线·零迁移·1772 绿·blueprint diff 归零·2026-06-16）。**越界动词族本批未做·改判拆批**：动词表 schema 全仓不存在（仅 docs/spec·V3 目标槽联合类型／V5 缔结解除对称性未定）→ 不糊最小版（防焊死前埋雷＋返工双迁移），拆独立批〔动词表批 = V3+V5+越界动词族〕排 effect 包后、焊死前；6.59 受治理键空间注册表类型亦未落 → deltas.path 先字符串占位（待 6.59 收紧＝加校验非改存储形状·零迁移）拆〔受治理键空间 6.59 批〕；K6 单一权威 pack_id 收紧（含 mod 注册表既有字段·非零迁移）拆独立批·effect 包 pack_id 暂同 mod 现状 z.string().default('') ❓-焊死风控·schema窗口未穷尽(越界动词族/6.59拆批)
- [ ]  **指纹取材集补全并上「成员变→指纹变」断言**——effect 包内容哈希、中文数字解析规则版、DSL 文法版、求值器函数库版、反代 endpoint 排除项，一次补进 AA6 全名单。漏登 = 焊死后假分叉，最隐蔽最致命。 ⏳ 本轮补：中文数字解析规则版 + baseURL/apiKeyRef/modelId/protocol/临时会话/案底 入 FINGERPRINT_EXCLUDED_FIELDS（property 穷举断言）；DSL 文法版/求值器函数库版前轮已入；**剩 effect 包内容哈希（P0-6）** ❓-焊死风控·effect包哈希(P0-6)未补
- [ ]  **闸二六轮扩到覆盖新对撞面再重新冻结**——REPLAY-01 八场景 + C2 混沌猴把新机制的对撞场景全吃进去，全绿才解锁 P0-6（你们的两道闸纪律本就要求「红着不许往下走」）。 ✅ REPLAY-01 八场景 + C2 混沌猴（10 事件类）本轮全吃进（1486绿）·**重新冻结＝闸二第 7 轮 AA3 半天会 ✅玩家 2026-06-16 已签字放行**（剩 CC 收尾回写 docs/spec→勾完成） ❓-焊死风控·CC收尾回写docs待勾
- [ ]  **新接口先 stub + 编译期断言**——受控接口能力集、effect 包、AOHP option 按现有「6 个冻结接口 stub 三件套」模式写返回类型冻结+抛错，编译期证明核心调用(记账/检定/结算)结构上不含越权字段(覆盖串/预填/endpoint)。 ✅ 本轮 stub 落地：interfaces/irreversibleGuard.ts·interfaces/toolCapability.ts(受控接口能力集)·engine/deterministicGuard.ts + 4A–4D 断言（+37 tests·1486绿） ❓-焊死风控·部分stub已落·余未补
- [ ]  **安全边界在导入闸定稿前明文钉死**——反代 baseURL 导出剥离、IM4 第三方 JS 同源隔离、玩家主权安全地板、effect 包过五道闸钳制。安全洞焊死后再补要动闸逻辑，代价最高。 ❓-焊死风控·安全边界未钉

## P0-7 · 结算管线（规格定稿前 · 唯一动「段结构」的批）

- [ ]  🔴【守恒断言接线·选项A·从 per-entity 账本批拆出·2026-06-18】把 P0-6 的 `assertConservation` 纯函数**接进 `runTick` 结算管线**：H1 账面 clamp 之后断言 Σ全实体 getNetAsset===expectedNetAsset（蓝图落地映射②）·注入 `getNetAsset=(acct)=>持有[币种]+储蓄+存货估值(资产条目[类别='存货']·数量×成本价)+_应收−_应付`·两腿回约定库解析·sink 实体(SINK_ENTITY_KEY)纳入 Σ遍历·_费用 不进(报表流·防与 sink 双扣)·slice/soak rewire（server.ts:117 触 hosts/ 红线·独立 ALERT 批）。⚠ 字段已随 B5.6(c9e53d5) 重命名 _应收/_负债·存货寄生资产条目·_费用+sink 已预埋。⚠ **若不确定如何插入：CC 把 runTick 结算序 + getNetAsset 所需字段贴出 → 玩家手工插入**（用户 2026-06-18 指示）。
- [ ]  🔴【sink 实体 materialize + 只进不出 invariant·B5.6 派生·2026-06-18】SINK_ENTITY_KEY('**sink**') 实体零余额 init(P0-7 或迁移补)·getNetAsset 遍历 accounts 自动纳入 Σ·**动词层强制「沉没账户只进不出」**(从 sink 转出拒收·守恒承重不可被掀空)·SINK_ENTITY_KEY 保留键禁 mod 占用(随 B6/AA4)。
- [ ]  🔴【_费用 accrual 写入·B5.6 派生·2026-06-18】结算消费时点记 `_费用`(accrual·赊账消费即记)·与 `本期支出`(现金流出时点)**非双写**(不同事件·禁同一笔记两处)·`_费用` read-only 引擎写(_ 前缀派生·AI/mod 不可直写)·**不进 getNetAsset**(报表流)·消费动词的价值转移腿走 存货→sink。
- [ ]  🔴【模态栈三方并发·栈深 4 临界】模态栈满 × 看门狗 pop × 在途 Ring 2 响应三方撞一拍——epoch 围栏（过期响应作废）＋ pop 确定性落地态兜底（引擎合成系统文本·检定/HP 照真值）＋ 栈满拒推×急停 pop 同拍仲裁，进显式边状态转移表（接 F3 看门狗超时边 / AA1 调用世代号·并发临界专门核对）
- [ ]  🔴【D4·6.54】种子成熟日全局锚 + 埋种子动词「声明域」入参——⚠ **实装前必须先做审计 #1 的「全局拍号 vs 绝对时刻」术语裁定**，否则两种读法各写一半=重做
- [ ]  🔴【第三批落地·3-1】时间术语：D4 种子成熟日存「全局时刻」（写调用点前先 grep 替换·卡 D4）已写入蓝图
- [ ]  🔴【#1 术语裁定·四 已贴入】到期/成熟/排序锚**全局时刻**（主域纪元分钟）、悔棋/重掷才用**拍号**（步数·重掷语义专用）——卡 D4 写调用点前必落地、两词文档逐处分开（三悬案已裁 ✅）
- [ ]  🔴【6.76 续·硬化批·本轮加入】P1 自动代写**重大不可逆决策安全地板**（死亡/婚姻/血脉绑定/永久失核心资产归一类标记·无视档位强制降「需确认」·凌驾抢话档·类比 G7 引擎硬顶）；G3/G4 代写候选先过二审才落账·二审只改措辞不改落账事实·NPC自主/观战拍先落账二审异步纠偏（防双 LLM 串联延迟雪崩）；G7 快播=照常结算只不呈现（盐值照登记 tick_log）；P4 观战快播期主角自己的到期事件走离场演化自动结算（同 6.66·主角临时变离场实体）；G10 单拍物理审 vs 舞台几何校验分工；G11 actor_source 扩写 vs 新决策；M5 后果可见因果不可见——详见蓝图 6.76 续
- [ ]  🔴 **6.67 记账容错规格** → P0-7：〔重试记账〕手动通道（⚠条目可寻址跨拍存活）＋跨拍补偿性新账时点语义（append-only 带原事件 id 血统）＋幂等纪律
- [ ]  🔴 **Z3 条目级双向核销** → P0-7：无血统动词拒收（幻觉多记）/ 提案条目未消耗打回（漏记）；提案单即事务组（V1 红利）；提案单随覆写日志落账
- [ ]  🔴 **Z5 失败工单冻结**（Z 批最毒·6.67×6.58 接缝）→ P0-7：⚠落账瞬间表达式按原拍拍首快照预求值为定值，连同同段叙事＋提案单冻结成工单；一切重试只消费工单
- [ ]  🔴【irreversible 重放灾难·对撞⑤】`irreversible` 副作用工具产出**进冻结载荷**（同 Z5/R3-a）·重放短路读冻结·**绝不重调外部**；含 irreversible 的拍**重掏须禁/强警示**（防重复发邮件/扣款·已发生不可回滚）
- [ ]  🔴 **世界状态跨任务持久化，且从前序任务"终态"更新**（quests 篇：建好的桥在后续任务保留，除非中间任务炸了它；Domain DB 的 world state 由 final states 更新）。治 bug：内容单元之间状态不连续（前一任务改了世界，下一任务又复位）——这正是你"单写者 + 能派生的不存储"在**内容链层面**的落地校验点。
- [ ]  🟡 **规划自顶向下递归分解 + 中断后从反应点重规划**（正篇 day→hour→5-15min；否则"12点吃了午饭、12:30 又吃"）。治 bug：NPC 日程与突发事件冲突、重复已完成动作。日程结算要支持"被事件打断 → 从该拍重生成剩余日程"。
- [ ]  🟡 **信息扩散即正篇 emergent 行为**（投票候选/派对消息两天内 1→8、1→13 人）。对照你涟漪引擎 6.37。治 bug：检查你的两跳衰减 + 知情过滤是否复现"二手转述也会失真/丢失"（正篇有 1.3% 关系幻觉、转述链衰减）。
- [ ]  🟠 **月结挂自然月边界**(蓝图 4.7 注释有,排程无独立行)
- [ ]  🟠  提案血统?／无血统拒收／未消耗打回／提案单即事务组
- [ ]  🟠 **被动到期不参与截断、拍末清退**(P0 接受积分误差 / P1 分段积分选项)
- [ ]  🟠  **叠加结算分量级"已结算"标记接线**
- [ ]  🟠 **历法对齐拍自动续拍**(蓝图 6.45 散文有,排程无独立行)
- [ ]  🟠  无血统动词拒收（幻觉多记）/ 提案条目未消耗打回（漏记）/ 提案单即事务组（V1 红利）/ 提案单随覆写日志落账
- [ ]  🟠  失败工单本体：落账瞬间表达式按拍首快照预求值为定值 + 同段叙事 + 提案单 冻结成工单；一切重试只消费工单
- [ ]  🟠【AA1·四 已贴入】Ring 2 在途调用信封加可空 `调用世代?`（贴全局回滚计数器读数＋拍锚·零迁移）——回滚/fork/关账后过期返回丢弃 ⏳ schema 字段已落 memory.ts（11fc33a·4.8）；P0-7 结算管线主接线待
- [ ]  🟡【Q1/Q2/Q4/Q6·6.60】知情类 extensional 写入口展开落账 + 消费点两类枚举表定稿 + 谓词引用规则（限深 1、环拒收）——⚠ 与 W1 聚合分级的棘轮边界见审计 #8
- [ ]  🟡【sandbox 暂存原子性·对撞⑤】`sandbox` 工具暂存→commit 须在**级联轮单个阶段内原子完成**·不得跨轮 commit（防破「读拍前快照/写本阶段结果」隔离·接 J1 不动点迭代）
- [ ]  🟡【反代 modelId 双写·对撞②】反代档 `modelId` vs `$预算控制台.NSFW降级目标模型键` 两个模型键引用 re-grep 防双写漂移（同 N-7 单写·画像侧若需即派生现算）
- [ ]  🟡【越界案底分治·对撞⑦】官府正式案底=**append-only 棘轮**（立案不忘） ⊥ 民间风评=可衰减印象；案底过期锡游戏绝对时间（非墙钟）——接审计 #8 W1 聚合 vs 棘轮分治
- [ ]  🟡【增审7·已拍板 ✅】「消息发信槽位（雇主/母亲/恋人）」入 6.60 消费点两类枚举表 = 资格类现算＋发信落账瞬间定格
- [ ]  🟠【J1/J6·6.61】级联结算轮段 + 挂起命中队列（检出不吞、结算可延；本批最毒）——⚠ 与 W2 脏标记的字面冲突见审计 #7
- [ ]  🟠【F4·6.56】D 段零 await 同步临界区 + CI 静态检查 + 看门狗作用域明文
- [ ]  🟠【新排查·边沿读取·接 J2/J4＋P0-9 J3】级联轮/边沿判定一律从**拍前快照版本**读「上次观测值表＋挂起命中队列」、禁读现行内存态——否则悔棋/回滚后跨拍边沿判定漂移（与 §九第9类重放输入同源·回滚单元在 P0-9 J3 焊）
- [ ]  🟠【AA1 主接线·四 已贴入】结算管线对 Ring 2 在途返回做世代核对：号对不上即弃；与失败工单重试同时返回走幂等防双落账
- [ ]  
- [ ]  🟡【模型名归一镜像·Luker】反代/custom model id 归一 ＋ client/server 双份镜像不变量（normalizeTokenizerModel 注释明示「与 server getTokenizerModel 配对·改一处改两处」）——接 re-grep 双写点⑧·防反代 model id 归一双写漂移
- [ ]  🟡【J2/J4·6.61】观测点边沿语义（阶段边界序列、txn 组内不观测）+ 同触发器轮内不重入
- [ ]  🟡【V1/V6/V2·6.58】txn 组级提交 + 到达序/拍首快照求值两条 + 约定库扫描接线（成熟日 D4 锚定）
- [ ]  🟡【C2/C7·6.53】竞争仲裁阶段（确定性抽签种子纪律）+ 全席位意图屏障节拍公平
- [ ]  🟡【D1/D3/D6·6.54】跨域资金一次性结账 / 解封补结区间口径 / 跨域同拍序键（全局拍号,域ID,种子id）
- [ ]  🟡【G3·6.49】指令组锚点快照 pin
- [ ]  🟡【K7·6.52】种子载荷埋点快照（永不回查包）
- [ ]  🟡【E4·6.55】涟漪先落账、媒介拍末取材进固定结算序
- [ ]  🟡【S4·6.59】归一时点钉死=写入口（读取点永不再归一）
- [ ]  🟡【Q3·6.60】落账型消费瞬间定格 + 入圈四通道唯一合法写入点
- [ ]  🟡【缺口1/8】谢幕前拦截扫描步 + 每拍盐值登记进 tick_log
- [ ]  🟡【M7·6.50】模糊预感确定性采样
- [ ]  🟡【R1/R3·6.63】调用点按三段契约写死 + 回合边界外部事件最小载荷注入
- [ ]  🟡【6.67】〔重试记账〕手动通道 + 跨拍补偿性新账 + 幂等防双落账——⚠ 速记载荷住址未写，见审计 #12
- [ ]  🟡【断言②·第四项核验】重掷整组重跑恒等（F1 组内隔离实证）——「恒等」= 同一次重掷可重放＋元层写不进组内重放集，NOT「重掷=原值」（计数器+1→普通通道换骰·注释钉死防误写）；依赖 F1（P0-4）＋G3 锚点 pin＋AA1 在途世代号；时间窗 = P0-7 之后；= AA-合并会场景①「悔棋后旧任务迟到」最小前置
- [ ]  🟡【第三批落地·3-2】级联轮「每轮全量重扫」＝脏标记「只重扫被改对象」确定性等价（明文一句）已写入蓝图
- [ ]  🟢【6.62】「引擎自动启停补丁集=确定性结算行为」一行注释
- [ ]  🟢【宏-B/pick·源码核验】确认 random 必改确定性种子已覆盖；补登 pick 哈希种子法（会话哈希×内容哈希×位置偏移=「unique but repeatable」）可借鉴到「同情境多候选择一」的确定性 RNG——接确定性铁律（已基本覆盖·补 pick 借鉴落点）
- [ ]  🟢【ReactionQueue 阈值口径·对撞⑦b】ReactionQueue 去抖/深度上限阈值 与 涟漪稀疏化阈值（W1/W4）**走同一口径**·不另设第二套阈值打架（两套谣言/级联防风暴机制差一拍即不一致）

## 🍺 P0-7 · 酒馆功能结算管线（真缺口核心）

- [ ]  🔴【同拍结算管线·案例 1/3/5】确定性定序 ＋ 级联不动点 ＋ 读快照/写结果隔离：① 阶段定序＋阶段内序键（拍号,阶段序,域ID,成熟日升序,id 字典序·接 D6 跨域同拍序键 / D4 成熟日全局锚·连 INTRA-阶段也定序）；② 级联＝单写者 worklist 不动点迭代（visited 防环＋有界轮次·接 J1 级联轮 / 6.61 / $涟漪候选 缓冲上限）；③ 每公式显式声明读拍前快照 vs 读本阶段结果（默认读快照·案例 3 分红命门）；④ 涟漪＝有向图 BFS·单调衰减·阈值截断·取 max 防循环（案例 5·有向不对称是意图非 bug·知情本拍末落定影响下一拍）——验证＝固定种子重放比对状态指纹
- [ ]  🔴【纠缠闭包粒度·6.66】闭包沿战争/约定/父子强边扩展会否吞掉整个种群→locality 失效退化全图重算；弱边阈值截断够不够狠（解法同涟漪：累积强度单调衰减＋阈值截断界定闭包半径·非裸传递可达）
- [ ]  🔴【派生视图 vs 落账事件边界】作弊心声栏（照真值现算镜子）vs 侦探手机消息（过去真发生永久档案）两套机制：数据来源（现算 vs 读存储）＋ 存储语义必须分流（判据＝关掉再打开还在吗）
- [ ]  🔴【叙事漂移三道闸】① 重 roll＝重渲不重判（冻结账本复用落账事实包·接 6.67/6.41）；② 叙事→事实单向墙＋反向提案通道（有后果名词纳入意图速记必抽项＋对账闸散文↔提案单覆盖性校验·补 6.68 残留盲区）；③ 纯风味细节不落存——①② 属焊死级
- [ ]  🟠【酒馆·涌现判定·真缺口】涌现式交互判定（AI 裁决未预定义动作/组合 → 缓存固化为确定性规则）：首次遇未定义交互 → 一道受控 AI 裁决调用（裁可行性+难度·产结构化提案过五道闸）→ 结果缓存固化为确定性规则（交互特征→判定配方）→ 下次同类直接走缓存不再调 AI；缓存规则进指纹、单次裁决调用不进；缓存键用结构化交互特征（动词键+目标类型+情境标签）走确定性归一（复用 6.59）·语义聚类只遥测提名不自动合并——⚠ 五轮对撞 R3 三真缺口见对撞报告
- [ ]  🟠【slice 实证·搜查未知秘密须过检定 declassify·2026-06-16】承上 M4 旁路实证：玩家自定义“找掌柜藏的人”应被当作**搜查动作**译记账动词→调查/感知检定→命中才推暴露度/declassify、未命中不揭示，而非叙事直接坐实秘密；与本阶段「偷看/调查进记账」同口径（被撞见概率/解锁 flag/秘密暴露度＋）。**搜查对象=他人未知秘密时，知情程度 0→N 的唯一合法路径是检定 declassify，禁叙事直揭**——焊死前必接，否则 P0-6 后补要动判定通道。
- [ ]  🟠【反代 / 自定义端点档·AA3 §3/§7】模型键扩展 `{ protocol: openai-compatible｜anthropic｜gemini, baseURL, apiKeyRef, modelId }`（OpenAI 兼容优先）·key 走 R5-a 机密区（存档外·导出剥离）·反代＝普通模型键可挂任意槽位/`NSFW降级目标模型键`·走 N-6 房间级路由·健康探测＋fallback＋`routedVia`/`catalog_source` 可观测——**用户机密区主动配 ✅ 一等支持 / 卡预设内嵌 endpoint＋明文 key ⚰️ 墓碑**·不内置不分发反向取 key 脚本（中立）·`baseURL`/`endpoint`/`apiKeyRef` 进指纹排除 B1d（同预设走不同反代须重放判等）
- [ ]  🟡【副作用级别元数据·TavernHeadless】受控接口注册表条目加 `副作用级别?`（`none`/`sandbox`/`irreversible`）·`sandbox`＝页级沙箱（commit 才生效）·`irreversible`＝MCP 外部——接 R6 受控接口能力集·覆盖层写入默认落最小作用域＋显式提升
- [ ]  🟡【越界动词族＋案底记录·TinyWorld】`force_`/`attempt_`/`demand_`/`attack_`/`home_invasion` 越界动词族 ＋ `_notice_chance`/`_dodge_chance`（trait 概率·走 seeded rngFor）＋ 越界记忆＋严重违法案底（过期/案底状态）——接通缉=状态标签（6.32 待办）＋官府组织目标·律法线越界治理
- [ ]  🟡【单写者冲突仲裁复杂度·规模·本轮补入】多席位「意图屏障＋确定性随机仲裁」O(N²) 两两扫 vs 按资源分桶局部检测——800 人下须定复杂度上界（接 C2/C7 竞争仲裁·W2 性能结构确定性双跑）
- [ ]  🟡【认知档案/涟漪引擎 O(N²)·规模·本轮补入】稀疏矩阵 800 人下稠密化（上限约 80 万条目）→ $涟漪候选 缓冲硬上限＋溢出降级（接 W1/W4/W6 稀疏化阈值·案例⑤涟漪取 max·W5 名人涟漪 fixture）
- [ ]  🟡【触发扫描器索引化·规模·本轮补入】绝对日期到期走优先队列/堆 vs 全量线性扫；6.61 级联「每轮全量重扫」N 轮×800 人开销评估（接审计 #7 脏标记等价·W2 性能双跑·W5 万触发扫描 fixture）
- [ ]  🟡【实体/历史 GC 通道·规模·本轮补入】已 declassify 秘密/失效约定/灭亡组织/家族树 DAG/已故 NPC 归档——退休/冷存压缩通道防 2000 年单调上涨（实体级全覆盖·扩下条 NPC 持有数据 GC）
- [ ]  🟡【NPC 持有数据 GC】手机.消息流 等随 2000 年单调增长→老消息冷存/摘要化（呼应实体/历史 GC）
- [ ]  🟡【谄媚度→自我认知修正·案例 4】破产事件读拍前快照决策、谄媚度重算＋误差修正放本拍末认知结算阶段、写下一拍可见值（破循环依赖）；公式＝变化量按系数衰减＋类 EMA 锚上次值＋单步护栏（接 V3 实力分平滑）；多次修正靠滞后阻尼＋OCEAN 调制防震荡（接认知死锁人格调制）
- [ ]  🟡【认知误判死锁】投影补现实反馈/证伪通道（后果以涟漪回灌认知·信心双向·无矛盾≠确认）＋ OCEAN 调制纠正速率 ＋ 反 runaway 逃逸阀——投影漂移＝引擎机械函数非 LLM 自由编（单写者）
- [ ]  🟡【偷看/调查进记账】偷看手机＝玩家动作→记账译动词→触发判定（被撞见概率/解锁 flag/秘密暴露度＋·接 E1 翻看媒介落账管线）
- [ ]  🟡【落账消息确定性＋时间戳】事件发生拍由「引擎给真值→LLM 写文案→五道闸→落账」一次性生成、钉死时间戳、之后只读；多周目/回滚种子锚事件拍号不混回滚盐
- [ ]  🟡【媒介日期皮肤悖论】媒介日期存整型纪元分钟真值＋发行历法语境、禁固化渲染串；正文按发行语境渲染（diegetic 固定）、UI/检索按玩家皮肤渲染（接 E4 媒介拍末取材）
- [ ]  🟡【媒介可皮肤化字段族】货币/度量衡/地名同日期归位：存真值＋发行语境、不存渲染串；货币多一道历史汇率（发行时点汇率非当前）；在 6.41 媒介登记表标 可皮肤化{语境类型}
- [ ]  🟡【NSFW 饱和度→正剧拉回】NSFW 计入母题配额近期密度（玩家驱动也计数）→ 加速 NPC 疲劳 ＋ 抬高到期事件硬闯催熟权重（母题配额镜像）→必须有玩家可调节旋钮
- [x]  🟠【N-5·切模型×正剧拉回耦合环·P0-7/P0-8 接缝·✅1415·computeNarrativePullback 类型签名强制时点隔离 recentDensity=T1→finalIntentTags=T2·isNsfwScene 读 T2 不读 T1·阈值 0.7 exclusive·narrativePullback 14 tests】切模型 × NSFW 饱和度正剧拉回 须时点语义分离防反馈环：正剧拉回读「近期密度」调**叙事意图**（拉回前时点），场景检测器读「本拍最终意图标签」（拉回后时点）路由模型——两读点时点不同·不得互相回灌制造路由×拉回振荡
- [ ]  🟡【保底勿凭空造事＋快进久旱】久旱优先催熟已酝酿（临阈秘密/半程进度/将到期种子）；快进态挂 $流速.自动暂停触发 一条久旱自动暂停
- [ ]  🟡【酒馆·并发原语】并发调用编排原语：现严格串行（叙事→记账）·叙事+生图/语音并行需新增并发原语——P0 留接口、实装 P2·⚠ 作用域仅限呈现层并行不跨结算临界区（R4）
- [ ]  🟡【单写者冲突仲裁复杂度·规模·本轮新补入】多席位「意图屏障＋确定性随机仲裁」O(N²) 两两扫 vs 按资源分桶局部检测——800 人下须定复杂度上界（接 C2/C7 竞争仲裁·W2 性能结构确定性双跑）
- [ ]  🟡【认知档案/涟漪引擎 O(N²)·规模·本轮新补入】稀疏矩阵 800 人下稠密化（上限约 80 万条目）→ `$涟漪候选` 缓冲硬上限＋溢出降级（接 W1/W4/W6 稀疏化阈值·案例⑤涟漪取 max·W5 名人涟漪 fixture）
- [ ]  🟡【触发扫描器索引化·规模·本轮新补入】绝对日期到期走优先队列/堆 vs 全量线性扫；6.61 级联「每轮全量重扫」N 轮×800 人开销评估（接审计 #7 脏标记等价·W2 性能双跑·W5 万触发扫描 fixture）
- [ ]  🟡【实体/历史 GC 通道·规模·本轮新补入】已 declassify 秘密/失效约定/灭亡组织/家族树 DAG/已故 NPC 归档——退休/冷存压缩通道防 2000 年单调上涨（实体级全覆盖·扩下条 NPC 持有数据 GC）
- [ ]  🟢【权力递归读取成本·规模·本轮新补入】`实际影响力 = Π(路径势力份额)` 现算·深层嵌套×高频读取是否加缓存（P1 调平·接 Q/W 分级调平）
- [ ]  🟢【ReactionQueue 防风暴三道闸·TinyWorld】反应链 `chain_depth` 深度上限丢弃 ＋ `max_age` 时效淘汰 ＋ 同 agent 最多合并 N 条去抖——显式化涟漪/级联防谣言风暴参数（接 J1 级联轮 / W1 稀疏化 / `$涟漪候选` 缓冲上限）
- [ ]  🟢【权力递归读取成本·规模·本轮补入】实际影响力 = Π(路径势力份额) 现算·深层嵌套×高频读取是否加缓存（P1 调平·接 Q/W 分级调平）
- [ ]  🟢【V2/N3】负面清单两行：持续性规则禁直写未来账 / 存档管理永远零动词

## 🌍 世界信息接口 · P0-7 定时效果 + 状态化（已定案）

- [ ]  🟡【世界信息·定时效果·已定案】定时效果（Sticky 持续 / Cooldown 冷却 / Delay 延迟激活）→ 走引擎**世界规则状态层**（复用延时种子成熟日 + 仲裁器冷却表·锚全局时刻·距上次先判哨兵0）——比无状态 WI 更强；定时效果状态（Sticky剩余/Cooldown剩余/Delay倒计时）进存档·随拍前快照回滚·随 fork 分叉·**进 U1 迁移面**——⚠ R8
- [ ]  🟡【世界信息·硬约束状态化·已定案】lore 的「逻辑/禁令」纯叙事约束则随知识注入让叙事遵守；**一旦要 gate 状态就升级为判定谓词 + 对应状态机**（衣物状态机/食物世界时钟谓词/口音角色语言风格位）·状态转移走五道闸落账——⚠ lore 双重性质 R10：纯知识载荷=规则型包(卸载即净) / gate 状态=实体型包(落档脱包律A)

### **🌐 动态提示词（Dynamic Prompt）· 云端策略热加载（已评估可行 · 2026-06-15 · 落点 P0-1/P0-6/P0-8/P0-11/P0-9）**

**结论：架构契合·可实现**。本质 = 远程来源、可热加载的「叙事皮肤/破限引子」包；解禁词/破限引子/assistant预填/NSFW强度本就属叙事层、已在 FINGERPRINT_EXCLUDED_FIELDS（B1d 分区·不进盐/不进指纹），故「云端拉最新策略」**不破确定性**（叙事层漂移不影响事实/指纹重放·复用 6.68/SV2）。explicit/light 自动启用、但须保留玩家可见开关（默认开·非静默强制）。**五条硬边界**：①作用域 schema 白名单——云端串只能填叙事条目·禁碰记账/检定/谜底校准/结算（discriminated union 已焊）；②玻璃箱——拉取后仍对玩家可见（explicit 解禁词不锁黑箱）；③重放按当拍冻结值——tick_log 定格所用云端策略包版本/内容哈希（同 N-1/N-2 路由快照），或接受事实-only 重放（SV2）；④只拉不推 + 验签（6.74）+ 纯声明式禁内嵌 JS（R6-a）——网络失败 fail-open 回落本地缓存/引擎内置（P0-8 回退链）；⑤旁路、绝不进 Ring 0 结算路径、绝不上传游戏状态（隐私）。

## P0-8 · prompt 组装（规格定稿前）

- [ ]  🔴【新排查·叙事漂移第②闸落 P0-8 规格·焊死级·接 6.68＋架构层三道闸②】对账闸规格明文：**记账 AI 必须输出提案单，且提案单须覆盖叙事中所有「有后果的意图」**——散文↔提案单做覆盖性校验（有后果名词/数额/动作逐项抽取比对），漏项=校验失败→定点重写一次→仍漏则降级+标记+玩家可仲裁；堵「叙事说『你拿了一百万』、提案单只记 +10 元」这类对账闸拦不住的语义漏记；与输出侧软拒检测正交（软拒查绕开意图标签=事后验收 / 覆盖性查记账漏抽=对账完整性） ⚠️ **纵切实证回填（slice M2·2026-06-15）**：拍 3 LLM 把叙事「两文铜钱」记成「50 文」，对账覆盖性闸**只认阿拉伯数字（如「2 文」）、漏抓汉字数字「两文」**而未触发漏项重写，全靠 H1 clamp 截到余额上限 30 才没穿帮——正是本行「一百万」式语义漏记的真实复现。**修复口径**：覆盖性抽取复用已入指纹的「中文数字解析规则版」做汉字金额识别（与下方〔中文数字解析版本化·对撞⑦〕同一份解析器·禁第二实现）；中文文游里汉字金额是常态非边角。✅ **M2.5 已补汉字数字识别（2026-06-15 23:52·1603 绿/30 files·chineseNumber.test.ts 61 + m25.test.ts 13）**：「两文铜钱」→covered:false/missing=[2]、阿拉伯数字不回归、解析器入指纹版本常量（顺手修正「一文不花」成语误抽 fixture）。✅ **M2.6 单位识别 + 未知单位 fail-closed 已补（2026-06-16 00:10 · 1631 绿/30 files · +28 tests · lint 净）**：slice 只认「文/文钱」为规范单位（CANONICAL_UNITS），其它单位（块/两/贯/吊/未知/解析不出）一律 fail-closed——「花了三百块」+提案 300 → covered:false / reason「单位不可确认」、绝不静默放行；两段正则（段①抢先锁定「文/文钱」位置防「两文铜钱」被段②误读、段②不得覆盖段①匹配位）；混合叙事（文+块）不可确认单位优先、整体 fail-closed；extractMoneyAmounts 改返回 MoneyAmount 数组（破坏性变更·全调用方已更新）；CHINESE_NUMBER_RULE_VERSION bump 到 2 · 指纹 PRESET_MUTATIONS 中文数字解析规则版=2 断言 + fingerprint 84 测试全绿；不在 slice 建货币换算系统。✅ **第三层（M2.7·语义/方向硬化）已完成（2026-06-16 01:04 · 1704 绿/31 files · +73 tests · lint 净）**：入口 NFKC 归一复用已钉版 S3 实现堵全角/空格/括号漏读（零宽字符正则改用 Unicode 转义码位集 U+200B/200C/200D/2060/FEFF，禁⑥ no-platform-normalize 不破）；方向（from/to）+ 性质（现金/债权/找零/代付）纳入覆盖比对（金额对但方向或性质错亦 fail-closed）；from/to 解析不出走 degraded、不默认主角。**对账闸三层全绿——M2.5 汉字数字 + M2.6 单位 fail-closed + M2.7 语义/方向，slice 叙事漂移第②闸的金额漏记面已闭合。** ⚠ 本轮 CC 报告仅列 lint/正则收尾与测试号增量（+73），未逐条回贴语义验收用例（找零方向/赊账债权/垫付代付/总价『共』四类）——已凭『全绿 + 73 新测试』采信，如需可向 CC 索取 itemized 验收清单。
- [ ]  🟠【slice 实证·赊账双分录平账·2026-06-16·二次修订】M5 手玩在 对话/给钱/赊账(检定)/还账/悔棋 间来回点后「帐对不平」——**两刀**：① 第一刀补镜像腿 `王掌柜应收`(资产·恒==debt)让赊账=「借应收／贷应付」同额、加 `assertTrialBalance()`(①应收==应付 ②Σ现金+应收−应付==230)四处自检——**仍不平**。② 真根因＝只补了「债权债务」这对腿、却漏了「价值」那对腿：赊账时林九没拿到任何等值(现金没动)、还账却照扣林九现金，于是「赊账→还账」整周期把钱白送给王掌柜(王掌柜漂到 200 文以上)；因现金总额始终被转账守恒在 230、`assertTrialBalance` 永不触发，缺口被掩盖。**修复（第二刀）**：赊账=王掌柜先把 CREDIT_AMOUNT 文等值现金垫给林九(守恒转账 王掌柜→林九·复用 TransferWorklist+assertConservation/assertNetZero)＋记债权债务对腿；还账原路转回并同额冲销 → 整周期后每账户精确复原(王掌柜回到 200)；状态栏加「应收」令平账可见(应收恒==欠账)·server.ts typecheck 净。**断言（加一个方向够不够）**：不够——只加「应收」一个方向只平了债权维度、没平价值维度；通则＝每笔经济业务要**两对腿**(债权债务对 ＋ 价值转移对)，纯现金账本里唯一能完全守恒的赊账模型＝「王掌柜垫资=放款」；要做「真·赊酒消费(林九净亏、王掌柜售货持平)」须引入 货物/费用 账户＝per-entity 完整六要素账本(M5 治本形态)。⚠ soak 仍不校验 debt/试算平衡 → debt 模型若再改，须把「应收==应付 ＋ Σ(现金+应收−应付)=230 ＋ 赊账还账整周期各账户复原」加进 soak 不变量(repro `--seed 12345 --runs 1`)。
- [ ]  🟠【slice 实证·赊账无清偿能力闸＋叙事/账本失配·2026-06-16·承上二次修订】M5 手玩至 第9天拍#54：林九把王掌柜现金赊光（王掌柜 0 文 / 林九 230 / 应收==欠账==200）后，**赊账(检定)仍可点、仍判「成功」、叙事仍撕借据并称林九「接过赊来的铜钱」，实际 0 文移动**（credit 被 clamp 到王掌柜余额 0）。守恒未破（总额 230、应收==应付）故 `assertTrialBalance` 不触发——又一例「被守恒掩盖的语义洞」。**两处缺陷**：① 缺**债主清偿能力闸**：王掌柜可放额=0 时，赊账动作应禁用/置灰 或 检定走「无力放款」诚实失败＋叙事拒绝（如「我这儿也没现钱了」），而非静默放 0 文＋叙事谎称借出；② **叙事/账本失配**（= 六之二A「账面对得上叙事」反例·方向＝叙事谎称发生了一笔实际没发生的转账）。**治本根因**：承二次修订——赊账被建模成「王掌柜垫现金给林九＝放款」，故王掌柜现钱必被掏空到 0；但客栈赊账语义＝林九**挂账消费**（货/服务出、应付增），王掌柜现钱本不该动，「可赊额度」被错误绑死在「王掌柜手上现钱」。**修复口径**：〔stopgap〕赊账前置债主清偿能力检查（可放额为 0→禁用/诚实失败/拒绝模板·绝不 0 文谎称成功）；〔治本〕引入货物/费用腿的 per-entity 六要素账本（赊酒＝林九费用+、应付+，王掌柜存货−、应收+，现金不动→「现金赊光」伪命题消失·与 P0-1 per-entity 行同源·M5 治本形态）。soak 补「债主无力放款时赊账不得产生借出/不得 0 文谎称成功」不变量。 ✅【soak 不变量落地·全绿·2026-06-16】CC 只动 soak.ts（未碰 server.ts／RING_K／数学库／canonicalize／rngFor）落三条不变量：①试算平衡（应收==应付·每拍兜底＋悔棋后单独校验）②现金／林九／债务三向同额方向敏感 ③清偿能力闸 fail-closed（现金 0 必借出 0·不得凭空生债）；结算公式逐行复用 server.ts handleAction（settleCredit／settleRepay·复用 getBalance／TransferWorklist／assertConservation·禁第二实现）；悔棋补 auxStack（与 ring 同容量淘汰·同 server.ts 设计）同步还原 debt／wangReceivable；王掌柜本金 200→13 逼出 zero-cap／partial-cap／full-credit 三分支（非空测·100 局各 119／129／227 次）；＋确定性子场景 runBorrowRepayScenario（seed 999·借 2 次→还清·三人现金逐位复原）。标准批 1000×20 全绿。✅ 已 commit＋push（基线 4e1e213·2026-06-16·69 files/+7952）——hosts/slice 全树首次入 git ＋ packages/core 多个先前未跟踪文件（knowledgeFilter/nsfwContent/replay/chaos/softReject/deterministicGuard 等）一并落盘；M5 纵切成果进版本控制，旧「无 commit 史被 server.ts 副本覆盖」灾难风险闭环。

<aside>
📋

**📐 对账闸已知边界 & backlog 清单（B/D 误区盘点 · 2026-06-16 · 未来 bug 排除）**

原则：闸只做「比对/确认」，认不出一律 fail-closed（降级不落账）；下列项当前最坏 = 误触发一次重写/降级（噪声），**绝不错账**。C 类（方向/性质/NFKC/from-to）已进 M2.7 指令、不在此列。

**B · 解析边界（归 P0-8 对账闸 · 复用同一份「中文数字解析规则版」解析器 · 禁第二实现 · 扩展时一并 bump 版本进指纹）**

- [ ]  🟢 大写数字（壹/贰/叁/拾/伍）+ 混合大小写 → 暂不解析、数值/单位不可确认即 fail-closed 降级
- [ ]  🟢 单位换算与刁钻组合（两/贯/吊/钱/分/厘/毫 ·「一两二钱三分四厘」「半两」「三分银子」「两吊五百文」「一文半」）→ 非「文」一律 fail-closed；**不建货币换算系统**（引擎自算 = fail-open 风险）；真要做须换算表入指纹、单列项目
- [ ]  🟢 约数/范围（三五文 / 三到五文 / 十来文 / 几文钱 / 两来文 / 数两银子 / 二三十两）→ 不精确金额不强制匹配、降级不落账
- [ ]  🟢 否定·假设·反问·比较（一文不花 / 不费一文 / 就算给十两 / 难道缺这二两 / 比昨天多花五文）+ 成语误抓（「一文不花」里「一文」被读成金额 1）→ 与提案比对不上即 fail-closed；未来加成语/语境 denylist 降噪
- [ ]  🟢 非货币量词与夸张比喻（三苹果 / 一斗米 / 一群人 / 花得像流水）→ 金额抽取强制「数字+货币单位」即天然不触发、安全
- [ ]  🟢 时代/方言单位（一块钱 / 毛 / 刀 / 铜板「枚」）→ 单位≠文即 fail-closed；铜板「枚」若要支持需登记「一枚=一文」兑换（默认仍 fail-closed）

**D · 不归对账闸 · 路由到对应阶段（与金额无关）**

- [ ]  🟡 时间口语/历法（前个儿 / 过两天 / 午时三刻 / 三更 / 腊月二十三 / 惊蛰 / 半晌）→ **P0-7 时间核**（接 #1 术语裁定 · 锚纪元分钟非墙钟 · 模糊口语时间归叙事层、不破确定性）
- [ ]  🟡 称谓/人名 → NPC 键（老张 / 小张 / 王爷爷 / 及时雨宋江别名）→ **P0-6 受治理键空间别名表** + **P1 知情过滤 M4**
- [ ]  🟢 量词认知与语法歧义（一头牛 vs 一条牛 / 连动句 / 被动无标记 / 的地得 / 差点没 V）→ 叙事 Agent 侧、非账务闸职责
- [ ]  🟡 主语/宾语省略·指代不清（「吃了。」「给了。」「他和张三…」）→ from/to 解析归 **M2.7**（解析不出 degraded · 不默认主角）
</aside>

- [ ]  🔴 P0-8：切片预算「预算管家」接 B1–B6 四级供给阶梯（知情过滤硬闸永先于预算）＋ 最大长度截断处理（结构化判废重试／叙事续写拼接）＋ 思维链输出契约剥离 ＋ 媒体条目接口占位
- [ ]  🔴 **信念派生管线主体 + 并发角色冲突仲裁**（P–R–B #1#2，Bbase=Σ(w·f)、Bfinal=Φ(Bbase+Σδ)，Φ=钳[0,1]）。这是 NPC 决策切片的派生源。防 bug：NPC 决策无个体信念依据 → 千人一面、行为与人设脱节。
- [ ]  🔴 **叙事语义校验闸 + 角色内纠偏回复**（PANGeA #1）作为具名调用类型，纠偏走回退链。防 bug：**直接收口你反复手补的 M4 家族**（旁路知情过滤 / 强制离场未生效 / NPC 无记忆）——现在每个都在单独打补丁，这道闸把"散文违反规则"统一拦+角色内纠正。
- [ ]  🔴 **LLM-Modulo 因果/动机校验器**（WhatELSE #2）：把引擎当外部 verifier，校验 LLM 散文的因果连贯+动机合理。防 bug：**收口 M5 家族**（NPC 前后矛盾 / 无动机行动）。⚠依赖项：动机校验要读 NPC 派生信念 → **本条依赖上面 P–R–B 派生先就位**。
- [ ]  🔴 **校验闸/因果校验器本身必须过知情过滤**（你的核心约束）。防 bug：这是最隐蔽的坑——因果校验器为了判合理性去读了**真值/秘密**，再把它写进 NPC 纠偏回复 → 真值泄漏。校验读真相只用于判定，喂回 NPC 的纠偏文本只能用认知投影。
- [ ]  🔴 X6 草稿态归属（swipe 候选不进 RootSchema/不落账/不进冷区）、X8 多源条目逐源渲染取源、X13 feed 采样 seed（拍号,刷新序号·不含墙钟）——三条独立行
- [ ]  🔴【输出侧软拒检测】挂 6.41 叙事分发输出侧机械校验扩维（查输出是否绕开事实包关键意图标签）→ 失败接定点重写一次→降级；与输入侧分级/lore 正交（事后验收 vs 事前预防）
- [ ]  🔴【AOHP option_id 稳定键·对撞①】AOHP `option_id` 必须是**稳定结构键（动词键+目标键+参数）·非位置序号**；菜单生成若需子集走 seeded rngFor·不用 LLM 测词决定选项集——否则重掏/悔棋/菜单重渲后玩家选的动作漂移·确定性重放破
- [ ]  🔴【叙事 CoT 内部化＋分发器剥离】思维链永不渲染给玩家、6.41 处剥离干净（原生 reasoning 或 tagged <thinking> 由分发器吃掉）——防真值/秘密泄露；选择性开（触秘/NSFW 红线）
- [ ]  🔴【slice 实证·自定义输入旁路知情过滤·2026-06-16】M4 手玩：玩家在自定义动作框输入「林九去后院找掌柜藏的人」→ 叙事直接揭示「王掌柜藏通缉犯」秘密、林九“找到了通缉犯”，但林九不在该秘密知情名单上。**非引擎硬谜底泄露**（状态栏/`$谜底` 未暴露、`filterSecretsForPOV` 仍在）——属**元层越权**：自定义 RP 输入未过知情过滤前置闸，玩家凭元知识驱动角色做“知道秘密才能做”的动作、叙事据此确认并 declassify。与 Y6 编辑楼层旁路 B/Z/M4、Y13 世界书旁路知情过滤**同族**。**修复口径**：① 自定义动作的叙事调用，喂 AI 的事实切片必先过知情过滤（按林九认知投影 + `filterSecretsForPOV`·与 M1 ∩窗同源）——秘密不进切片，AI 无从确认/揭示；② 对未知秘密的搜查/调查必须走「记账译动词→判定→declassify」（接 P0-7 偷看/调查进记账），不可由自由叙事直揭；③「仅推进叙事不动账目」档 ≠ 免知情过滤——叙事侧切片过滤先于账目豁免（账目可不动、知情过滤不可旁路）。 ✅【M4 守卫恢复并修紧·2026-06-16】world.ts 曾被 server.ts 副本误覆盖、事后重建（hosts/slice 从未入 git·无 commit 史可查），重建漏导出 SECRET_S1 且秘密母题文案改写（藏匿通缉犯→窝藏通缉旧友·谜底原文未变）致 m4.test.ts 5 红。CC 走 (B) 对齐：补回 SECRET_S1 导出（增量·零行为变化）＋把 9 处 not.toContain 旧文案换成真实文案——关键＝旧反向断言因文案过时形同虚设（测一个从不出现的串·恒过），换真实文案后才真正在测「秘密不泄露」＝把守卫修紧非修松；filterSecretsForPOV 过滤逻辑一字未动。pnpm test 1760 全绿/33 files·lint 净·soak 未动仍绿。⚠ slice 类型债（非护城河）：world.ts 重建后 buildWorld() 精简结构与 RootState 差 41 字段（balances 已剥离单独管理）致 assemble.ts/index.ts/server.ts TS2345——类型层噪音·vitest 不强制类型检查故不影响运行/测试/确定性·CC 已停手未扩大；P1「全生命周期 demo·复用纵切」开工前需先收这道类型债。
- [ ]  🔴 **【I篇】** 分层记忆（主上下文=内存 / 外部=磁盘）+ **确定性换页** + 内存压力警告触发召回淘汰。→ 治 M2「NPC 长对话失忆」与上下文超窗。
- [ ]  🔴 **【F篇】** 优雅降级（High/Medium/Base 保真度档）：LLM 实时 → 缓存 → 模板兜底，物理层在想象层降级时仍可玩。→ "LLM 非阻塞"的具体形态。
- [ ]  🟡 **【I篇】** 淘汰前先把要点写回 working context（**摘要固化**）再丢原文。→ 治切片淘汰时事实丢失。
- [ ]  🟡 **【H篇】** 共享记忆（协调用）vs 私有记忆（内部推理用）**严格分离**。→ "真相与认知分层"在记忆侧的落地。
- [ ]  🟡 **【M篇】** 导演层统一调度、演员只演；把"场景延续/是否生成新场景"做成显式判定函数。→ 编排逻辑从演员 LLM 抽离。
- [ ]  🟡 **【K篇】** 沉浸感(Immersion) vs 能动性(Agency) 是会打架的正交目标（强推 plot 破坏沉浸）。→ 显式权衡，别只优化一个。
- [ ]  🟢 **【M篇】** 按玩家发言生成"表演指导"（情绪/肢体/语气）作为可选切片，给单 NPC 回应注入一致性。
- [ ]  🟠【M1·6.50】NPC 心声输出端 ∩ 窗（主角已知 ∩ 该NPC已知；高危·免费照妖镜）
- [ ]  🟠【slice 实证·自定义输入旁路强制离场（在场名单/实体生命周期）·2026-06-16】M4 手玩：玩家自定义输入「林九把红姨绑走、逼她去别处成亲」→ 叙事照单执行、镜头里客栈只剩王掌柜，红姨被「强制离场」。与上「自定义输入旁路知情过滤」同族（自由叙事越权改权威状态），但触及的是**在场名单/实体生命周期**这一新维度。**先判属哪类**（看状态栏「在场」行 ＋ 下一拍红姨是否复现）：① **若引擎在场名单真把红姨移除** → 真·旁路写状态（🔴 升级）：自定义输入越权改在场/生命周期，可被利用作弊——绑走/赶走/「杀掉」证人以挫败知情过滤（如移除秘密持有人王掌柜）、绑走债主逃避收账（直接接今日赊账债主线）、缩小在场名单操纵组装面；② **若在场名单未变（红姨仍在场）、仅叙事谎称她走了** → 叙事/状态失配（同 13:52 谎称型·六之二A「账面对得上叙事」扩展到在场维度），下一拍红姨复现即暴露为「前后矛盾」（= 六之二C 测项）。**范围铁律**：`离场演化` 在 MVP 明确 OUT（二·角色 OUT 列）→ 在场名单应从 fixture 4.4 冻结、MVP 根本无合法离场通道，故任何由自由文本造成的事实性离场都**定义上出契约**；正解同「场景铁律」（把林九按在悦来客栈）——叙事可提红姨暂离，但在场名单不动。**契约洞**：输入框只承诺「仅推进叙事，不动账目」，**未承诺不动在场名单/知情面/生命周期** → 焊死前须把「自定义输入写权限边界」一次钉死：叙事通道纯只读于 账目 ∧ 在场名单 ∧ 实体生命周期 ∧ 知情面，在场/生命周期变更只能走受闸的结构化动作（且 MVP 离场 OUT＝冻结）。
- [ ]  🟠【AOHP 候选动作菜单预约束·TinyWorld】弱模型档「后端生成候选动作菜单 → LLM 只选编号＋写正文 → 后端校验结算」：`ActionOption{tool_name/params/value_slot/target_choices/min-max}` 后端拥有 tool/目标/参数·模型只选 option_id·**菜单＝白名单·菜单外即拒**·目标须在 `target_choices`·数值 clamp 到 min-max——**事前约束**（补对账闸事后校验·堵语义漏记）·自由正文留分隔符之外（真相层⊥派生层）·菜单可见性门控（只列看得到/认识的目标）对位供给阶梯
- [ ]  🟡 **"deny all" 兜底：图谱外/规则外的查询要拒答或升级，而不是即兴编造**。治 bug：NPC 一遇知识盲区/越界就幻觉（时代错置、编造事实）。校验器判"无法确定"时应让 NPC 表达"不知道/回避"，不得改写真值。
- [ ]  🟡 **越界分类法运用**（PANGeA #2）：校验闸引用 P0-1 枚举给违规分类，尤其中式历史背景的**时代错置**（查 P0-3 时间核）。防 bug：时代错置在你 lore 硬约束里目前没有对应校验步 → 穿帮台词漏网。
- [ ]  🟡 **校验闸面向本地小模型设计**（PANGeA #4，Llama-3 8B 28%→98%）。防 bug：校验若依赖 GPT-4 级能力 → 你"玩家自带 key/本地小模型"目标落空。
- [ ]  🟡【弱模型多级回退＋防注入净化·TinyWorld】结构化解析三套正则（标准 `[03]`/`ACTION:03`/裸号）＋中文数字解析 ＋ 内置防注入净化（剥离模型输出里的 `prompt end marker` 等噪声）——接软拒多级回退 ＋ IM4
- [ ]  🟡【派生草稿层＋记忆认知隔离·TinyWorld/Luker】`NarrationDraft`/`DiaryDraft` 带默认值＋多键归一＝派生草稿层；日记区分 `mentioned_known_names` vs `mentioned_visual_refs`——接姓名知识（P0-1）＋ per-seat 投影
- [ ]  🟡【slice 实证·NPC 跨拍记忆/情绪连续性缺口·2026-06-16】M5 手玩：林九上一拍把红姨绑走（自定义输入·承上「强制离场」），下一拍红姨回客栈后王掌柜/红姨对此毫无记忆与情绪反应；二人二次成亲等重大事件 NPC 亦无反应 → 前后矛盾（= 六之二C「前后矛盾」测项实锤）。**定性：叙事层/记忆子系统缺口，非护城河漏洞**——账本守恒（230）、知情过滤、在场名单均未破，属切片明确 OUT 的「NPC 记忆演化／离场演化」。**根因分层**：① prompt 只喂近 K 拍叙事切片（RING_K 环形缓冲）·重大事件一旦滑出窗口即不可见，且事件仅以叙事文本存在、未结构化标到具体 NPC 头上；② NPC 认知档案＝静态 fixture（mentioned_known_names／既往记忆种子），玩家行为从不回写进 NPC 记忆，每拍人设都从同一 fixture 重建＝「刚出厂的新副本」；③ world state 永不前进（buildWorld 单例·gameClock 仅派生），无持久世界状态记录「绑架/成亲」类世界事件；④ 无情绪/认知状态结算（＝模块6 认知档案/记忆 ＋ P0-7 认知结算：谄媚度→自我认知修正·案例4 / 认知误判死锁 未实装）；⑤ 叙事/状态失配（与上「强制离场」case② 同源；温度 0.9 让 LLM 自由编，但核心是「无记忆可喂」非温度）。最可能直接原因＝①＋②组合。**验证法**：对红姨做显著动作后打印下一拍真正发给 LLM 的 prompt 原文——prompt 里查无此事件＝①窗口截断；事件在但未挂红姨认知档案段下＝②无记忆回写；都在仍无反应＝温度/指令权重。**归宿**：蓝图 模块6 认知档案/记忆 ＋ P0-7 认知结算；切片内不修（OUT-of-slice·护城河未破），治本随认知结算/记忆累积管线（模块6 / M5 之后）。 ✅【M5 prompt-dump 实证·2026-06-16·根因坐实 + case② 全锤】玩家抓到拍3/拍4 真实请求体，根因比预估更粪：**每拍 system prompt = 静态模板 + 仅当拍动作、零历史**——(a) prompt 里无任何「近 K 拍叙事切片」（窗口实为 0、非 K 有限）；(b) 模板写死「描述主角刚到客栈的第一印象」每拍不变（world 永不前进的活证）；(c) 连「身上：0 文钱」都没读 live 账本（实际林九 30 文）、主角 称呼/背景/属性、NPC 描述全空——主角现金/NPC 状态/记忆/账本/事件统统未注入。故 LLM 全靠当拍动作裸生成、无任何 grounding，引擎账本/在场名单照常守住。**三组手玩实例全判 case②（在场名单全程恒三人、账本全程恒 230 未动·叙事谎称型，非 case① 真旁路写状态、非护城河漏洞）**：① 死亡复活（杀两 NPC→次日复活）；② 红姨被「杀」后仍能收 2 文（拍#10 给钱动作＝合法结构化动作·红姨 0→2·守恒 OK·叙事甚至自圆「杀了人还给跑腿钱」＝前后矛盾但账对）；③ 绑两人交山匪「领了赏金扬长而去、留下空荡客栈」（拍#3）——在场仍三人、且「领了赏金」林九账面未加（叙事谎称一笔没发生的转账·同 13:52 谎称型）。**直接修复（slice prompt 层·非确定性引擎）**：组装器须喂 ① 近 K 拍叙事历史 ② live 账本/在场/NPC 状态与记忆 ③ 去掉写死「刚到客栈」改按拍/世界态生成；治本仍归 模块6 记忆 + P0-7 认知结算（OUT-of-slice·护城河未破）。
- [ ]  🟠【DP·组装器旁路注入】explicit/light/community 档组装叙事调用时优先取云端缓存策略·拉不到回落引擎内置（覆盖回退链 P0-8）；命中策略包版本/内容哈希写 _系统.tick_log（重放定格·同 N-1）；记账/检定/谜底校准/结算永用引擎内置·永不读云端串
- [ ]  🟠【E1/E3·6.55】翻看媒介读取落账管线（covert 检定→先落账后呈现）+ 失真传闻分流（宿主视角条目、永不解析成文）
- [ ]  🟠【C5·6.53】跨席位读屏防护规格（per-seat 投影快照、$ 层物理不出进程）
- [ ]  🟠【第三批落地·3-6】知情分治切片组装（两窗分野：心声窄窗∩／媒介宽窗+读取落账）已写入蓝图
- [ ]  🟠【6.76 续·硬化批·本轮加入】G5 二审语言边界（维度标语言无关·语义维度对不可解析语言降「未评估」不误退）/ G9 二审降级绑「玩家是否在看这拍」（仅快播降级）；M1 路由保守偏转述 / M2 代写=补充不重复 / M6 二审尊重生效文风许可范围 / M7 抢话×视角耦合 / M9 视角随媒介出块恢复；P7 转述/代写注入前人称坐标变换（玩家「我」→主角实体键→按视角宿主转人称·X2/X9 含人称变换）；P8 嵌套媒介出块恢复用栈（限最大深度）；O2 小剧场知识范围=独立于内容尺度的正交维度（默认跟随防剧透层·严格模式限「仅主角已知」）/ O3 小剧场提示词组装走引擎组装器（Y6 无历史·X2/X9·递归限深1·绝不直读宿主楼层）/ O5 拒答超时直接提示重试；IM1 二审输出严格 schema+无写权沙箱 / PF2 二审超时 fail-open 放行但代写仍过机械校验+标记未质审——详见蓝图 6.76 续
- [ ]  🟠Z1 对账闸详规：提案单 Zod schema＋记账动词逐槽机械 diff
- [ ]  🟠Z2 方向敏感槽枚举：转账收支/缔约方角色/关系极性/主被动/秘密涉事方 五类必填槽
- [ ]  🟠【宏-A·源码核验】evaluateMacros 固定三段求值序（preEnv → env → postEnv·内容无双花括号即短路）登记为**占位解析槽求值序子契约**——接 P0-8 组装器·装配确定性前提（同脚本不同求值序=不同结果）
- [ ]  🟠【AOHP 菜单侧信道·对撞①】AOHP 菜单生成**过知情过滤**·且「不可选/灰选项的存在性」本身不得泄露未知状态（要么不列、要么列但不暴露原因）——同 M1 心声∩窗照妖镜·多人下菜单须 per-seat
- [ ]  🟡【**Z4 散文残跳口径三件兜底** — 只有「**让位序**」进了 P0-8 6.64 行(「四级供给阶梯+不可裁三件+让位序」);**「散文残跳口径三件兜底」+「数字类事实冲突时变量切片＞工作记忆演出摘要」无独立行**。
- [ ]  🟡【prompt 转换矩阵·Luker】一 IR → 多 provider shape（Claude/Gemini/Cohere/AI21/Mistral/XAI）＋ PROMPT_PROCESSING_TYPE（none/merge/strict/placeholders/single）＋ mergeMessages（同角色 squash·system→user 强制·占位补位）——接装配 IR ＋ 酒馆消息塑形（R5·已有消息塑形·补转换矩阵细则）
- [ ]  🟡【姓名知识=render-time 投影·对撞③】姓名知识是**渲染期投影 over 实体键**·不 baked 进 frozen prose；派生草稿层须在结构化附注保留实体键——否则 search-by-name 漏命·per-seat 名字知识不可分（回忆词可漂·实体不可漂）
- [ ]  🟡【中文数字解析版本化·对撞⑦】弱模型回退的中文数字解析规则**版本化进指纹**（三百/叁佰/3百→300 归一不确定=假分叉·数额是判定输入）——同 S3 归一/DSL 文法版随 U3 分段
- [ ]  🟡【NSFW 含蓄降级档】降级时备「含蓄淡出」过渡档（一句得体 fade-to-black）而非裸记账摘要
- [ ]  🟡【命名实体音译漂移·RAG 侧】RAG 分块挂实体键 metadata、召回＝实体键过滤＋向量（非裸名字串嵌入·防克瑞斯/Chris 漏召·接图扩散召回/中文 embedding 双坑）
- [ ]  🟡【B1–B6·6.64】组装器三拍板：组装确定性律（切片不进指纹）/ 四级供给阶梯+不可裁三件+让位序 / 双闸正交（次数闸×体积闸）——⚠ 工作记忆底座供源边界见审计 #14
- [ ]  🟡【M4·6.50】元指令判别默认拒向（解析失败降级纯 RP 零落账）
- [ ]  🟡【N1·6.51】跨周目记忆摘要有界性（禁链式拼接）
- [ ]  🟡【E2/E6·6.55】重生成读发布时刻 frozen 快照（一行不写必错）+ 书信双宿主在途态
- [ ]  🟡【6.67】注册表重试策略降出厂值 + 玩家覆盖层 `重试策略?`
- [ ]  🟡【AA9·五 已贴入·评估 E 归位】L2 蒸馏失败机械兜底（按重要等级截断式摘要·零 LLM·体积闸照常）——与 N1 有界性同点（同一组装器约束）
- [ ]  🟡【增审1·已拍板 ✅】采纳 = 显式固化标记（废 6.43「最高序号=采纳」）＋X7 拍板成立「离开即保存当前候选」；崩溃未固化草稿合法丢失、恢复按 E2 重生成（事实不漂措辞可漂）
- [ ]  🟡【Y8/Y11/Y12·二十一批】整流原子性（整段提交或整段作废＋⚠新世代）/ 世界书预算闸（token 上限＋确定性截断＋递归限深 1）/ 世界书入 6.41⑦ 注入面安检清单
- [ ]  🟡【三十·卡格式·已拍板 ✅】「开场白／序章」具名调用输入槽加「卡自带开场白素材」＋「时点矛盾→AI 按当前时点改写」分支（拒答/超时/畸形走回退链兜底登场）；占位解析挂组装层（`user`/`char`→实体键→渲染填名·复用 X2/X9）；**三十之二增补 ✅**：素材源扩列（固定文本/锚点引导两模式）＋NPC 两层适配检查（机械层＋L1 层）三出口（注入输出契约段/降级参考素材改写/跳过）＋已耗标记幂等（swipe 重渲不重耗·6.69 律一）＋主角不在场默认改写为主角视角/听闻
- [ ]  🟡【第三批落地·3-12】AA9 L2 蒸馏失败机械兜底（按重要度降序截断拼接·零 LLM·与 N1 同点）已写入蓝图
- [ ]  🟢 **self-reflection 自反思**（PANGeA #3，动态生成规则→是非判定→纠偏）作为校验闸内部机制。
- [ ]  🟢 **Plot Reviewer 角色动机模拟**（WhatELSE #4）作为因果校验器的子检查。
- [ ]  🟢【防注入净化边界·对撞⑦】防注入净化（剥 PROTOCOL_NOISE）只碰**结构化协议头**·不碰自由正文（玩家正文写“prompt end marker”字样不得被吞）
- [ ]  🟢【E5·6.55】两窗分野明文（元层窄窗 ∩ / 世界内宽窗+落账）
- [ ]  🟢【Y9/Y10·二十一批】渲染缓存先过机械校验后写 / 「已结算未呈现」扫描含该拍无采纳行 / 选项激活点 = 整流提交之后

### 🎚️🎬🤖 玩家主权 / NSFW 四档 / 破限引擎化 · P0-8 prompt 组装/回退链/模型路由 行（源块见文末三模块）

<aside>
🔒

**community-gate 暗坑已闭环（1867b2e · 2026-06-14 · 1298 tests 绿）**：原 `RootSchemaStrict` refine 只在测试跑、运行时 migrate 用无 refine 的 `RootSchema`，「community 才解锁覆盖」在真实游戏未生效。三层防御已落——① **组装器真地板**（`prompt/index.ts overrideAllowed()` 每次叙事调用 live 现算 `内容分级==='community' && 允许玩家覆盖===true`·systemPrompt/assistantPrefill 同门·不信缓存 flag·漏了也注入不进）；② **migrate 自愈**（`RootSchema.parse` 后 `RootSchemaStrict.safeParse`·违规 warn + 置 允许玩家覆盖=false·收紧授权方向·**绝不 throw** 防死档）；③ **P0-6 导入闸** `RootSchemaStrict.safeParse` 拒收（已导出·按既有行处理）。**项目3 UI 门**（非 community 禁勾选覆盖）= 体验便利非安全·归宿主/前端 **P1 设置面板**·安全已由①兜住·暂缺不阻塞。验收 `communityGate.test.ts` 10 条全绿（脏档自愈 + 强塞不注入）。

</aside>

- [ ]  🔴【世界信息·谓词切片注入·已定案】组装器按 lore 触发谓词/语义命中**确定性切片注入**（取代 ST 世界书关键词匹配·比关键词强）→ 条目多则走二·RAG 召回（中文 embedding+按字符分块）；纳入切片预算 6.64 + 知情过滤（知识本体只读·不含谜底）——⚠ lore 双轨 R7-b：**叙事注入可语义召回（不进指纹）/ gate 判定必确定性谓词命中（进指纹）**·两轨分清
- [ ]  🔴【lore 两层（三小层）读取口径·= M5 prompt-dump bug 修复方向·2026-06-18】组装器在此汇流三层喂叙事：**底层** `_lore知识库`（恒真config·不变）+ **表层-公共** 编年史(6.43·知情过滤后入册)+声誉/公共印象聚合 + **表层-投影** 当前 POV 认知档案投影（标「他以为」·主观知识受限）。**四边界**：①底层 config 指纹走包哈希 B1c·表层 game-state 随状态 replay·不进 preset 哈希（两套 treatment 别混）②表层入叙事必过当前 POV 知情过滤（认知档案 per-观察者主观·非公共·不裸喂防上帝视角）③真相(底层)vs 认知投影(表层)须标记④底层只装永不变常识·实体现状/世界变化归表层（违=Y13 双 track）·表层编年史须 L2 蒸馏+切片预算 6.64。详见蓝图 模均16/15 两层口径注（✅ 已落蓝图模块15 lore 区·2026-06-18·含「编年史换角/多人下按当前 POV 再过滤」补正）。
- [ ]  🔴【🎬NSFW四档·切模型玻璃箱·硬约束①②不变】本回合切模型须明示「因 X 切到模型 Y」(成本/封号可见·禁静默切)·只在玩家已配 key 的模型间切·禁路由无 key 模型·NSFW 降级目标模型须玩家预先配 key 标记 ⏳ 函数级已落（077f3c7·explicitReason 恒非空满足①·无 key→nsfw-disabled 自动降级满足②）·待 Ring 0 接线呈现玻璃箱文案＋UI 未配 key 禁用开关
- [ ]  🟠【🎬NSFW四档·场景检测器】「开·场景预判」触发信号=内容分级 explicit/community 档 ∧ 当前拍叙事意图/情境标签命中 NSFW（复用母题配额/NSFW 饱和度标签）·检测器命中判定写 tick_log 随快照版本化（回滚/重放后路由不漂移）·预判命中切 NSFW 模型后若仍失败保留失败重 roll 作二次安全网（在已切 NSFW 模型上重 roll·不切第三个模型）——⏳ **待接线**：场景检测器（内容分级∈{explicit,community} ∧ 情境标签命中）实现在叙事管线前置（077f3c7 已留 scenePredictedNsfw 入参）✅ 场景检测器 `isNsfwScene(state,intentTags)` 本轮已实装（1356·仅 explicit/community∧标签命中触发·可注入测试）·命中判定经 `assembleTickRoute` 写 tick_log.路由快照随档版本化；剩「预切后仍失败在已切模型重 roll 二次安全网」待 P0-7 retry 接线
- [ ]  🟠【🎬NSFW四档·模型路由×确定性·硬约束③不变】三态开关本身入指纹排除名单（偏好层·拨动不进盐）·本拍实际路由模型随档快照（或进排除名单+「逐字不可复现但语义保真」复用 6.68）·不可中途漂移制造假分叉 ✅ 三态开关＋目标模型键已入 FINGERPRINT_EXCLUDED_FIELDS（077f3c7·Gate B 穷举 78 property）·routedVia+modelKey 供 tick_log 随档（⏳待 Ring 0 写入）
- [ ]  🟠【🤖破限引擎化】模型族→引子自动选择：组装器按当前模型族(Claude/Gemini/GLM)自动挑破限引子＋决定注入角色(Claude=system 注入+assistant 预填·Gemini/哈基米=assistant 预填为主)·只换引子注入策略·不主动切模型
- [ ]  🟠【酒馆·RAG召回过滤·真缺口】RAG 召回**只喂叙事侧、绝不喂记账/判定输入**（防回声室 6.43④）+ 召回内容过知情过滤（按宿主认知投影·E 批两窗分野）——⚠ 召回知情过滤口径见对撞报告 R2
- [ ]  🟡【🎚️玩家主权】覆盖注入点(叙事专属)：组装器仅在叙事调用且专家模式开时用覆盖串替换/拼接默认 SystemPrompt·记账/检定/谜底校准/结算永用引擎内置·覆盖串不进 6.41 事实包/意图速记·不碰落账语义
- [ ]  🟡【🎚️玩家主权】覆盖回退链：覆盖致频繁乱码/拒答仍走既有降级回退(系统文本兜底/重试·复用 6.67＋输出侧软拒)·专家模式不豁免回退·连续失败可自动暂停提示
- [ ]  🟡【🎬NSFW四档】解禁词玻璃箱：explicit 引擎解禁词对玩家可见·community 可编辑·不锁黑箱
- [ ]  🟡【🎬NSFW四档】分档注入(叙事专属)：组装器按 `内容分级` 选叙事提示词强度 off安全/light轻度成人/explicit成人小说家框架+解禁词/community 同 explicit+玩家覆盖串·四档全仅注入叙事调用
- [ ]  🟡【🎬NSFW四档·正交性】NSFW降级模型开关 ⊥ 内容分级（后者控提示词强度·前者控是否/何时切模型）·explicit 不再硬编码「不主动切模型」改由本开关决定·仅作用于叙事调用（记账/检定/谜底校准/结算永不切模型）
- [ ]  🟡【6.69·渲染模式三分·P0-8 单列】调用类型注册表条目 `渲染模式?` 行为落地三分：主叙事=直读流 / 特定格式·生成媒体=占位整达（超时封顶走 6.67 超时秒数旋钮）/ 记账·判定·谜底校准·离场演化声明=静默后台不渲染楼层——字段已随 6.75 批预埋（4.8/4.9），此处补行为行
- [ ]  🟡【🤖破限引擎化】assistant 预填注入点(叙事专属)：组装器仅叙事调用拼接 `assistant预填?`(伪 assistant 消息/continue prefill)·核心调用永不带·不进事实包/意图速记·不碰落账语义
- [ ]  🟡【酒馆·结构化输出】函数调用/工具 + 结构化输出（输出 schema 两条落地手段）：adapter 按 provider 择优·均不暴露玩家；硬约束=仅聊天补全源支持（文本补全走 OpenAI 兼容）走能力声明降级链·不做服务端 schema 校验失败返空对象 → 必须自带解析+校验+修复（并入五道闸/对账闸）·关键结构化产出（记账）走强制 schema 而非自发 tool call
- [ ]  🟡【酒馆·朗读范围】朗读范围过滤走结构化输出已知段：确定性「只读 dialogue 段」不靠星号/引号正则猜
- [ ]  🟡【酒馆·前缀缓存】上下文前缀稳定性优化（吃满后端 KV/前缀缓存省成本）：摘要/状态置顶·新条目仅尾部追加·n+m 分段窗口生长——归切片预算 6.64·可选不改架构
- [x]  🔴【🎬NSFW四档·切NSFW模型=玩家三态开关·改版2026-06-15·原「引擎内部一档·非用户开关」口径作废】`$玩家偏好.NSFW降级模型` 三态——**关**：永不切模型·叙事生成失败只走 6.67 重 roll 叙事（同模型·重渲不重判·账本冻结·连续失败 N 次自动暂停提示）·绝不切回退/NSFW 模型；**开·场景预判**：叙事调用前 NSFW 场景检测器命中即预切 NSFW 模型·预切后若仍生成失败则在已切的 NSFW 模型上重 roll 叙事作二次安全网（不再切第三个模型）；**开·失败兜底（默认·开 NSFW 即为求稳）**：默认模型先跑·仅软拒/拒答检测命中才切 NSFW 模型重试 ⏳ `selectNarrativeModel(state,{softRejectDetected,scenePredictedNsfw})→ModelRouteDecision{modelKey,routedVia,explicitReason}` 已落（077f3c7·24 tests·关→default/null·失败兜底软拒→nsfw-fallback·场景预判→nsfw-preempt·无 key→nsfw-disabled/null）；✅ **本轮接线完成（1356 绿·+17 tests）**：`assembleTickRoute(state,intentTags,sceneDetector?)` 首次按 `_tick.id` 定格→写 `_系统.tick_log.路由快照{routedVia,modelKey,explicitReason}`（N-1）·`replayRoute(entry)` 读冻结值忽略 live state（N-2）；剩 UI 未配 key 禁用开关 → P1 设置面板
- [x]  🔴【N-1·一拍路由定格·✅本轮 1356 绿·+17 tests】模型路由本拍首次组装即定格写 tick_log：`assembleTickRoute` 首次按 `_tick.id` 调一次场景检测器→`selectNarrativeModel`→写 `_系统.tick_log.路由快照{routedVia,modelKey,explicitReason}`·本拍内 swipe/重 roll 复用同一冻结路由·不重跑检测器（spy 计数=1·3-swipe 逐字节恒等断言绿）
- [x]  🔴【N-2·live 偏好⊥冻结路由·✅本轮 1356 绿】NSFW 三态开关=live 偏好 ⊥ tick_log 历史冻结路由·`replayRoute(entry)` 读冻结路由快照全忽略 live state·改开关只影响新拍不回写历史拍（关→开切换后重放旧拍不漂移断言绿·pre-N-1 entry 返 null）
- [x]  🟠【N-3·key 运行时失效·✅1415·selectNarrativeModel 加可选 keyChecker·切前 live 校验（静态画像检查后/路由前）·失效降关态+玻璃箱「key失效已回落」+key 名·关态/缺 key 短路不调·nsfwKeyValidity 11 tests】已配 key 运行时失效（撤销/过期/额度耗尽）→ 切前 live 校验·失效则降级为「关态」行为（永不切·只 6.67 重 roll 叙事）+ 玻璃箱「key 失效已回落」文案·禁静默路由到死 key
- [x]  🟠【N-4·软拒检测确定性·✅1415·SOFT_REJECT_RULE_VERSION=1+关键短语表+detectSoftReject 确定性无 LLM（关键词优先→REFUSAL_OPENER 长度组合启发式）·软拒检测规则版本入 FINGERPRINT_PRESET_FIELDS+hashPresetFingerprint 签名·softReject 24 tests】软拒/拒答检测器假阳假阴 → 检测走确定性规则（关键短语表＋长度/结构启发式·不引 LLM）·规则版本进指纹取材集随 U3 版本分段——与 P0-8 输出侧软拒检测同源·复用同一规则表

## P0-9 · 存档层（写进任务规格 · 零返工窗）

- [ ]  🔴【G2·6.49】fork/继承/裸SL 多步写入单事务原子性（死档级）
- [ ]  🔴 P0-9：U1 迁移单元＝全树（迁移面清单显式列：时间线分块／里程碑快照／封存域块／环形缓冲／观测值表／挂起队列／tick_log／归并表·谓词·补丁集运行实例／**叙事流冷区·编年史分块（互审Bug1）**／已结算标记（G-3））＋ 仅关账态入口门规 ＋ 整树单事务 WAL（G2）＋ 存档头双写（G2b）
- [ ]  🔴 **U1 迁移单元 = 全树**（推倒重来级）→ P0-9：迁移面清单（含互审补上的叙事流冷区·编年史分块）＋仅关账态入口门规＋整树单事务 WAL（G2）＋存档头双写（G2b）
- [ ]  🔴 P0-9／P0-2：U2 禁重模拟（迁移＝确定性纯数据映射·确定性禁全套·双机逐字节恒等·严禁「重放事件流重建快照」代替映射）⚠【B-b 审计补·2026-06-18】迁移戳.墙钟时间=ISO 串纯展示 → 双机逐字节恒等(U2/U7)比较须排除墙钟时间，或墙钟由 host 注入·禁 core [Date.now](http://Date.now)()；否则双机迁移时间戳不同必假报不恒等。盖戳逻辑（迁移戳 append + migration_version+1）属本行 P0-2 运行侧、B-b 只交 schema 地基
- [ ]  🔴 P0-9：U4 结算线分界（已结算归历史只迁形状·未结算懒账 v2 接管零标记·fork 回 v1 里程碑续写为 v2 段）
- [ ]  🔴【拍前快照机制】非每拍全量字节快照：ring buffer（近 K 拍回滚锚）＋ delta ＋ 快照树 checkpoint，否则长跑写放大爆炸（接 G3 锚点 pin / 快照稀疏化 / J3 观测值表回滚）
- [ ]  🔴【反向核查·G-2】U5 冻结载荷迁移覆盖清单显式补「占位模板快照(NPC/组织/地点·K4)」——漏迁=包卸载兜底重建失败
- [ ]  🔴 **【I篇】** archival / recall 分层存储是好范式，**但"谁触发换页"必须改成确定性内核**——别让 LLM 自主写存储，否则破坏单写者铁律与真相层。
- [ ]  🟠【G2b·6.49】存档头双写 + 校验和（全档唯一不可降级单点）
- [ ]  🟠【D3/D5·6.54】封存域版本化扩**所有域**（fork 时全域 copy-on-write）+ 域钟读数随块版本化（绝不游离快照外）
- [ ]  🟠【6.76 续·硬化批·本轮加入】SV1 叙事控制设置只随时间前进不随悔棋回退·历史呈现 frozen 冻结；SV2 悔棋/读档=事实精确·呈现尽力（重放逐位一致只承诺事实/指纹不承诺呈现文本）；SV3 历史拍呈现存 frozen 文本快照·禁懒渲染依赖活文风库（迁移删键不炸）；SV4 重生成=事实读 frozen+呈现用当前设置；SV5 设置随 fork 复制不共享·新玩家重置默认；SV6 小剧场收藏账户级全局·状态树之外·不随 fork/悔棋；SV7 观战所见 meta 入口接 6.45 严格模式与裸SL 预知同等；P9 小剧场 pin 舞台快照（snapshot-on-instantiate）；L4 观战连播快照稀疏化·悔棋以观战段为单元粗回滚——详见蓝图 6.76 续
- [ ]  🟠【git UX 映射 SL 三档·对撞⑤b】git 式 undo/redo + 保存点 UX 须**映射到正规 SL 三档**（纠错免标/玩法回溯天命锡拍号/裸 SL 作弊标记）·禁开第二条不留谱系旁路；delete-tag 丢弃须**只标记非真删**（与单树谱系 6.47 对齐·redo 才有物可恢）——同 Y2 裸 SL 旁路
- [ ]  🟠【反代 baseURL 导出剥离·对撞②】反代 `baseURL` 随导出/分享/封存礼包**剥离或导入时显式确认**（非只剥 key）——防玩家 prompt 被静默发往陆生人 endpoint·[localhost/私网](http://localhost/私网) endpoint 进别人手里为死端/SSRF 面
- [ ]  🟡 **【Matrix-Game篇 + F篇】** 坚持"派生/哈希 + 关键内容落盘"，而不是让模型每次重新"想象"世界（Matrix-Game 连重访一致都做不到）。
- [ ]  🟡 U4 结算线分界：已结算归历史/未结算归未来、fork 回旧里程碑长 v2 新枝
- [ ]  🟡【git 式叙事分叉/分支撤销重做 UX·WorldLines/novel-master】event-sourced＋file-backed＋git-diffable 分支/撤销/重做呈现 ＋ 当前 tag 解析＋master 默认回退 ＋ `add-tag --copy-from-current`（COW 分支拷贝）＋实验 tag＋`delete-tag` 丢弃——接 fork/周目谱系单树（6.47·一套 parent 链遍历）·前端 UX（P0-9 后）
- [ ]  🟡 X10 短信线程纳入叙事流冷区 / X14 积压折叠阈值（对接 6.43 三层金字塔·仅指向性通信逐条保留）→ P0-9
- [ ]  🟡【integrity 乐观并发·Luker/命-B⁵】chat 写入 integrity slug 乐观并发（client 带 integrity·server 不符拒写防丢更新）＋ patch-first（write-file-atomic 原子写 ＋ fast-json-patch RFC6902 增量 ＋ throttle 备份）——坐实命-B⁵（cursor offset+size+modified_millis 比同步层强）·接 P0-9 拍前快照 ring buffer＋delta（已有·补具体协议）
- [ ]  🟡【DP·版本定格重放】tick_log 随拍快照记录本拍所用云端策略包版本/内容哈希（同 N-1/N-2 路由快照机制）·replayRoute 同源读冻结值忽略 live 云端内容；或接受「事实/指纹逐位恒等·呈现文本尽力」（SV2 已承诺）
- [ ]  🟡【跨周目快照膨胀＋玩家精细管理】里程碑快照加 珍藏? 标记（零动词·豁免稀疏化/GC）＋ 手动删噪声碑 ＋ 珍藏保真/摘要档计入体积预算 ＋ 软上限提示（接体积五闸/$预算控制台）
- [ ]  🟡【J3·6.61】上次观测值表 + 挂起命中队列随拍前快照回滚、随线分叉——⚠ 回滚单元须含两者（缺挂起命中队列=跨拍边沿判定漂移·接 P0-7 边沿读取/§九第9类重放输入）
- [ ]  🟡【K3/K4·6.52】实例化瞬间快照定型（snapshot-on-instantiate）+ 墓碑存根落档
- [ ]  🟡【G4/G6/G9c·6.49】封存域 copy-on-write / 裸SL 代际水印 + 回想过滤 / 裸SL 执行序（载入→盖章→移交）
- [ ]  🟡【缺口8】快照稀疏化策略（近 N 拍+里程碑）+ 存档头全局回滚计数器
- [ ]  🟡【N1·6.51】继承规格条数/字数硬上限（与 P0-8 同条双落点）
- [ ]  🟡【Y3·二十一批】导出/分享/封存礼包套 U1 同款「仅关账态」门规、由持锁写者执行——堵只读标签导出半拍中间态
- [ ]  🟡【增审2·已拍板 ✅】读档作弊线 =「谁的记忆」：检查点/旧档默认干净 fork 免标记；角色记忆注入按预设开关（重生流 = 正规通道 / 未开 = 作弊章）；玩家脑内记忆不设防；酒馆检查点载入按此映射（计数器 +1、谱系档位二）
- [ ]  🟢 **派生信念确认不入档**（P–R–B，派生量）；**校验/纠偏产物随 frozen 呈现**（SV 系列，"事实不漂、措辞可漂"）。防 bug：重生成历史拍时纠偏改了既成事实 → 存档事实漂移。
- [ ]  🟢【保存点系统 UX·CradleAI】随时恢复任意历史状态的玩家面板——接 SV2 悔棋＋拍前快照＋fork（前端兑现·机制已在）
- [ ]  🟢【U1×AA1·六·三十二 已贴入】U1 迁移入口门规补一行：「迁移前在途 Ring 2 调用全体按 AA1 过期丢弃」（U 批拍于二十五、AA1 拍于二十九·互审未盖到区·进 AA-合并会第八场景）
- [ ]  🟢【secrets 不入备份·源码核验+ST1.17】再坐实 R5-a 配置⊥密钥物理分离·导出剥离（ST 上线后才补此洞·印证应从一开始设计对·机-C/机-E 同哲学）
- [ ]  🟢【F7·6.56】恢复序一行：载档后重扫「已结算未呈现」补弹
- ✅🟠【第三批落地·2-1 Y7】账本/已结算标记住不随楼层回滚的全局变量域、楼层只放展示镜像 已写入蓝图
- ✅🟡【第三批落地·2-1 Y3】导出/封存礼包套「仅关账态」门规·持锁写者执行 已写入蓝图
- ⏳🟡【反向核查·S-3】赛事实例 模板快照 纳入冻结载荷族迁移覆盖(同 K4/U5)

## P0-10 · 回归测试体系

- [ ]  🔴 U7 基准重录成本治理 → P0-10：v1 fixture 永久保留＋迁移 CI 三件（六不变量全过 / 双机迁移恒等 / 迁移后 fork 回 v1 里程碑续跑不死档）＋semver 拒载更新版本档（K2 同机制）
- [ ]  🔴 **【H篇 + I篇 + M篇】** 加认知层 / 分层记忆 / 多 agent **不会自动变好**，效果强依赖底座模型能力，弱模型反而被拖垮（H 篇 Llama 3.1 8B、I 篇弱模型函数调用差）。→ 把这些重型模块列为**可裁剪、默认关、按需开**，上线前用回归测试确认底座扛得住。
- [ ]  🔴 **player-proxy 玩家代理作为测试驱动**（WhatELSE #3，正/负/角色三型）。防 bug：校验闸/叙事空间没有自动化对抗测试 → 只能人肉试。三型代理正好做叙事的"对抗性语料生成器"。
- [ ]  🔴 **对抗性越界语料 fixture 族**（PANGeA 分类法 × C1 先例）：时代错置/物理违规/越权技能/提示泄漏/未来视各若干条，跑**真模型**测校验闸拦截率，做软门。防 bug：和你 C1 对账闸语料同理——校验闸真实强度只能测不能想。
- [ ]  🔴 **【M篇】** 多 agent 串行调用延迟叠加会爆（其头号自陈局限）。→ 多角色编排必须并行 + 非阻塞 + 可降级。
- [ ]  🔴 **【J篇】** 硬强制合规延迟巨大。→ 这类校验放进非阻塞结算路径，绝不卡主循环。
- [ ]  🟡 **【H篇】** 符号求解（Clingo）有计算成本。→ 轻量化或塞进非阻塞路径。
- [ ]  🟡 **信念派生数值表 fixture**（P–R–B）：给定 facet+角色断言 Bbase/Bfinal（照检定公式数值表先例）。防 bug：派生公式回归无基准 → 改权重悄悄改了所有 NPC 信念无人察觉。
- [ ]  🟡【W5·6.65】五件规模基准 fixture（500 NPC 装配/百年快进/名人涟漪/万触发扫描/优化等价双跑）+ 键控三处锁定——**本批核心交付，DoD 后改=重录全部基准**
- [ ]  🟡  C1 对抗性记账语料族 fixture(真模型语料压对账闸·实测 6.68 拦截率)
- [ ]  🟡  事务覆盖面枚举表 + RNG 三盐通道枚举表(普通混盐/天命锚拍/区间锚不混盐·6.66/6.45)
- [ ]  🟡   断电撕裂恢复用例(开局 commit 套 G2 + 撕裂点 fixture·恢复后状态一致断言)
- [ ]  🟡【H5·6.57】跨平台双机重放用例
- [ ]  🟡「全 LLM 失败下出生→死亡」独立 fixture
- [ ]  🟡【W2·6.65】性能结构确定性等价双跑断言（覆盖到期索引/脏标记/级联轮）
- [ ]  🟡 **"采访式"可信度评测 + 消融对照**（正篇按 自我认知/记忆/规划/反应/反思 五类各 5 问，消融掉记忆/反思/规划做 TrueSkill 排名，证明每个组件都关键）。治 bug：你蒸馏/派生/校验改了之后无可信度回归基准。可做成你 NPC 的"五类采访 fixture"，消融某层断言可信度下降。
- [ ]  🟡 **四个量化指标可直接拿来当 NPC/叙事回归基准**：Consistency（与既述信息不冲突的比例）、Coherence、**Item Status（必现物品出现率）**、Complex Question（复杂问答正确率）。治 bug：你蒸馏/校验改动后没有可信度回归断言。
- [ ]  🟢 **行为 vs 态度差距**（1000人篇：态度 85%、Big Five 80%，但经济博弈仅 66%）。治 bug：别高估 NPC"行动派生"的准确度——陈述信念≠真实抉择，校验器对动机类要留容差。
- [ ]  🟡【最小重放断言·时间窗分层·第四项核验】三件不在同一时间窗：断言① = P0-5 gate / 断言② = P0-7 后 gate / 断言③ = AA-合并会交付物——「P0-6 开工前三件全绿」不成立（②③ 依赖件未就位）；公共地基 = 重放输入集完整（指纹取材＋意图序列＋盐值＋失败工单＋调用世代号），与「AA-合并会提前到 P0-6 开工前」方向一致（提前开会同时解锁②③ 测试台）
- [ ]  🟡【AA3·四 已贴入·DoD 基准面裁定】半天会过旧卡行为清单打「保留/豁免（标第几轮改）」标签、豁免清单存 docs/spec；P0-10 分两组判——**P0-10 动工前必做**
- [ ]  🟡【AA-合并会·四五 已贴入】重放器立项＋AA1 异步时序对撞合并一场：①输入集全清单 ②**八场景轰炸**（悔棋迟到/fork 在途/快进乱序/关账掉落/重试双落账/跨版本段×难度段·AA8/双层世代·AA10/迁移×在途·三十二场景＋）③重放器派正式编号＋owner——**已决定提前到 P0-6 开工前开（玩家拍板 ✅·解锁断言②③ 测试台 + C2 harness）**
- [ ]  🟡【第三批落地·3-2】级联轮 vs 脏标记 优化前后双跑断言 已写入蓝图
- [ ]  🟡【第三批落地·3-3】DSL fuzzer 跨机确定性用例 已写入蓝图
- [x]  🟡【审计 #D·✅REPLAY-01 已立项（owner=蔡怡家佳·编号 REPLAY-01）+本轮最小实现落地（replay/·前三场景绿）】**重放器立项**（清单未列，本页补：盐值/指纹/照账重演的承诺需要一个有编号的 owner，建议挂本任务）
- [x]  🟡【断言③·第四项核验·✅本轮 REPLAY-01 交付·replay.test.ts 悔棋+旧任务迟到场景读冻结记录值（盐值/路由）·live 偏好不漂移·1415 绿】盐值从 tick_log 记录值读取重放恒等（不读现头计数器）——**预设重放器已存在**，故 = AA-合并会重放器最小实现的交付物（三件里唯一卡在「测试台还没有」）；依赖 tick_log `盐值?`（P0-1·已排）＋盐值登记（P0-7）
- [ ]  🟡P0-10：U7 基准重录治理（v1 fixture 永久保留 ＋ 迁移CI三件：六不变量全过／双机迁移恒等／迁移后 fork 回 v1 里程碑续跑不死档；semver 拒载更新版本档）

## P0-11 · 双宿主薄壳

> 宿主边界对撞已完成（第二十一批 Y1–Y13 · 详规见补漏清单第二十八部分），本段为其主战场归位。
> 
- [ ]  🔴【Y1·二十一批】跨标签单写者选举：Web Locks 写锁＋租约/心跳；第二标签只读观战＋显式「接管」按钮（死档级——F2 锁为进程内、G2 不管双活写者）
- [ ]  🔴 MVU 绑定规格三件（6.69）→ 宿主状态层（随 P0-11 适配）：硬数值白名单 = 引擎独占写入＋更新前校验夹层＋幂等血统账本
- [ ]  🔴【Y2·二十一批】接管协议三步（夺锁 → 弃内存态 → 从库重载）＋假死标签解冻无条件只读化永不重抢——缺②③ = 不盖章不留谱系的裸 SL 旁路
- [ ]  🔴 P0-11：宿主 adapter 把 采样参数／max_tokens／stream／思维链／媒体 编码透传真实 API（AA2 扩展
- [ ]  🟠【Y6·二十一批】酒馆「无历史模式」：上下文唯一来源 = 引擎组装器、宿主楼层仅展示（本批最毒——否则编辑楼层 = 直接注入，B/Z/M4 防线整条旁路）
- [ ]  🟠【Y7·二十一批】账本/已结算标记住不随楼层回滚的全局变量域，楼层域只放展示镜像——堵 MVU 楼层级快照随删楼自动回滚 desync
- [ ]  🟠【Y4·二十一批】世代号机制：swipe/重试 +1、过期宿主事件丢弃、固化只认当前世代——堵 swipe 连点风暴乱序抄录
- [ ]  🟠【DP·网络旁路 fail-open】策略包 fetch 走宿主 adapter 旁路（同 API key 旁路·绝不进结算临界区）+ 本地缓存 + 非阻塞 fail-open（断网回落缓存/内置）；**pull-only·绝不上传游戏状态/玩家内容**（隐私·导出同剥离）
- [ ]  🟡【Y5·二十一批】楼层编辑不回写：AI 行渲染提交一次性冻结，此后编辑事件忽略（仅宿主展示层有效）
- [ ]  🟡 P1：设置面板模型旋钮 UI（温度／Top P／惩罚／最大长度／流式／思维链／上下文上限滑块）接 `$预算控制台` 覆盖层
- [ ]  🟡【AA2·四 已贴入】preset 只承载模型/采样参数与调用入口，主叙事正文上下文仍由引擎组装器以 Y6 无历史模式注入、关死 preset 自动附带历史
- [ ]  🟢 **派生信念 + 校验闸在双宿主结果一致**纳入现有双宿主一致性测试（10 拍 diff=0）。防 bug：校验闸在酒馆/web-debug 行为分叉。
- [ ]  🟢【AA10/AA12·五 已贴入】Y4 旁注两层世代正交（互不回填）；Y6 无历史模式世界书扫描源钉死 = 引擎组装器当前切片、不扫宿主楼层（递归限深 1）
- [ ]  🟢【三十·卡格式·已拍板 ✅】酒馆 `user` 宏 adapter 映射到主角实体键（入口在宿主、解析在引擎统一发生·双宿主逐字节一致）
- ✅🔴【第三批落地·2-1 Y1】跨标签单写者选举（Web Locks 写锁+租约/心跳·第二标签只读观战+接管按钮）已写入蓝图
- ✅🔴【第三批落地·2-1 Y2】接管协议三步（夺锁→弃内存态→从库重载）+假死只读化 已写入蓝图
- ✅🟠【第三批落地·2-1 Y6】酒馆无历史模式（上下文唯一来源＝引擎组装器）已写入蓝图
- ✅🟠【第三批落地·2-1 Y4】世代号（swipe/重试+1·过期丢弃·与引擎层世代 AA1 两层正交）已写入蓝图
- ✅🟡【第三批落地·2-1 Y5/Y8/Y9/Y10】楼层编辑不回写/整流原子性/缓存先校验后写/恢复扫描 已写入蓝图
- ✅🟡【第三批落地·2-1 Y11/Y12+AA12】世界书预算闸+注入安检+扫描源钉死引擎切片 已写入蓝图

## 🍺 P0-11 · 酒馆功能双宿主 adapter（API 连接收口）

- [ ]  🟠【酒馆·消息塑形】Prompt 后处理（消息塑形策略）：adapter 请求塑形步——无/合并同角色/半严格(强制角色交替)/严格(单系统消息+user优先)/单 user 消息·可剥离 tool calls·开放枚举·住 adapter 唯一碰 API 层·不进指纹（R5 明文）
- [ ]  🟠【酒馆·API key 存储·真缺口】端点可配置(CORS)+自带 key 浏览器直连 → **API key 存存档外机密区·绝不进存档/`$`层/导出**（导出剥离）——⚠ 对撞报告 R5 真缺口
- [ ]  🟡【酒馆·端点 adapter】OpenAI 兼容端点可配置(一项覆盖 OpenRouter/DeepSeek/Mistral/本地 Ooba/自定义网关) + Claude/Gemini 专用 adapter + 聊天补全/文本补全双模式 adapter
- [ ]  🟡【酒馆·连接预设】连接配置文件（玩家级命名连接预设·保存/一键切换〔端点+key+模型+采样默认〕）——玩家便利层
- [ ]  🟡【双 ZIP 判型·SillyDroid/TauriTavern】导入闸 L0 后判型「两桶」分法（官方用户备份 ZIP=真相子集 vs 宿主全量数据 ZIP·include_secrets 默认关）＋ TauriTavern Rust 源码坐实（全量数据归档 ⊥ 用户备份归档·导入＝全树替换＋重建派生）——接 U6 外置态 / U1 全树迁移两桶（互审 Bug2 现实样本）
- [ ]  🟢【后端拥有生成态·Luker】GenerationJob 断线安全持久化 ＋ ack 不静默丢弃 ＋ active-output 恢复——坐实 Y1/Y2 接管协议 ＋ U6 宿主侧外置态（工程样本·对位 TauriTavern data_archive_jobs 状态机）

## 第二项核验 grep 实点（已修蓝图·文档同步待完）

- [x]  🔴 命中一/二/三/四 + 审计 #2/#D2 已就地修入蓝图（详见清单三十三）
- [ ]  🟡 **docs/spec 全仓 grep「拍号 vs 全局时刻」术语批量替换**（#1 裁定·与 D2 注释同窗·卡 P0-7 D4 写调用点前必做）
- [ ]  🟢 **B1b/B1c 蓝图回写两行**（随 P0-5 B1 接线时顺手）
- [ ]  🟢 **C1 豁免 docs/spec 同步**（清单三十三）：1.7「封存礼包谢幕卡（元层只读调用·非写类设置·不受裁决六组边界约束·保持拍边界执行）」蓝图已补注·剩 docs/spec 口径同步
- [ ]  🟢 **4.x 总表九处 pending diff 与 P0-1 补丁 PR 同批合入**（清单三十三约束·否则一致性脚本永远红）
- [ ]  🟢 **三十「弃用清单」补节 + 6.42 importer D2 标注 + 三十总纲四 E1 一行**（随文档同步纪律清账批）

## P1（随蓝图 P1 任务带走）

- [ ]  🔴 **重玩性必须来自"作者期/离线的非确定性"，绝不能是"运行期非确定性"**（Aldabra：精确法每次出同一组 quest→无重玩；GA 出多样集→可重玩。但你的铁律是 runTick 确定性）。治 bug：为了"每周目不同"误把随机塞进结算管线 → 破坏可复现/指纹。正解：同一存档+种子永远可复现，多样性放在生成期/选种期。这条要写进设计决议，否则会和确定性核心打架。
- [ ]  🟡 **【F篇】** 规模化 LLM 推理成本（估算每天数千美元）被原文无视。→ 并入你 A 篇已立的成本红线。
- [ ]  🟡 **【F篇】** "关键帧 + 插值"降本：重要节点全量 LLM，过场/次要描述用廉价插值，估算约 4× 降本，还能沿大概率路径预生成。
- [ ]  🟢 **【Matrix-Game篇】** 17B 视频扩散成本对"确定性核 + 薄文本 LLM"完全不适用。→ 价值仅在提醒别被世界模型 hype 带偏。
- [ ]  🟡 **本地小模型校验收益兑现**（PANGeA #4）：P1 玩家自带 key 直连时验证小模型经校验闸可用。
- [ ]  🟡 **离线生成 vs 在线执行分离**（quests 篇：OQG 预处理跑 GA 出全量任务，GM 实时只执行）。对接你"LLM 非阻塞 + 内容预生成"。治 bug：把重生成放进 runTick 造成实时阻塞。生成态(离线/作者期) 与 结算态(在线/确定性) 要分层。
- [ ]  🟡 **张力曲线 / story arc 作为生成的 fitness 引导**（quests 篇 T={(kill,+),(save,−),(=)} 每个 operator 标张力增减；GA 按 story arc 拟合）。治 bug：你若缺显式**节奏/张力模型**，生成内容平铺直叙、无起伏。
- [ ]  🟢 **玩家模型 = 多维向量 + 距离 fitness + 小集合惩罚**（Aldabra：Durand 三维 PM；子集太小加惩罚保重玩）。可作你玩家画像/动态难度的参考；注意 DM 会把极端 PM 拉回中庸（避免角色被单一偏好锁死）。
- [ ]  🟢 **四个互相冲突的评估维度**：Adaptability / Replayability / Authorial Control / Player Autonomy——四者天然打架，需显式取舍文档。
- [ ]  🟡【世界书条件编辑器可视化·AiChat】节点连线模式 ＋ 条件命中/未命中实时面板——触发扫描器＋条件注入调试 UX（直观看哪些条目本拍命中）
- [ ]  🟡  存档安全四项(persist() + 导出存档一等公民 + iOS 七天清档三件套 + API 端点可配 CORS)
- [ ]  🟡【DSL/STscript 自动补全编辑器·Luker】fuzzy 评分＋浮层＋slash 命令补全——DSL v1 编辑 UX（前端旁路·与引擎核心弱相关·命令注册表白名单驱动补全）
- [ ]  🟡【角色卡/Soul-bundle 模块化目录＋形状一致 VFS·WorldLines/novel-master】`persona/`/`background/`/`memory/`/`rules/`/`agents/`/`trajectory/` bundle 结构 ＋ 形状一致三层模板 VFS（全局→项目→会话工作区·路径形状一致便备份/迁移/Agent 理解）——角色卡/NPC 包结构（P1）·对位律庚全树形状迁移＋出厂预设→存档级覆盖层→会话/fork 三层
- [ ]  🟡 **作者期叙事空间工具 + 抽象阶梯雏形**（WhatELSE #1，beat/scene/sequence/act/story）：接你 P1 事件池铺量，给事件包做分层创作。防 bug：你目前只有离散事件包/母题配额，缺可配抽象的故事弧编排 → 长线叙事松散。
- [ ]  🟡【UI 事实/文采分层】账本驱动状态变化加确定性视觉标记，降低「砸没砸」记混
- [ ]  🟡【沙盒模式标签 UI·6.78·2026-06-17】上帝视角／认知迷雾总开关的前端开关：插件要显示「全部 NPC 真实好感度排行／关系图谱真值」等隐藏信息时，必须经此开关（不旁路谜底隔离）；翻开即给存档盖**沙盒模式标签**（不叫「作弊模式」·观感友好）。标签 = 透明的存档诚实状态标记（入 FINGERPRINT_EXCLUDED·不进判定指纹·不破重放），玩家主权（IM2/IM3）——插件可呈现此 toggle 按钮，但须玩家显式确认开启、不得静默替玩家翻或隐藏标签；与作弊三档「沙盒档」轨迹水印同源
- [ ]  🟡【内容分工线】引擎管事实/LLM 管文采：派生数值引擎现算、真值揭示引擎给真值 LLM 润色、纯风味 LLM 自由（接多部件并存 6.41）
- [ ]  🟡【缺口6】遮蔽样式表现层实装（随播报表现层）
- [ ]  🟡【6.67】〔重试叙事〕〔重试记账〕双按钮 + AUTO「记账失败自动暂停」枚举项
- [ ]  🟡【Q/W】万人级选择器「组织级条目+个体派生」分级调平 + W3 体积闸参数回调 + W4 长线体积
- [ ]  🟡【世界书面板知情过滤·对撞⑦b】条件编辑器「命中/未命中」面板若**玩家可见须过知情过滤**（防秘密 lore 谓词剧透）；仅作者/dev 调试面板须明标「非玩家面」
- [ ]  🟡【Soul-bundle 迁移单元·对撞⑦b】Soul-bundle/角色卡目录路径形状=**律庚全树迁移单元**（路径形状随迁移保持·换底片不换照片）·与出厂预设→存档覆盖层三层同构
- [ ]  🟡【三十·卡格式·已拍板 ✅】角色卡导入器（6.42·封闭测卡 Beta）：读关系声明 → 建关系边＋双向认知档案（亲密度/认识时长/谁知谁的秘密）；既往记忆种子塞记忆层（来源「导入预设」）；导入确定性 = 同卡同主角 → 同样关系边（双宿主一致、不破 fixture/迁移恒等）
- [ ]  🟡【6.75·新功能批·本轮加入·P1】二审/玩家代理回复/小剧场/观战/写权/舞台行为 + 前端三面板；闸D embedding 流派语义指纹 + 闸C 受体多代理（复用 6.55/6.22/6.16）均归 P1·详见蓝图 6.75
- [ ]  🟡【6.76·本轮加入·P1】玩家代理/转述/视角二元组/观战磕CP节拍器/防穿帮/前端三党面板 → P1；结构性 bug 归位：G1切换组边界→P0-4·G6盐隔离→P0-5·G3/G4/G7/G10/G11/M5→P0-7·G5/G9/M1-M10二审组装→P0-8·L3导入校验→P0-6·L4连播快照→P0-9。详见蓝图 6.76
- [ ]  🟡【6.76 续·硬化批·本轮加入·P1】PF1 叙事控制簇 LLM 调用（代写/二审/per-seat 渲染）纳入每拍调用预算闸；PF3 视角宿主解析失败回退全知+警示；PF4 退化输入缺省（空文风=中性默认·非法抢话档=回退最保守「玩家独占」fail-safe 向最小授权）；IM4 HTML 净化渲染时+分享/导出时都跑（收藏内容视为不可信数据）；IM6 二审旋钮/维度预设设默认玩家可覆盖（无 mod 不可关维度·除引擎安全级）；P10 小剧场 meta 泄露接 6.45 严格模式（默认接受·脑内不设防）；P11 反玛丽苏维度归 M6 统一口径；P12 二审严格度=0（跑但不退）≠ 维度全关（不跑）明文——详见蓝图 6.76 续
- [ ]  🟡 **访谈/问卷驱动的人格校准**（1000人篇：注入 2 小时访谈 → 比纯人口学/persona 高 14–15pp；reduced bias）。治 bug：P–R–B 的 δ/权重表"从哪来"——这给了可落地的校准范式（少量结构化访谈 > 标签拼人设）。落 P0-1 字段 + 校准期。
- [ ]  🟡 **成本/延迟红线**（正篇：25 个 agent 跑 2 天 = 数千美元、数天）。治 bug：每 NPC 一条 LLM 记忆流的架构在你"玩家自带 key/本地小模型"下不可行 → 反向印证你"认知档案确定性派生、LLM 非阻塞"是对的；P0-8 组装要控每 NPC token 预算。
- [ ]  🟢 **审计日志 / 记忆可见可控**（1000人篇 audit log 提案）。对照你覆写日志/tick_log 已覆盖；可考虑给玩家透明化"NPC 记得我什么"。
- [ ]  🟢【全生命周期 demo · 回归/耐久测试台·本轮排入】儿时→死亡全程 demo 作为长程 bug 排查工具：**复用纵切（[testdemo.md](http://testdemo.md)）的单拍 tick 循环**（不重写第二套）+ 打开时间/纪元推进 driver 反复调用 tick；**自身跑重放模式**（读 tick_log 冻结值·短路真实 LLM·否则上万拍又慢又贵又不确定）；压测纵切压不到的长程面：账本守恒长程不漂移／跨纪元·世代 RNG 恒等／快照 ring buffer·GC 长程淘汰／离场演化确定性；**触发 = 长时程功能（纪元/快进/世代/离场演化）落地后**才开工·指定产出 AA8（跨版本段重放×难度段切换）/AA10（宿主世代×引擎世代双层乱序）fixture——**现在只设计登记·不与纵切并行开工**（防范围反弹回横向）
- [ ]  🟢 **明确区分 player-proxy 的两种身份**：6.76 的玩家代理是**玩法功能**，WhatELSE 的是**测试工具**，别混。
- [ ]  🟢【客户端本地 token 计数预览·Luker】adapter 命中即本地算·否则 HTTP 兜底——不烧 API 的 token/成本预览（接预算控制台 6.64·快进 token 预告辅助）
- [ ]  🟢【本地 token 计数=估算·对撞②b】客户端本地 token 计数标「估算」·不进账·与真 API 计费双轨偏差不喂体积闸硬判（仅预算预览）
- [ ]  🟢【⑪⑮·六·三十之二 已贴入】L2 绑定矩阵规格一行：对 A/B/C 全形态开放（微变体 ≠ 形态 C 专利）；⑮ 导入器原生通道与 L1 通道同窗交付（混合包合法·导入确定性=同卡同主角同结果断言进 fixture）
- [ ]  🟢【H7】百年人生数值预算表调平；【J2b】振荡遥测阈值调平；【6.62】纪元长链仲裁性能
    - [ ]  🟢【G9a/G9b/G9d】fork 配额/修剪、换角计数器跨线扩展位、立碑配额 UX

### **🖥️ 前端防卡顿 · 惰性加载角色库 + 渲染虚拟化（评估 · 现状缺显式条目 · 落点 P1 前端面板）**

**现状：排程内无「惰性加载角色库」显式开关**。已覆盖的是**引擎计算侧**规模（涟漪/认知 O(N²) 缓冲上限·触发扫描器索引化·实体/历史 GC·NPC 持有数据 GC），**非 UI 渲染侧**。SillyTavern 卡顿源 = 一次性渲染全角色列表/全头像/长楼层 DOM 膨胀，属**前端渲染**问题（P1 前端面板）、与引擎计算解耦。**可实现·且引擎已天然支持**：「能派生不存储」+ 派生视图现算 + per-seat 投影/切片 → 面板本就是按需派生视图，可只渲染视口内条目。下列前端虚拟化项此前未单列、本轮补登 P1。

## 🍺 P1 · 酒馆功能行为/前端（接口已 P0 冻结）

- [ ]  🟡【酒馆·已设计行为】回应长度/流式/上下文上限/采样参数/思维链档/swipe重roll/检查点故事分支/扮演代写/继续+自动继续/导入导出存档/群组聊天/提示词组装/PHI末尾指令/分词器——schema 已预埋·行为落地（走已冻结的重试主权 6.67/楼层三律 6.69/调用注册表）
- [ ]  🟡  代写/转述/视角二元组/观战磕CP节拍器/防穿帮 + 前端三党面板（零/半/抢一键切）；PF1/PF3/PF4 性能预算与退化缺省；IM4/IM6 HTML 净化与维度可覆盖；P10/P11/P12 小剧场 meta 严格模式·反玛丽苏归 M6·严格度0≠维度全关
- [ ]  🟡 **最可能的缺口：NPC 的"学习/技能积累/成长"组件**。综述把 learning 列为六大核心之一，而你现有蓝图里 NPC 多为派生+衰减，缺显式"经验→能力提升"回路。人生模拟器里 NPC 若完全不成长会显假。建议明确排期（大概率 P2），或显式声明"本版 NPC 不学习"作为已知取舍。
- [ ]  🟡【酒馆·端点直连/用量】端点+自带 key 浏览器直连(CORS) + 提示词项目化/token 用量查看（预算控制台计量+web-debug）
- [ ]  🟡【酒馆·目标系统】AI 目标/任务系统（Objective）=引擎状态（任务树存状态层）+完成判定走判定/谓词+当前任务组装器确定性注入——免轮询·可回放·与账本一致（不采用 ST「每 N 条问 LLM 完成没」昂贵 fail-open）；隐藏任务=不展示状态字段
- [ ]  🟡【惰性加载角色库】角色/NPC 面板列表虚拟滚动（windowing·只渲染视口内行）+ 分页/无限滚动 + 头像/立绘 lazy-load（IntersectionObserver·进视口才取）+ 详情按需派生（点开才现算认知档案·非预渲全 800 条）
- [ ]  🟡【派生面板按需现算】状态栏/关系网/涟漪面板走「派生视图现算」而非订阅全量（接「派生视图 vs 落账事件边界」·读快照切片·只算可见实体）
- [ ]  🟡【长楼层 DOM 削减】叙事流楼层虚拟化（仅渲染近 K 楼·远楼折叠/卸载 DOM·接 6.69 楼层三律·展示层卸载不碰真值）+ 图片/媒体 lazy-load
- [ ]  🟢【索引/缓存提示】大列表搜索/筛选走预建索引（接触发扫描器索引化·复用同口径）·避免每帧全表线性扫
- [ ]  🟢 **文档**：「事实/呈现两分」三宪法（空间事实唯一呈现 per-seat 分流 / 时间事实可重放呈现 frozen+允许漂移 / 信任 mod 只能加内容不能夺主权）写进修订决议设计原则
- [ ]  🟢【渲染预算守护】前端帧预算（一帧渲染条目上限·超出排队下一帧）防主线程长任务卡 UI——纯展示层·不进指纹·非阻塞
- [ ]  🟢【酒馆·GAL皮肤层】情绪立绘/服装切换/逐字动画+Blip + Live2D/VRM(口型同步/眨眼/碰撞箱) + Dynamic Audio 情绪 BGM/环境音——全由同一情绪键(P0-1 字段)统一驱动·渲染层确定性套样式·经 IM4 净化
- [ ]  🟢【酒馆·排版皮肤层】叙事美化/排版皮肤层：玩家选皮肤(不写正则)·mod 写声明式样式规则表+自定义展示部件渲染模板(属性卡/状态条)·引擎按结构化已知段确定性套样式·IM4 净化+作用域隔离·可回放零 XSS
- [ ]  🟢【酒馆·其余便利】LaTeX/KaTeX 公式渲染 / 快捷键 / Markdown 快捷键 / 编辑消息(限 A 桶·走 6.76 二审·绝不改账本) / 作者注释分流(风格→A 桶 / 临时世界状态→引擎状态层) / CFG 比例(经附加采样透传) / auto-swipe 质量门控 / 禁止外部媒体(接 IM4) / 替代问候语多开局(已并入开场白 6.72) / 复制·私人注释

**→ P1/P2 · 条件编译实现（隔离就位后·改造成本可控·不留技术债）**

- [ ]  🟡【BASELINE·构建剔除】Webpack/Vite 插件按 BUILD_VARIANT 切换 NSFW 模块导入路径（full=真模块·sfw/light=空桩）→ Tree Shaking/dead-code elimination 把 explicit/community 串与破限引子剔出 bundle（**构建期非运行时隐藏**·防逆向/扫描命中）；运行时 baseline 门作构建门冗余兜底
- [ ]  🟡【BASELINE·字段条件剔除】允许玩家覆盖SystemPrompt?/玩家SystemPrompt覆盖?/assistant预填?/破限引子? 在 NSFW_BASELINE!=='full' 时 discriminated union 构建期不含该字段族（条件类型+构建剔除·schema 白名单天然支持）
- [ ]  🟡【BASELINE·发布矩阵+CI 反向断言】构建脚本三 target（sfw/light/full）·CI grep 校验 sfw/light bundle **零** explicit/community 串与破限引子（反向断言·同六禁 CI 风格）；GitHub release=full·区域 app store=按地区选 sfw/light·docs/spec 明牌 baseline 语义与「开源版无限制」声明

## P2（接口已 P0 冻结 · 实装后置）

- [ ]  🟡【检索反馈闭环·PeroCore】Jaccard/共现零 Token 判记忆是否被用 → quality± → 在线训练召回权重（⚠ 非确定性·必旁路·**绝不喂判定/记账 R2-b**·叙事侧召回质量优化）
- [ ]  🟡 **【One Life篇】** 探索是瓶颈：随机策略常卡在科技树、发现不了必要前置。→ 你的内容解锁需要引导机制，不能纯靠玩家瞎试。
- [ ]  🟢 **【清单篇】** 用其机制分类（planning/memory/multi-agent/world-model/tool-use/role-play/self-improvement/generation）当覆盖度 checklist，核对没漏掉整类机制。
- [ ]  🟡 单叙事流×per-seat 视角/抢话矛盾（按席位分别渲染 或 视角降房间级共识·须拍）；MP1–MP9（MP3 视角⊥知情不得借权=安全级·P0 schema 注释先钉）；O1/O4 多人小剧场/观战按席位知情过滤·选项 per-seat 呈现；「事实/呈现两分」宪法接口守则与 C5 per-seat 投影同处冻结
- [ ]  🟡【6.66】离场演化调度器（纠缠闭包计算+切段）——闭包雪球风险靠弱边阈值压住
- [ ]  🟡 P2：内联媒体多模态输入管线实装（详规见 P0-5 全量清单〇之四）
- [ ]  🟡【C 批全量·6.53】多人/AI 同席实装（席位表/屏障/仲裁/托管/投影推送）
- [ ]  🟡【D 批·6.36/6.54】多域穿越与封存解封实装
- [ ]  🟡【6.62】纪元系统内容层（装配器/策展/歌单）
- [ ]  🟡【缺口2】骰池分支实装——⚠ 拍生命周期需加「等待分配」态，见审计 #15
- [ ]  🟡 **叙事空间工具升格为 mod 制作工具**（WhatELSE #1）：抽象阶梯并入六类包（事件包/NPC 包）制作规范。
- [ ]  🟢 **角色/信念定义开放给 mod**（P–R–B 可迁移性）：NPC 包/机制 mod 可自定义角色→信念映射，校验沿用五道闸 + 知情过滤。
- [ ]  🟢 **player-proxy 测试工具开放给 mod 作者**（WhatELSE #3）做 mod 内容回归。
- [ ]  🟢【ReflectionService 记忆维护＋一键回滚·PeroCore】7 类维护 ＋ `MaintenanceRecord` 一键回滚——接实体/历史 GC 通道 ＋ N1 跨周目摘要有界 ＋ 覆写日志可回滚
- [ ]  🟢【六家聊天记录导入适配器·Luker】Ooba/Agnai/CAI/KoboldLite/Chub/Risu——导入闸格式覆盖扩展（对位 TauriTavern `chat_format_importers` 同六家·与卡/世界书导入对称）
- [ ]  🟢【analyze-complexity 规划旁路·novel-master】AI 评分 1–10＋推荐拆分数＋JSON 报告——偏规划工具·若做归叙事侧旁路（与引擎核心弱相关）
- [ ]  🟢【颗粒度递进叙事 pipeline·Yoshi 类脑】周大纲→轮次细纲→草稿→成品 ＋ 每轮 400 字客观小结归档——对位阶段递进＋跨周目摘要（偏内容生产工作流·叙事侧）
- [ ]  🟢【Q 批】遗忘显式动词占位；【E7】终局揭示预设开关；【6.45】玩家 meta 知识严格模式
- [ ]  🟢【AA7·四 已贴入】多人工单预写「悔棋/读档默认房主说了算、房规可改投票」（机制零新件·纯产品规则）

---

- [ ]  🟡【6.76 续·硬化批·本轮加入·P2 多人】P2 单叙事流×per-seat 视角/抢话矛盾（按席位分别渲染·复用 C5 per-seat 投影 或 视角/人称降房间级共识·须拍）；MP1 代写走意图屏障后（抢话席位先交「授权代写」占位意图）/ MP2 观战快播降 per-seat 呈现回放速度不碰世界钟 / MP3 视角⊥知情·可见性用观看者自己知情名单·不得借他人主角知情权（安全级提权洞·**P0 schema 注释先钉**）/ MP4 二审维度分呈现层 per-seat·事实层全局唯一 / MP5 掉线 AI 托管强保守冻结重大决策 / MP6 swipe 默认只重渲自己·重掷共享事实走房主权/投票 / MP7 互斥意图 C2 仲裁定序 / MP8 投影分流元泄露开关（默认 per-seat 自律）/ MP9 小剧场 per-seat 私有旁路；O1 多人小剧场/观战快照按席位知情名单过滤（防上帝视角泄露他人隐私·隐私级）/ O4 可交互选项=事实派生+per-seat 知情过滤呈现；**「事实/呈现两分」宪法接口守则**（空间事实全局唯一·呈现 per-seat 分流）与 C5 per-seat 投影同处冻结——详见蓝图 6.76 续

## 🍺 P2 · 酒馆功能实装（接口已 P0 冻结）

- [ ]  🟠【酒馆·多模态实装】内联媒体输入多模态管线：组装器媒体条目编码(base64/url/file) + 媒体净化(IM4) + 多模态能力降级链(无 vision→caption)——与 6.70 媒介「输出」区分(此为喂 LLM「输入」)
- [ ]  🟠【酒馆·生图实装】图像生成（SD/FLUX/DALL-E/ComfyUI）：组装器按当前实体+情境拼生图提示词(映射 GAL 皮肤层) + 双模式(注入模式主 AI 写标签 / 独立生图模式另配便宜模型·走旁路调用类型) + 排除状态栏 HTML 不参与分析
- [ ]  🟠【酒馆·语音实装】TTS+STT+RVC 语音皮肤：源可配置 + 情绪键驱动动态声线 + 非阻塞旁路(RVC 不流式) + 语言按 locale(本地化铁律)
- [ ]  🟠【酒馆·RAG实装】RAG 向量检索管线：embedding 可配置→分块→相似度 topK(阈值过滤)→注入；图扩散召回+DPP 多样性+多意图拆分+客观/主观双阶段提取(喂真相认知分层)·召回质量优化不改架构；中文双坑(中文 embedding+按字符分块)
- [ ]  🟡【酒馆·并发编排实装】并发调用编排(叙事+生图/语音并行)·复用调用世代号·作用域仅呈现层·并发度纳入预算闸
- [ ]  🟠【酒馆·剧本模式·候选待定】剧本/导演模式（预制剧情线+自由度旋钮）：锚点(must-happen)+禁忌(must-not-happen)=剧情节点前置/禁止谓词(引擎状态)·组装器确定性注入+判定层裁触禁忌·不照搬 WestWorld 每回合调导演 AI·属产品方向判断非引擎缺口
- [ ]  🟢【检索反馈闭环旁路·对撞⑦b】检索反馈闭环在线训练=非确定性·**必旁路**（绝不进指纹/重放/判定·已知 R2-b 再钉）——召回质量优化不得反哺账本

---

---

###

---

#

[AA3 · DoD 基准面豁免清单半天会**会议目标**：对每一类旧卡 / 旧酒馆行为逐条打三选一标签，锁定 DoD 基准面豁免清单。](https://app.notion.com/p/AA3-DoD-DoD-dd9fd7a815a24ea7881dbc3b6bcad6ae?pvs=21)

[动词表 V批设计 · V3 目标槽 × V5 对称性 × 越界 × 植入 meta](https://app.notion.com/p/V-V3-V5-meta-732c2315e4834e02bccb1cca237be022?pvs=21)

[P0-6 五道闸主战场 + 导入闸 — 批次切分与排程](https://app.notion.com/p/P0-6-daf72af9f4934e6fa5aaad650a9474fe?pvs=21)